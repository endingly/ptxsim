// Generated by python/scripts/gen_all.py. Do not edit.
// Generated at: omitted (set SOURCE_DATE_EPOCH for a reproducible timestamp)

#include "resolved_ir.gen.hpp"

namespace ptx_frontend::resolved_ir {

template <>
std::expected<Mov, ResolveDiagnostic>
resolve<Mov>(const syntax_ast::AstInstruction &ast,
             const ResolveContext *context) {
  const auto selected_variant = selectVariant<Mov>(ast);
  if (!selected_variant)
    return std::unexpected(selected_variant.error());

  auto fields = resolve_fields(
      ast, Mov::get_syntax_descriptor(), Mov::get_resolved_descriptor(),
      magic_enum::enum_name(*selected_variant), context);
  if (!fields)
    return std::unexpected(fields.error());

  if (fields->variant_name == "Scalar") {
    if (fields->operand_layout.value == 0) {
      return Mov{.execution_predicate = std::move(fields->execution_predicate),
                 .variant = Mov::Scalar{
                     .operand_layout = fields->operand_layout,
                     .type = resolved_modifier<ScalarType>(*fields, "type"),
                     .operands =
                         Mov::Scalar::Operands{
                             Mov::Scalar::ScalarOperands{
                                 .dst = resolved_operand<ResolvedRegisterRef>(
                                     *fields, "dst"),
                                 .src = resolved_operand<ResolvedMovSource>(
                                     *fields, "src"),
                             },
                         },
                 }};
    }
    if (fields->operand_layout.value == 1) {
      return Mov{
          .execution_predicate = std::move(fields->execution_predicate),
          .variant = Mov::Scalar{
              .operand_layout = fields->operand_layout,
              .type = resolved_modifier<ScalarType>(*fields, "type"),
              .operands =
                  Mov::Scalar::Operands{
                      Mov::Scalar::PackOperands{
                          .dst = resolved_operand<ResolvedRegisterRef>(*fields,
                                                                       "dst"),
                          .src = resolved_operand<ResolvedRegisterVector>(
                              *fields, "src"),
                      },
                  },
          }};
    }
    if (fields->operand_layout.value == 2) {
      return Mov{
          .execution_predicate = std::move(fields->execution_predicate),
          .variant = Mov::Scalar{
              .operand_layout = fields->operand_layout,
              .type = resolved_modifier<ScalarType>(*fields, "type"),
              .operands =
                  Mov::Scalar::Operands{
                      Mov::Scalar::UnpackOperands{
                          .dst = resolved_operand<ResolvedRegisterVector>(
                              *fields, "dst"),
                          .src = resolved_operand<ResolvedRegisterRef>(*fields,
                                                                       "src"),
                      },
                  },
          }};
    }
    throw ResolveException("Unknown Mov::Scalar operand layout.");
  }
  if (fields->variant_name == "Pred") {
    return Mov{.execution_predicate = std::move(fields->execution_predicate),
               .variant = Mov::Pred{
                   .operand_layout = fields->operand_layout,
                   .dst = resolved_operand<ResolvedPredicate>(*fields, "dst"),
                   .src = resolved_operand<ResolvedPredicate>(*fields, "src"),
               }};
  }

  throw ResolveException("Unknown Mov variant in resolved field table.");
}

template <>
std::expected<Ld, ResolveDiagnostic>
resolve<Ld>(const syntax_ast::AstInstruction &ast,
            const ResolveContext *context) {
  const auto selected_variant = selectVariant<Ld>(ast);
  if (!selected_variant)
    return std::unexpected(selected_variant.error());

  auto fields = resolve_fields(
      ast, Ld::get_syntax_descriptor(), Ld::get_resolved_descriptor(),
      magic_enum::enum_name(*selected_variant), context);
  if (!fields)
    return std::unexpected(fields.error());

  if (fields->variant_name == "GenericScalar") {
    return Ld{
        .execution_predicate = std::move(fields->execution_predicate),
        .variant = Ld::GenericScalar{
            .operand_layout = fields->operand_layout,
            .semantics =
                resolved_modifier<MemoryConsistency>(*fields, "semantics"),
            .scope = resolved_modifier<MemoryScope>(*fields, "scope"),
            .mmio = resolved_modifier<bool>(*fields, "mmio"),
            .cache = resolved_modifier<CacheOperator>(*fields, "cache"),
            .type = resolved_modifier<ScalarType>(*fields, "type"),
            .dst = resolved_operand<ResolvedRegisterRef>(*fields, "dst"),
            .address = resolved_operand<ResolvedAddress>(*fields, "address"),
        }};
  }
  if (fields->variant_name == "ExplicitScalar") {
    return Ld{
        .execution_predicate = std::move(fields->execution_predicate),
        .variant = Ld::ExplicitScalar{
            .operand_layout = fields->operand_layout,
            .state_space =
                resolved_modifier<MemoryStateSpace>(*fields, "state_space"),
            .cache = resolved_modifier<CacheOperator>(*fields, "cache"),
            .semantics =
                resolved_modifier<MemoryConsistency>(*fields, "semantics"),
            .scope = resolved_modifier<MemoryScope>(*fields, "scope"),
            .mmio = resolved_modifier<bool>(*fields, "mmio"),
            .type = resolved_modifier<ScalarType>(*fields, "type"),
            .dst = resolved_operand<ResolvedRegisterRef>(*fields, "dst"),
            .address = resolved_operand<ResolvedAddress>(*fields, "address"),
        }};
  }
  if (fields->variant_name == "GenericVector") {
    return Ld{
        .execution_predicate = std::move(fields->execution_predicate),
        .variant = Ld::GenericVector{
            .operand_layout = fields->operand_layout,
            .semantics =
                resolved_modifier<MemoryConsistency>(*fields, "semantics"),
            .scope = resolved_modifier<MemoryScope>(*fields, "scope"),
            .cache = resolved_modifier<CacheOperator>(*fields, "cache"),
            .vector = resolved_modifier<VectorArity>(*fields, "vector"),
            .type = resolved_modifier<ScalarType>(*fields, "type"),
            .dst = resolved_operand<ResolvedRegisterVector>(*fields, "dst"),
            .address = resolved_operand<ResolvedAddress>(*fields, "address"),
        }};
  }
  if (fields->variant_name == "ExplicitVector") {
    return Ld{
        .execution_predicate = std::move(fields->execution_predicate),
        .variant = Ld::ExplicitVector{
            .operand_layout = fields->operand_layout,
            .state_space =
                resolved_modifier<MemoryStateSpace>(*fields, "state_space"),
            .cache = resolved_modifier<CacheOperator>(*fields, "cache"),
            .semantics =
                resolved_modifier<MemoryConsistency>(*fields, "semantics"),
            .scope = resolved_modifier<MemoryScope>(*fields, "scope"),
            .vector = resolved_modifier<VectorArity>(*fields, "vector"),
            .type = resolved_modifier<ScalarType>(*fields, "type"),
            .dst = resolved_operand<ResolvedRegisterVector>(*fields, "dst"),
            .address = resolved_operand<ResolvedAddress>(*fields, "address"),
        }};
  }

  throw ResolveException("Unknown Ld variant in resolved field table.");
}

template <>
std::expected<St, ResolveDiagnostic>
resolve<St>(const syntax_ast::AstInstruction &ast,
            const ResolveContext *context) {
  const auto selected_variant = selectVariant<St>(ast);
  if (!selected_variant)
    return std::unexpected(selected_variant.error());

  auto fields = resolve_fields(
      ast, St::get_syntax_descriptor(), St::get_resolved_descriptor(),
      magic_enum::enum_name(*selected_variant), context);
  if (!fields)
    return std::unexpected(fields.error());

  if (fields->variant_name == "GenericScalar") {
    return St{
        .execution_predicate = std::move(fields->execution_predicate),
        .variant = St::GenericScalar{
            .operand_layout = fields->operand_layout,
            .semantics =
                resolved_modifier<MemoryConsistency>(*fields, "semantics"),
            .scope = resolved_modifier<MemoryScope>(*fields, "scope"),
            .mmio = resolved_modifier<bool>(*fields, "mmio"),
            .cache = resolved_modifier<CacheOperator>(*fields, "cache"),
            .type = resolved_modifier<ScalarType>(*fields, "type"),
            .address = resolved_operand<ResolvedAddress>(*fields, "address"),
            .src = resolved_operand<ResolvedRegisterRef>(*fields, "src"),
        }};
  }
  if (fields->variant_name == "ExplicitScalar") {
    return St{
        .execution_predicate = std::move(fields->execution_predicate),
        .variant = St::ExplicitScalar{
            .operand_layout = fields->operand_layout,
            .state_space =
                resolved_modifier<MemoryStateSpace>(*fields, "state_space"),
            .cache = resolved_modifier<CacheOperator>(*fields, "cache"),
            .semantics =
                resolved_modifier<MemoryConsistency>(*fields, "semantics"),
            .scope = resolved_modifier<MemoryScope>(*fields, "scope"),
            .mmio = resolved_modifier<bool>(*fields, "mmio"),
            .type = resolved_modifier<ScalarType>(*fields, "type"),
            .address = resolved_operand<ResolvedAddress>(*fields, "address"),
            .src = resolved_operand<ResolvedRegisterRef>(*fields, "src"),
        }};
  }
  if (fields->variant_name == "GenericVector") {
    return St{
        .execution_predicate = std::move(fields->execution_predicate),
        .variant = St::GenericVector{
            .operand_layout = fields->operand_layout,
            .semantics =
                resolved_modifier<MemoryConsistency>(*fields, "semantics"),
            .scope = resolved_modifier<MemoryScope>(*fields, "scope"),
            .cache = resolved_modifier<CacheOperator>(*fields, "cache"),
            .vector = resolved_modifier<VectorArity>(*fields, "vector"),
            .type = resolved_modifier<ScalarType>(*fields, "type"),
            .address = resolved_operand<ResolvedAddress>(*fields, "address"),
            .src = resolved_operand<ResolvedRegisterVector>(*fields, "src"),
        }};
  }
  if (fields->variant_name == "ExplicitVector") {
    return St{
        .execution_predicate = std::move(fields->execution_predicate),
        .variant = St::ExplicitVector{
            .operand_layout = fields->operand_layout,
            .state_space =
                resolved_modifier<MemoryStateSpace>(*fields, "state_space"),
            .cache = resolved_modifier<CacheOperator>(*fields, "cache"),
            .semantics =
                resolved_modifier<MemoryConsistency>(*fields, "semantics"),
            .scope = resolved_modifier<MemoryScope>(*fields, "scope"),
            .vector = resolved_modifier<VectorArity>(*fields, "vector"),
            .type = resolved_modifier<ScalarType>(*fields, "type"),
            .address = resolved_operand<ResolvedAddress>(*fields, "address"),
            .src = resolved_operand<ResolvedRegisterVector>(*fields, "src"),
        }};
  }

  throw ResolveException("Unknown St variant in resolved field table.");
}

namespace checker {

template <>
CheckResult check<Mov>(const Mov &instruction, const Context &context) {
  const auto check_scalar = [&](const Mov::Scalar &selected) -> CheckResult {
    const std::array<FieldView, 1> fields = {{FieldView{
        .field_id = "type",
        .bool_value = std::nullopt,
        .cache_operator = std::nullopt,
        .scalar_type = selected.type.value,
        .vector_arity = std::nullopt,
        .memory_state_space = std::nullopt,
        .memory_consistency = std::nullopt,
        .memory_scope = std::nullopt,
        .locations = selected.type.locs,
    }}};
    const std::array<ModifierValueView, 1> modifier_values = {
        {ModifierValueView{
            .kind_id = "type",
            .value_kind = checker::ModifierValueKind::ScalarType,
            .bool_value = false,
            .scalar_type = selected.type.value,
            .rounding_mode = RoundingMode::Invalid,
            .cache_operator = CacheOperator::Unspecified,
            .vector_arity = VectorArity::Invalid,
            .memory_state_space = MemoryStateSpace::Invalid,
            .memory_consistency = MemoryConsistency::Omitted,
            .memory_scope = MemoryScope::None,
            .is_present = true,
            .locations = selected.type.locs,
        }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Mov::get_checker_descriptor(), "Scalar", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Mov::get_checker_descriptor().variants[0].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const auto check_mov_scalar_scalar_operands =
        [&](const Mov::Scalar::ScalarOperands &payload) -> CheckResult {
      if (selected.operand_layout.value != 0) {
        return std::unexpected(CheckDiagnostics{CheckDiagnostic{
            .kind = CheckDiagnosticKind::OperandLayoutPayloadMismatch,
            .range = context.instruction_range,
            .message =
                "Resolved operand-layout tag and payload alternative disagree.",
        }});
      }
      const std::array<OperandView, 2> operands = {
          {OperandView{
               .field_id = "dst",
               .actual_shape = check_end::OperandShape::Register,
               .immediate_type = std::nullopt,
               .register_type = payload.dst.value.declared_type,
               .locations = payload.dst.locs,
           },
           [&]() -> OperandView {
             if (const auto *immediate =
                     std::get_if<ResolvedImmediate>(&payload.src.value)) {
               return OperandView{
                   .field_id = "src",
                   .actual_shape = check_end::OperandShape::Immediate,
                   .immediate_type = immediate->type,
                   .register_type = std::nullopt,
                   .locations = payload.src.locs,
               };
             }
             if (const auto *register_ref =
                     std::get_if<ResolvedRegisterRef>(&payload.src.value)) {
               return OperandView{
                   .field_id = "src",
                   .actual_shape = check_end::OperandShape::Register,
                   .immediate_type = std::nullopt,
                   .register_type = register_ref->declared_type,
                   .locations = payload.src.locs,
               };
             }
             if (const auto *special_register =
                     std::get_if<ResolvedSpecialRegisterRef>(
                         &payload.src.value)) {
               const auto info = base::metadata(special_register->id);
               return OperandView{
                   .field_id = "src",
                   .actual_shape = check_end::OperandShape::SpecialRegister,
                   .immediate_type = std::nullopt,
                   .register_type = std::nullopt,
                   .special_register_type = info.element_type,
                   .special_register_id = special_register->id,
                   .value_availability =
                       AvailabilityDescriptor{
                           .minimum_ptx_version =
                               {
                                   info.minimum_ptx_major,
                                   info.minimum_ptx_minor,
                               },
                           .minimum_sm_version = info.minimum_sm,
                       },
                   .value_name = special_register->spelling,
                   .locations = payload.src.locs,
               };
             }
             if (const auto *function =
                     std::get_if<ResolvedFunctionRef>(&payload.src.value)) {
               return OperandView{
                   .field_id = "src",
                   .actual_shape = check_end::OperandShape::Symbol,
                   .immediate_type = std::nullopt,
                   .register_type = std::nullopt,
                   .value_availability = function->address_availability,
                   .value_name = function->spelling,
                   .locations = payload.src.locs,
               };
             }
             if (const auto *symbol =
                     std::get_if<ResolvedSymbolRef>(&payload.src.value)) {
               return OperandView{
                   .field_id = "src",
                   .actual_shape = check_end::OperandShape::Symbol,
                   .immediate_type = std::nullopt,
                   .register_type = std::nullopt,
                   .value_availability = symbol->address_availability,
                   .value_name = symbol->spelling,
                   .locations = payload.src.locs,
               };
             }
             // An offset address keeps its value requirements on the
             // symbol base, so expose those requirements through the same
             // operand view used for a direct symbol source.
             const auto &address = std::get<ResolvedAddress>(payload.src.value);
             const auto *symbol = std::get_if<ResolvedSymbolRef>(&address.base);
             return OperandView{
                 .field_id = "src",
                 .actual_shape = check_end::OperandShape::Address,
                 .immediate_type = std::nullopt,
                 .register_type = std::nullopt,
                 .value_availability = symbol == nullptr
                                           ? std::nullopt
                                           : symbol->address_availability,
                 .value_name = symbol == nullptr
                                   ? std::string_view{}
                                   : std::string_view{symbol->spelling},
                 .locations = payload.src.locs,
             };
           }()}};
      return check_operands(Mov::get_resolved_descriptor()
                                .variants[0]
                                .operand_layouts[0]
                                .bindings,
                            fields, operands,
                            Mov::get_checker_descriptor()
                                .variants[0]
                                .operand_type_compatibilities,
                            context);
    };
    static_assert(
        detail::VariantCheckFunction<decltype(check_mov_scalar_scalar_operands),
                                     Mov::Scalar::ScalarOperands>);

    const auto check_mov_scalar_pack_operands =
        [&](const Mov::Scalar::PackOperands &payload) -> CheckResult {
      if (selected.operand_layout.value != 1) {
        return std::unexpected(CheckDiagnostics{CheckDiagnostic{
            .kind = CheckDiagnosticKind::OperandLayoutPayloadMismatch,
            .range = context.instruction_range,
            .message =
                "Resolved operand-layout tag and payload alternative disagree.",
        }});
      }
      const std::array<OperandView, 2> operands = {
          {OperandView{
               .field_id = "dst",
               .actual_shape = check_end::OperandShape::Register,
               .immediate_type = std::nullopt,
               .register_type = payload.dst.value.declared_type,
               .locations = payload.dst.locs,
           },
           [&]() -> OperandView {
             OperandView view{
                 .field_id = "src",
                 .actual_shape = check_end::OperandShape::Vector,
                 .vector_arity =
                     static_cast<uint8_t>(payload.src.value.elements.size()),
                 .locations = payload.src.locs,
             };
             size_t index = 0;
             for (const auto &element : payload.src.value.elements) {
               if (index >= view.vector_element_types.size())
                 break;
               if (element) {
                 view.vector_element_types[index] =
                     element->declared_type.value_or(ScalarType::Invalid);
               } else {
                 ++view.vector_sink_count;
               }
               ++index;
             }
             return view;
           }()}};
      return check_operands(Mov::get_resolved_descriptor()
                                .variants[0]
                                .operand_layouts[1]
                                .bindings,
                            fields, operands,
                            Mov::get_checker_descriptor()
                                .variants[0]
                                .operand_type_compatibilities,
                            context);
    };
    static_assert(
        detail::VariantCheckFunction<decltype(check_mov_scalar_pack_operands),
                                     Mov::Scalar::PackOperands>);

    const auto check_mov_scalar_unpack_operands =
        [&](const Mov::Scalar::UnpackOperands &payload) -> CheckResult {
      if (selected.operand_layout.value != 2) {
        return std::unexpected(CheckDiagnostics{CheckDiagnostic{
            .kind = CheckDiagnosticKind::OperandLayoutPayloadMismatch,
            .range = context.instruction_range,
            .message =
                "Resolved operand-layout tag and payload alternative disagree.",
        }});
      }
      const std::array<OperandView, 2> operands = {
          {[&]() -> OperandView {
             OperandView view{
                 .field_id = "dst",
                 .actual_shape = check_end::OperandShape::Vector,
                 .vector_arity =
                     static_cast<uint8_t>(payload.dst.value.elements.size()),
                 .locations = payload.dst.locs,
             };
             size_t index = 0;
             for (const auto &element : payload.dst.value.elements) {
               if (index >= view.vector_element_types.size())
                 break;
               if (element) {
                 view.vector_element_types[index] =
                     element->declared_type.value_or(ScalarType::Invalid);
               } else {
                 ++view.vector_sink_count;
               }
               ++index;
             }
             return view;
           }(),
           OperandView{
               .field_id = "src",
               .actual_shape = check_end::OperandShape::Register,
               .immediate_type = std::nullopt,
               .register_type = payload.src.value.declared_type,
               .locations = payload.src.locs,
           }}};
      return check_operands(Mov::get_resolved_descriptor()
                                .variants[0]
                                .operand_layouts[2]
                                .bindings,
                            fields, operands,
                            Mov::get_checker_descriptor()
                                .variants[0]
                                .operand_type_compatibilities,
                            context);
    };
    static_assert(
        detail::VariantCheckFunction<decltype(check_mov_scalar_unpack_operands),
                                     Mov::Scalar::UnpackOperands>);

    const auto &layouts =
        Mov::get_resolved_descriptor().variants[0].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "Scalar", selected.operand_layout.value, layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Mov::get_checker_descriptor().variants[0],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto payload_check =
          std::visit(detail::Overloaded{check_mov_scalar_scalar_operands,
                                        check_mov_scalar_pack_operands,
                                        check_mov_scalar_unpack_operands},
                     selected.operands);
      if (!payload_check) {
        diagnostics.insert(diagnostics.end(), payload_check.error().begin(),
                           payload_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(
      detail::VariantCheckFunction<decltype(check_scalar), Mov::Scalar>);

  const auto check_pred = [&](const Mov::Pred &selected) -> CheckResult {
    const std::array<FieldView, 1> fields = {{FieldView{
        .field_id = "type",
        .bool_value = std::nullopt,
        .cache_operator = std::nullopt,
        .scalar_type = Mov::Pred::type,
        .vector_arity = std::nullopt,
        .memory_state_space = std::nullopt,
        .memory_consistency = std::nullopt,
        .memory_scope = std::nullopt,
        .locations = std::span<const SourceRange>{},
    }}};
    const std::array<ModifierValueView, 1> modifier_values = {
        {ModifierValueView{
            .kind_id = "type",
            .value_kind = checker::ModifierValueKind::ScalarType,
            .bool_value = false,
            .scalar_type = Mov::Pred::type,
            .rounding_mode = RoundingMode::Invalid,
            .cache_operator = CacheOperator::Unspecified,
            .vector_arity = VectorArity::Invalid,
            .memory_state_space = MemoryStateSpace::Invalid,
            .memory_consistency = MemoryConsistency::Omitted,
            .memory_scope = MemoryScope::None,
            .is_present = true,
            .locations = std::span<const SourceRange>{},
        }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Mov::get_checker_descriptor(), "Pred", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Mov::get_checker_descriptor().variants[1].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const std::array<OperandView, 2> operands = {
        {OperandView{
             .field_id = "dst",
             .actual_shape = check_end::OperandShape::Predicate,
             .immediate_type = std::nullopt,
             .register_type = selected.dst.value.register_ref.declared_type,
             .locations = selected.dst.locs,
         },
         OperandView{
             .field_id = "src",
             .actual_shape = check_end::OperandShape::Predicate,
             .immediate_type = std::nullopt,
             .register_type = selected.src.value.register_ref.declared_type,
             .locations = selected.src.locs,
         }}};
    const auto &layouts =
        Mov::get_resolved_descriptor().variants[1].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "Pred", selected.operand_layout.value, layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Mov::get_checker_descriptor().variants[1],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto operand_check = check_operands(
          layouts[selected.operand_layout.value].bindings, fields, operands,
          Mov::get_checker_descriptor()
              .variants[1]
              .operand_type_compatibilities,
          context);
      if (!operand_check) {
        diagnostics.insert(diagnostics.end(), operand_check.error().begin(),
                           operand_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(detail::VariantCheckFunction<decltype(check_pred), Mov::Pred>);

  return std::visit(detail::Overloaded{check_scalar, check_pred},
                    instruction.variant);
}

template <>
CheckResult check<Ld>(const Ld &instruction, const Context &context) {
  const auto check_generic_scalar =
      [&](const Ld::GenericScalar &selected) -> CheckResult {
    const std::array<FieldView, 5> fields = {
        {FieldView{
             .field_id = "semantics",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = selected.semantics.value,
             .memory_scope = std::nullopt,
             .locations = selected.semantics.locs,
         },
         FieldView{
             .field_id = "scope",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = selected.scope.value,
             .locations = selected.scope.locs,
         },
         FieldView{
             .field_id = "mmio",
             .bool_value = selected.mmio.value,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.mmio.locs,
         },
         FieldView{
             .field_id = "cache",
             .bool_value = std::nullopt,
             .cache_operator = selected.cache.value,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.cache.locs,
         },
         FieldView{
             .field_id = "type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = selected.type.value,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.type.locs,
         }}};
    const std::array<ModifierValueView, 5> modifier_values = {
        {ModifierValueView{
             .kind_id = "semantics",
             .value_kind = checker::ModifierValueKind::MemoryConsistency,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = selected.semantics.value,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.semantics.locs,
         },
         ModifierValueView{
             .kind_id = "scope",
             .value_kind = checker::ModifierValueKind::MemoryScope,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = selected.scope.value,
             .is_present = true,
             .locations = selected.scope.locs,
         },
         ModifierValueView{
             .kind_id = "mmio",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = selected.mmio.value,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.mmio.locs,
         },
         ModifierValueView{
             .kind_id = "cache",
             .value_kind = checker::ModifierValueKind::CacheOperator,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = selected.cache.value,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = selected.cache.value != CacheOperator::Unspecified,
             .locations = selected.cache.locs,
         },
         ModifierValueView{
             .kind_id = "type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = selected.type.value,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.type.locs,
         }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Ld::get_checker_descriptor(), "GenericScalar", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Ld::get_checker_descriptor().variants[0].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const std::array<OperandView, 2> operands = {
        {OperandView{
             .field_id = "dst",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.dst.value.declared_type,
             .locations = selected.dst.locs,
         },
         [&]() -> OperandView {
           const auto *symbol =
               std::get_if<ResolvedSymbolRef>(&selected.address.value.base);
           std::optional<MemoryStateSpace> effective_state_space;
           ParameterDirection parameter_direction = ParameterDirection::None;
           if (symbol != nullptr && symbol->address_state_space) {
             // Preserve the declaration-derived effective space. In
             // particular, device parameters may produce local rather
             // than declaration-space addresses in other instructions.
             switch (*symbol->address_state_space) {
             case syntax_ast::AstStateSpace::Global:
               effective_state_space = MemoryStateSpace::Global;
               break;
             case syntax_ast::AstStateSpace::Shared:
               effective_state_space = MemoryStateSpace::Shared;
               break;
             case syntax_ast::AstStateSpace::Local:
               effective_state_space = MemoryStateSpace::Local;
               break;
             case syntax_ast::AstStateSpace::Parameter:
               effective_state_space = MemoryStateSpace::Parameter;
               break;
             case syntax_ast::AstStateSpace::Constant:
               effective_state_space = MemoryStateSpace::Constant;
               break;
             case syntax_ast::AstStateSpace::Register:
               break;
             }
           }
           if (symbol != nullptr && symbol->declaration_kind) {
             if (*symbol->declaration_kind ==
                 binding::SymbolKind::InputParameter) {
               parameter_direction = ParameterDirection::Input;
             } else if (*symbol->declaration_kind ==
                        binding::SymbolKind::ReturnParameter) {
               parameter_direction = ParameterDirection::Return;
             } else if (*symbol->declaration_kind ==
                        binding::SymbolKind::CallParameter) {
               parameter_direction = ParameterDirection::CallArgument;
             }
           }
           std::optional<uint64_t> address_alignment;
           const auto low_bit = [](uint64_t value) {
             return value == 0 ? uint64_t{0} : value & (~value + 1);
           };
           if (symbol != nullptr) {
             address_alignment = symbol->address_alignment;
           } else if (const auto *immediate = std::get_if<ResolvedImmediate>(
                          &selected.address.value.base)) {
             address_alignment = low_bit(immediate->bits);
           }
           if (address_alignment && selected.address.value.offset) {
             const uint64_t offset_alignment =
                 low_bit(selected.address.value.offset->value.bits);
             if (offset_alignment != 0 &&
                 (*address_alignment == 0 ||
                  offset_alignment < *address_alignment))
               address_alignment = offset_alignment;
           }
           // Register, immediate, and unresolved standalone address
           // bases remain unknown; spelling is not semantic evidence.
           return OperandView{
               .field_id = "address",
               .actual_shape = check_end::OperandShape::Address,
               .immediate_type = std::nullopt,
               .register_type = std::nullopt,
               .address_state_space = effective_state_space,
               .address_alignment = address_alignment,
               .enclosing_function_kind =
                   selected.address.value.enclosing_function_kind,
               .parameter_direction = parameter_direction,
               .parameter_qualifier =
                   selected.address.value.parameter_qualifier,
               .locations = selected.address.locs,
           };
         }()}};
    const auto &layouts =
        Ld::get_resolved_descriptor().variants[0].operand_layouts;
    const auto layout_check =
        check_operand_layout_tag("GenericScalar", selected.operand_layout.value,
                                 layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Ld::get_checker_descriptor().variants[0],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto operand_check = check_operands(
          layouts[selected.operand_layout.value].bindings, fields, operands,
          Ld::get_checker_descriptor().variants[0].operand_type_compatibilities,
          context);
      if (!operand_check) {
        diagnostics.insert(diagnostics.end(), operand_check.error().begin(),
                           operand_check.error().end());
      }
      const auto consistency_check = check_memory_consistency(
          Ld::get_checker_descriptor().variants[0].memory_consistency, fields,
          operands, context);
      if (!consistency_check) {
        diagnostics.insert(diagnostics.end(), consistency_check.error().begin(),
                           consistency_check.error().end());
      }
      const auto alignment_check = check_address_alignment(
          Ld::get_checker_descriptor().variants[0].address_alignment, fields,
          operands, context);
      if (!alignment_check) {
        diagnostics.insert(diagnostics.end(), alignment_check.error().begin(),
                           alignment_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(detail::VariantCheckFunction<decltype(check_generic_scalar),
                                             Ld::GenericScalar>);

  const auto check_explicit_scalar =
      [&](const Ld::ExplicitScalar &selected) -> CheckResult {
    const std::array<FieldView, 6> fields = {
        {FieldView{
             .field_id = "state_space",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = selected.state_space.value,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.state_space.locs,
         },
         FieldView{
             .field_id = "cache",
             .bool_value = std::nullopt,
             .cache_operator = selected.cache.value,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.cache.locs,
         },
         FieldView{
             .field_id = "semantics",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = selected.semantics.value,
             .memory_scope = std::nullopt,
             .locations = selected.semantics.locs,
         },
         FieldView{
             .field_id = "scope",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = selected.scope.value,
             .locations = selected.scope.locs,
         },
         FieldView{
             .field_id = "mmio",
             .bool_value = selected.mmio.value,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.mmio.locs,
         },
         FieldView{
             .field_id = "type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = selected.type.value,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.type.locs,
         }}};
    const std::array<ModifierValueView, 6> modifier_values = {
        {ModifierValueView{
             .kind_id = "state_space",
             .value_kind = checker::ModifierValueKind::MemoryStateSpace,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = selected.state_space.value,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.state_space.locs,
         },
         ModifierValueView{
             .kind_id = "cache",
             .value_kind = checker::ModifierValueKind::CacheOperator,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = selected.cache.value,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = selected.cache.value != CacheOperator::Unspecified,
             .locations = selected.cache.locs,
         },
         ModifierValueView{
             .kind_id = "semantics",
             .value_kind = checker::ModifierValueKind::MemoryConsistency,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = selected.semantics.value,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.semantics.locs,
         },
         ModifierValueView{
             .kind_id = "scope",
             .value_kind = checker::ModifierValueKind::MemoryScope,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = selected.scope.value,
             .is_present = true,
             .locations = selected.scope.locs,
         },
         ModifierValueView{
             .kind_id = "mmio",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = selected.mmio.value,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.mmio.locs,
         },
         ModifierValueView{
             .kind_id = "type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = selected.type.value,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.type.locs,
         }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Ld::get_checker_descriptor(), "ExplicitScalar", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Ld::get_checker_descriptor().variants[1].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const std::array<OperandView, 2> operands = {
        {OperandView{
             .field_id = "dst",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.dst.value.declared_type,
             .locations = selected.dst.locs,
         },
         [&]() -> OperandView {
           const auto *symbol =
               std::get_if<ResolvedSymbolRef>(&selected.address.value.base);
           std::optional<MemoryStateSpace> effective_state_space;
           ParameterDirection parameter_direction = ParameterDirection::None;
           if (symbol != nullptr && symbol->address_state_space) {
             // Preserve the declaration-derived effective space. In
             // particular, device parameters may produce local rather
             // than declaration-space addresses in other instructions.
             switch (*symbol->address_state_space) {
             case syntax_ast::AstStateSpace::Global:
               effective_state_space = MemoryStateSpace::Global;
               break;
             case syntax_ast::AstStateSpace::Shared:
               effective_state_space = MemoryStateSpace::Shared;
               break;
             case syntax_ast::AstStateSpace::Local:
               effective_state_space = MemoryStateSpace::Local;
               break;
             case syntax_ast::AstStateSpace::Parameter:
               effective_state_space = MemoryStateSpace::Parameter;
               break;
             case syntax_ast::AstStateSpace::Constant:
               effective_state_space = MemoryStateSpace::Constant;
               break;
             case syntax_ast::AstStateSpace::Register:
               break;
             }
           }
           if (symbol != nullptr && symbol->declaration_kind) {
             if (*symbol->declaration_kind ==
                 binding::SymbolKind::InputParameter) {
               parameter_direction = ParameterDirection::Input;
             } else if (*symbol->declaration_kind ==
                        binding::SymbolKind::ReturnParameter) {
               parameter_direction = ParameterDirection::Return;
             } else if (*symbol->declaration_kind ==
                        binding::SymbolKind::CallParameter) {
               parameter_direction = ParameterDirection::CallArgument;
             }
           }
           std::optional<uint64_t> address_alignment;
           const auto low_bit = [](uint64_t value) {
             return value == 0 ? uint64_t{0} : value & (~value + 1);
           };
           if (symbol != nullptr) {
             address_alignment = symbol->address_alignment;
           } else if (const auto *immediate = std::get_if<ResolvedImmediate>(
                          &selected.address.value.base)) {
             address_alignment = low_bit(immediate->bits);
           }
           if (address_alignment && selected.address.value.offset) {
             const uint64_t offset_alignment =
                 low_bit(selected.address.value.offset->value.bits);
             if (offset_alignment != 0 &&
                 (*address_alignment == 0 ||
                  offset_alignment < *address_alignment))
               address_alignment = offset_alignment;
           }
           // Register, immediate, and unresolved standalone address
           // bases remain unknown; spelling is not semantic evidence.
           return OperandView{
               .field_id = "address",
               .actual_shape = check_end::OperandShape::Address,
               .immediate_type = std::nullopt,
               .register_type = std::nullopt,
               .address_state_space = effective_state_space,
               .address_alignment = address_alignment,
               .enclosing_function_kind =
                   selected.address.value.enclosing_function_kind,
               .parameter_direction = parameter_direction,
               .parameter_qualifier =
                   selected.address.value.parameter_qualifier,
               .locations = selected.address.locs,
           };
         }()}};
    const auto &layouts =
        Ld::get_resolved_descriptor().variants[1].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "ExplicitScalar", selected.operand_layout.value, layouts.size(),
        context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Ld::get_checker_descriptor().variants[1],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto operand_check = check_operands(
          layouts[selected.operand_layout.value].bindings, fields, operands,
          Ld::get_checker_descriptor().variants[1].operand_type_compatibilities,
          context);
      if (!operand_check) {
        diagnostics.insert(diagnostics.end(), operand_check.error().begin(),
                           operand_check.error().end());
      }
      const auto consistency_check = check_memory_consistency(
          Ld::get_checker_descriptor().variants[1].memory_consistency, fields,
          operands, context);
      if (!consistency_check) {
        diagnostics.insert(diagnostics.end(), consistency_check.error().begin(),
                           consistency_check.error().end());
      }
      const auto alignment_check = check_address_alignment(
          Ld::get_checker_descriptor().variants[1].address_alignment, fields,
          operands, context);
      if (!alignment_check) {
        diagnostics.insert(diagnostics.end(), alignment_check.error().begin(),
                           alignment_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(detail::VariantCheckFunction<decltype(check_explicit_scalar),
                                             Ld::ExplicitScalar>);

  const auto check_generic_vector =
      [&](const Ld::GenericVector &selected) -> CheckResult {
    const std::array<FieldView, 5> fields = {
        {FieldView{
             .field_id = "semantics",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = selected.semantics.value,
             .memory_scope = std::nullopt,
             .locations = selected.semantics.locs,
         },
         FieldView{
             .field_id = "scope",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = selected.scope.value,
             .locations = selected.scope.locs,
         },
         FieldView{
             .field_id = "cache",
             .bool_value = std::nullopt,
             .cache_operator = selected.cache.value,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.cache.locs,
         },
         FieldView{
             .field_id = "vector",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = selected.vector.value,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.vector.locs,
         },
         FieldView{
             .field_id = "type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = selected.type.value,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.type.locs,
         }}};
    const std::array<ModifierValueView, 5> modifier_values = {
        {ModifierValueView{
             .kind_id = "semantics",
             .value_kind = checker::ModifierValueKind::MemoryConsistency,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = selected.semantics.value,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.semantics.locs,
         },
         ModifierValueView{
             .kind_id = "scope",
             .value_kind = checker::ModifierValueKind::MemoryScope,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = selected.scope.value,
             .is_present = true,
             .locations = selected.scope.locs,
         },
         ModifierValueView{
             .kind_id = "cache",
             .value_kind = checker::ModifierValueKind::CacheOperator,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = selected.cache.value,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = selected.cache.value != CacheOperator::Unspecified,
             .locations = selected.cache.locs,
         },
         ModifierValueView{
             .kind_id = "vector",
             .value_kind = checker::ModifierValueKind::VectorArity,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = selected.vector.value,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.vector.locs,
         },
         ModifierValueView{
             .kind_id = "type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = selected.type.value,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.type.locs,
         }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Ld::get_checker_descriptor(), "GenericVector", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Ld::get_checker_descriptor().variants[2].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const std::array<OperandView, 2> operands = {
        {[&]() -> OperandView {
           OperandView view{
               .field_id = "dst",
               .actual_shape = check_end::OperandShape::Vector,
               .vector_arity =
                   static_cast<uint8_t>(selected.dst.value.elements.size()),
               .locations = selected.dst.locs,
           };
           size_t index = 0;
           for (const auto &element : selected.dst.value.elements) {
             if (index >= view.vector_element_types.size())
               break;
             if (element) {
               view.vector_element_types[index] =
                   element->declared_type.value_or(ScalarType::Invalid);
             } else {
               ++view.vector_sink_count;
             }
             ++index;
           }
           return view;
         }(),
         [&]() -> OperandView {
           const auto *symbol =
               std::get_if<ResolvedSymbolRef>(&selected.address.value.base);
           std::optional<MemoryStateSpace> effective_state_space;
           ParameterDirection parameter_direction = ParameterDirection::None;
           if (symbol != nullptr && symbol->address_state_space) {
             // Preserve the declaration-derived effective space. In
             // particular, device parameters may produce local rather
             // than declaration-space addresses in other instructions.
             switch (*symbol->address_state_space) {
             case syntax_ast::AstStateSpace::Global:
               effective_state_space = MemoryStateSpace::Global;
               break;
             case syntax_ast::AstStateSpace::Shared:
               effective_state_space = MemoryStateSpace::Shared;
               break;
             case syntax_ast::AstStateSpace::Local:
               effective_state_space = MemoryStateSpace::Local;
               break;
             case syntax_ast::AstStateSpace::Parameter:
               effective_state_space = MemoryStateSpace::Parameter;
               break;
             case syntax_ast::AstStateSpace::Constant:
               effective_state_space = MemoryStateSpace::Constant;
               break;
             case syntax_ast::AstStateSpace::Register:
               break;
             }
           }
           if (symbol != nullptr && symbol->declaration_kind) {
             if (*symbol->declaration_kind ==
                 binding::SymbolKind::InputParameter) {
               parameter_direction = ParameterDirection::Input;
             } else if (*symbol->declaration_kind ==
                        binding::SymbolKind::ReturnParameter) {
               parameter_direction = ParameterDirection::Return;
             } else if (*symbol->declaration_kind ==
                        binding::SymbolKind::CallParameter) {
               parameter_direction = ParameterDirection::CallArgument;
             }
           }
           std::optional<uint64_t> address_alignment;
           const auto low_bit = [](uint64_t value) {
             return value == 0 ? uint64_t{0} : value & (~value + 1);
           };
           if (symbol != nullptr) {
             address_alignment = symbol->address_alignment;
           } else if (const auto *immediate = std::get_if<ResolvedImmediate>(
                          &selected.address.value.base)) {
             address_alignment = low_bit(immediate->bits);
           }
           if (address_alignment && selected.address.value.offset) {
             const uint64_t offset_alignment =
                 low_bit(selected.address.value.offset->value.bits);
             if (offset_alignment != 0 &&
                 (*address_alignment == 0 ||
                  offset_alignment < *address_alignment))
               address_alignment = offset_alignment;
           }
           // Register, immediate, and unresolved standalone address
           // bases remain unknown; spelling is not semantic evidence.
           return OperandView{
               .field_id = "address",
               .actual_shape = check_end::OperandShape::Address,
               .immediate_type = std::nullopt,
               .register_type = std::nullopt,
               .address_state_space = effective_state_space,
               .address_alignment = address_alignment,
               .enclosing_function_kind =
                   selected.address.value.enclosing_function_kind,
               .parameter_direction = parameter_direction,
               .parameter_qualifier =
                   selected.address.value.parameter_qualifier,
               .locations = selected.address.locs,
           };
         }()}};
    const auto &layouts =
        Ld::get_resolved_descriptor().variants[2].operand_layouts;
    const auto layout_check =
        check_operand_layout_tag("GenericVector", selected.operand_layout.value,
                                 layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Ld::get_checker_descriptor().variants[2],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto operand_check = check_operands(
          layouts[selected.operand_layout.value].bindings, fields, operands,
          Ld::get_checker_descriptor().variants[2].operand_type_compatibilities,
          context);
      if (!operand_check) {
        diagnostics.insert(diagnostics.end(), operand_check.error().begin(),
                           operand_check.error().end());
      }
      const auto consistency_check = check_memory_consistency(
          Ld::get_checker_descriptor().variants[2].memory_consistency, fields,
          operands, context);
      if (!consistency_check) {
        diagnostics.insert(diagnostics.end(), consistency_check.error().begin(),
                           consistency_check.error().end());
      }
      const auto memory_vector_check = check_memory_vector(
          Ld::get_checker_descriptor().variants[2].memory_vector, fields,
          operands, context);
      if (!memory_vector_check) {
        diagnostics.insert(diagnostics.end(),
                           memory_vector_check.error().begin(),
                           memory_vector_check.error().end());
      }
      const auto alignment_check = check_address_alignment(
          Ld::get_checker_descriptor().variants[2].address_alignment, fields,
          operands, context);
      if (!alignment_check) {
        diagnostics.insert(diagnostics.end(), alignment_check.error().begin(),
                           alignment_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(detail::VariantCheckFunction<decltype(check_generic_vector),
                                             Ld::GenericVector>);

  const auto check_explicit_vector =
      [&](const Ld::ExplicitVector &selected) -> CheckResult {
    const std::array<FieldView, 6> fields = {
        {FieldView{
             .field_id = "state_space",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = selected.state_space.value,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.state_space.locs,
         },
         FieldView{
             .field_id = "cache",
             .bool_value = std::nullopt,
             .cache_operator = selected.cache.value,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.cache.locs,
         },
         FieldView{
             .field_id = "semantics",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = selected.semantics.value,
             .memory_scope = std::nullopt,
             .locations = selected.semantics.locs,
         },
         FieldView{
             .field_id = "scope",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = selected.scope.value,
             .locations = selected.scope.locs,
         },
         FieldView{
             .field_id = "vector",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = selected.vector.value,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.vector.locs,
         },
         FieldView{
             .field_id = "type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = selected.type.value,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.type.locs,
         }}};
    const std::array<ModifierValueView, 6> modifier_values = {
        {ModifierValueView{
             .kind_id = "state_space",
             .value_kind = checker::ModifierValueKind::MemoryStateSpace,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = selected.state_space.value,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.state_space.locs,
         },
         ModifierValueView{
             .kind_id = "cache",
             .value_kind = checker::ModifierValueKind::CacheOperator,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = selected.cache.value,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = selected.cache.value != CacheOperator::Unspecified,
             .locations = selected.cache.locs,
         },
         ModifierValueView{
             .kind_id = "semantics",
             .value_kind = checker::ModifierValueKind::MemoryConsistency,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = selected.semantics.value,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.semantics.locs,
         },
         ModifierValueView{
             .kind_id = "scope",
             .value_kind = checker::ModifierValueKind::MemoryScope,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = selected.scope.value,
             .is_present = true,
             .locations = selected.scope.locs,
         },
         ModifierValueView{
             .kind_id = "vector",
             .value_kind = checker::ModifierValueKind::VectorArity,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = selected.vector.value,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.vector.locs,
         },
         ModifierValueView{
             .kind_id = "type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = selected.type.value,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.type.locs,
         }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Ld::get_checker_descriptor(), "ExplicitVector", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Ld::get_checker_descriptor().variants[3].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const std::array<OperandView, 2> operands = {
        {[&]() -> OperandView {
           OperandView view{
               .field_id = "dst",
               .actual_shape = check_end::OperandShape::Vector,
               .vector_arity =
                   static_cast<uint8_t>(selected.dst.value.elements.size()),
               .locations = selected.dst.locs,
           };
           size_t index = 0;
           for (const auto &element : selected.dst.value.elements) {
             if (index >= view.vector_element_types.size())
               break;
             if (element) {
               view.vector_element_types[index] =
                   element->declared_type.value_or(ScalarType::Invalid);
             } else {
               ++view.vector_sink_count;
             }
             ++index;
           }
           return view;
         }(),
         [&]() -> OperandView {
           const auto *symbol =
               std::get_if<ResolvedSymbolRef>(&selected.address.value.base);
           std::optional<MemoryStateSpace> effective_state_space;
           ParameterDirection parameter_direction = ParameterDirection::None;
           if (symbol != nullptr && symbol->address_state_space) {
             // Preserve the declaration-derived effective space. In
             // particular, device parameters may produce local rather
             // than declaration-space addresses in other instructions.
             switch (*symbol->address_state_space) {
             case syntax_ast::AstStateSpace::Global:
               effective_state_space = MemoryStateSpace::Global;
               break;
             case syntax_ast::AstStateSpace::Shared:
               effective_state_space = MemoryStateSpace::Shared;
               break;
             case syntax_ast::AstStateSpace::Local:
               effective_state_space = MemoryStateSpace::Local;
               break;
             case syntax_ast::AstStateSpace::Parameter:
               effective_state_space = MemoryStateSpace::Parameter;
               break;
             case syntax_ast::AstStateSpace::Constant:
               effective_state_space = MemoryStateSpace::Constant;
               break;
             case syntax_ast::AstStateSpace::Register:
               break;
             }
           }
           if (symbol != nullptr && symbol->declaration_kind) {
             if (*symbol->declaration_kind ==
                 binding::SymbolKind::InputParameter) {
               parameter_direction = ParameterDirection::Input;
             } else if (*symbol->declaration_kind ==
                        binding::SymbolKind::ReturnParameter) {
               parameter_direction = ParameterDirection::Return;
             } else if (*symbol->declaration_kind ==
                        binding::SymbolKind::CallParameter) {
               parameter_direction = ParameterDirection::CallArgument;
             }
           }
           std::optional<uint64_t> address_alignment;
           const auto low_bit = [](uint64_t value) {
             return value == 0 ? uint64_t{0} : value & (~value + 1);
           };
           if (symbol != nullptr) {
             address_alignment = symbol->address_alignment;
           } else if (const auto *immediate = std::get_if<ResolvedImmediate>(
                          &selected.address.value.base)) {
             address_alignment = low_bit(immediate->bits);
           }
           if (address_alignment && selected.address.value.offset) {
             const uint64_t offset_alignment =
                 low_bit(selected.address.value.offset->value.bits);
             if (offset_alignment != 0 &&
                 (*address_alignment == 0 ||
                  offset_alignment < *address_alignment))
               address_alignment = offset_alignment;
           }
           // Register, immediate, and unresolved standalone address
           // bases remain unknown; spelling is not semantic evidence.
           return OperandView{
               .field_id = "address",
               .actual_shape = check_end::OperandShape::Address,
               .immediate_type = std::nullopt,
               .register_type = std::nullopt,
               .address_state_space = effective_state_space,
               .address_alignment = address_alignment,
               .enclosing_function_kind =
                   selected.address.value.enclosing_function_kind,
               .parameter_direction = parameter_direction,
               .parameter_qualifier =
                   selected.address.value.parameter_qualifier,
               .locations = selected.address.locs,
           };
         }()}};
    const auto &layouts =
        Ld::get_resolved_descriptor().variants[3].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "ExplicitVector", selected.operand_layout.value, layouts.size(),
        context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Ld::get_checker_descriptor().variants[3],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto operand_check = check_operands(
          layouts[selected.operand_layout.value].bindings, fields, operands,
          Ld::get_checker_descriptor().variants[3].operand_type_compatibilities,
          context);
      if (!operand_check) {
        diagnostics.insert(diagnostics.end(), operand_check.error().begin(),
                           operand_check.error().end());
      }
      const auto consistency_check = check_memory_consistency(
          Ld::get_checker_descriptor().variants[3].memory_consistency, fields,
          operands, context);
      if (!consistency_check) {
        diagnostics.insert(diagnostics.end(), consistency_check.error().begin(),
                           consistency_check.error().end());
      }
      const auto memory_vector_check = check_memory_vector(
          Ld::get_checker_descriptor().variants[3].memory_vector, fields,
          operands, context);
      if (!memory_vector_check) {
        diagnostics.insert(diagnostics.end(),
                           memory_vector_check.error().begin(),
                           memory_vector_check.error().end());
      }
      const auto alignment_check = check_address_alignment(
          Ld::get_checker_descriptor().variants[3].address_alignment, fields,
          operands, context);
      if (!alignment_check) {
        diagnostics.insert(diagnostics.end(), alignment_check.error().begin(),
                           alignment_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(detail::VariantCheckFunction<decltype(check_explicit_vector),
                                             Ld::ExplicitVector>);

  return std::visit(
      detail::Overloaded{check_generic_scalar, check_explicit_scalar,
                         check_generic_vector, check_explicit_vector},
      instruction.variant);
}

template <>
CheckResult check<St>(const St &instruction, const Context &context) {
  const auto check_generic_scalar =
      [&](const St::GenericScalar &selected) -> CheckResult {
    const std::array<FieldView, 5> fields = {
        {FieldView{
             .field_id = "semantics",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = selected.semantics.value,
             .memory_scope = std::nullopt,
             .locations = selected.semantics.locs,
         },
         FieldView{
             .field_id = "scope",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = selected.scope.value,
             .locations = selected.scope.locs,
         },
         FieldView{
             .field_id = "mmio",
             .bool_value = selected.mmio.value,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.mmio.locs,
         },
         FieldView{
             .field_id = "cache",
             .bool_value = std::nullopt,
             .cache_operator = selected.cache.value,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.cache.locs,
         },
         FieldView{
             .field_id = "type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = selected.type.value,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.type.locs,
         }}};
    const std::array<ModifierValueView, 5> modifier_values = {
        {ModifierValueView{
             .kind_id = "semantics",
             .value_kind = checker::ModifierValueKind::MemoryConsistency,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = selected.semantics.value,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.semantics.locs,
         },
         ModifierValueView{
             .kind_id = "scope",
             .value_kind = checker::ModifierValueKind::MemoryScope,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = selected.scope.value,
             .is_present = true,
             .locations = selected.scope.locs,
         },
         ModifierValueView{
             .kind_id = "mmio",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = selected.mmio.value,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.mmio.locs,
         },
         ModifierValueView{
             .kind_id = "cache",
             .value_kind = checker::ModifierValueKind::CacheOperator,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = selected.cache.value,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = selected.cache.value != CacheOperator::Unspecified,
             .locations = selected.cache.locs,
         },
         ModifierValueView{
             .kind_id = "type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = selected.type.value,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.type.locs,
         }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(St::get_checker_descriptor(), "GenericScalar", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        St::get_checker_descriptor().variants[0].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const std::array<OperandView, 2> operands = {
        {[&]() -> OperandView {
           const auto *symbol =
               std::get_if<ResolvedSymbolRef>(&selected.address.value.base);
           std::optional<MemoryStateSpace> effective_state_space;
           ParameterDirection parameter_direction = ParameterDirection::None;
           if (symbol != nullptr && symbol->address_state_space) {
             // Preserve the declaration-derived effective space. In
             // particular, device parameters may produce local rather
             // than declaration-space addresses in other instructions.
             switch (*symbol->address_state_space) {
             case syntax_ast::AstStateSpace::Global:
               effective_state_space = MemoryStateSpace::Global;
               break;
             case syntax_ast::AstStateSpace::Shared:
               effective_state_space = MemoryStateSpace::Shared;
               break;
             case syntax_ast::AstStateSpace::Local:
               effective_state_space = MemoryStateSpace::Local;
               break;
             case syntax_ast::AstStateSpace::Parameter:
               effective_state_space = MemoryStateSpace::Parameter;
               break;
             case syntax_ast::AstStateSpace::Constant:
               effective_state_space = MemoryStateSpace::Constant;
               break;
             case syntax_ast::AstStateSpace::Register:
               break;
             }
           }
           if (symbol != nullptr && symbol->declaration_kind) {
             if (*symbol->declaration_kind ==
                 binding::SymbolKind::InputParameter) {
               parameter_direction = ParameterDirection::Input;
             } else if (*symbol->declaration_kind ==
                        binding::SymbolKind::ReturnParameter) {
               parameter_direction = ParameterDirection::Return;
             } else if (*symbol->declaration_kind ==
                        binding::SymbolKind::CallParameter) {
               parameter_direction = ParameterDirection::CallArgument;
             }
           }
           std::optional<uint64_t> address_alignment;
           const auto low_bit = [](uint64_t value) {
             return value == 0 ? uint64_t{0} : value & (~value + 1);
           };
           if (symbol != nullptr) {
             address_alignment = symbol->address_alignment;
           } else if (const auto *immediate = std::get_if<ResolvedImmediate>(
                          &selected.address.value.base)) {
             address_alignment = low_bit(immediate->bits);
           }
           if (address_alignment && selected.address.value.offset) {
             const uint64_t offset_alignment =
                 low_bit(selected.address.value.offset->value.bits);
             if (offset_alignment != 0 &&
                 (*address_alignment == 0 ||
                  offset_alignment < *address_alignment))
               address_alignment = offset_alignment;
           }
           // Register, immediate, and unresolved standalone address
           // bases remain unknown; spelling is not semantic evidence.
           return OperandView{
               .field_id = "address",
               .actual_shape = check_end::OperandShape::Address,
               .immediate_type = std::nullopt,
               .register_type = std::nullopt,
               .address_state_space = effective_state_space,
               .address_alignment = address_alignment,
               .enclosing_function_kind =
                   selected.address.value.enclosing_function_kind,
               .parameter_direction = parameter_direction,
               .parameter_qualifier =
                   selected.address.value.parameter_qualifier,
               .locations = selected.address.locs,
           };
         }(),
         OperandView{
             .field_id = "src",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.src.value.declared_type,
             .locations = selected.src.locs,
         }}};
    const auto &layouts =
        St::get_resolved_descriptor().variants[0].operand_layouts;
    const auto layout_check =
        check_operand_layout_tag("GenericScalar", selected.operand_layout.value,
                                 layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          St::get_checker_descriptor().variants[0],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto operand_check = check_operands(
          layouts[selected.operand_layout.value].bindings, fields, operands,
          St::get_checker_descriptor().variants[0].operand_type_compatibilities,
          context);
      if (!operand_check) {
        diagnostics.insert(diagnostics.end(), operand_check.error().begin(),
                           operand_check.error().end());
      }
      const auto consistency_check = check_memory_consistency(
          St::get_checker_descriptor().variants[0].memory_consistency, fields,
          operands, context);
      if (!consistency_check) {
        diagnostics.insert(diagnostics.end(), consistency_check.error().begin(),
                           consistency_check.error().end());
      }
      const auto alignment_check = check_address_alignment(
          St::get_checker_descriptor().variants[0].address_alignment, fields,
          operands, context);
      if (!alignment_check) {
        diagnostics.insert(diagnostics.end(), alignment_check.error().begin(),
                           alignment_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(detail::VariantCheckFunction<decltype(check_generic_scalar),
                                             St::GenericScalar>);

  const auto check_explicit_scalar =
      [&](const St::ExplicitScalar &selected) -> CheckResult {
    const std::array<FieldView, 6> fields = {
        {FieldView{
             .field_id = "state_space",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = selected.state_space.value,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.state_space.locs,
         },
         FieldView{
             .field_id = "cache",
             .bool_value = std::nullopt,
             .cache_operator = selected.cache.value,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.cache.locs,
         },
         FieldView{
             .field_id = "semantics",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = selected.semantics.value,
             .memory_scope = std::nullopt,
             .locations = selected.semantics.locs,
         },
         FieldView{
             .field_id = "scope",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = selected.scope.value,
             .locations = selected.scope.locs,
         },
         FieldView{
             .field_id = "mmio",
             .bool_value = selected.mmio.value,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.mmio.locs,
         },
         FieldView{
             .field_id = "type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = selected.type.value,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.type.locs,
         }}};
    const std::array<ModifierValueView, 6> modifier_values = {
        {ModifierValueView{
             .kind_id = "state_space",
             .value_kind = checker::ModifierValueKind::MemoryStateSpace,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = selected.state_space.value,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.state_space.locs,
         },
         ModifierValueView{
             .kind_id = "cache",
             .value_kind = checker::ModifierValueKind::CacheOperator,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = selected.cache.value,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = selected.cache.value != CacheOperator::Unspecified,
             .locations = selected.cache.locs,
         },
         ModifierValueView{
             .kind_id = "semantics",
             .value_kind = checker::ModifierValueKind::MemoryConsistency,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = selected.semantics.value,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.semantics.locs,
         },
         ModifierValueView{
             .kind_id = "scope",
             .value_kind = checker::ModifierValueKind::MemoryScope,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = selected.scope.value,
             .is_present = true,
             .locations = selected.scope.locs,
         },
         ModifierValueView{
             .kind_id = "mmio",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = selected.mmio.value,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.mmio.locs,
         },
         ModifierValueView{
             .kind_id = "type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = selected.type.value,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.type.locs,
         }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(St::get_checker_descriptor(), "ExplicitScalar", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        St::get_checker_descriptor().variants[1].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const std::array<OperandView, 2> operands = {
        {[&]() -> OperandView {
           const auto *symbol =
               std::get_if<ResolvedSymbolRef>(&selected.address.value.base);
           std::optional<MemoryStateSpace> effective_state_space;
           ParameterDirection parameter_direction = ParameterDirection::None;
           if (symbol != nullptr && symbol->address_state_space) {
             // Preserve the declaration-derived effective space. In
             // particular, device parameters may produce local rather
             // than declaration-space addresses in other instructions.
             switch (*symbol->address_state_space) {
             case syntax_ast::AstStateSpace::Global:
               effective_state_space = MemoryStateSpace::Global;
               break;
             case syntax_ast::AstStateSpace::Shared:
               effective_state_space = MemoryStateSpace::Shared;
               break;
             case syntax_ast::AstStateSpace::Local:
               effective_state_space = MemoryStateSpace::Local;
               break;
             case syntax_ast::AstStateSpace::Parameter:
               effective_state_space = MemoryStateSpace::Parameter;
               break;
             case syntax_ast::AstStateSpace::Constant:
               effective_state_space = MemoryStateSpace::Constant;
               break;
             case syntax_ast::AstStateSpace::Register:
               break;
             }
           }
           if (symbol != nullptr && symbol->declaration_kind) {
             if (*symbol->declaration_kind ==
                 binding::SymbolKind::InputParameter) {
               parameter_direction = ParameterDirection::Input;
             } else if (*symbol->declaration_kind ==
                        binding::SymbolKind::ReturnParameter) {
               parameter_direction = ParameterDirection::Return;
             } else if (*symbol->declaration_kind ==
                        binding::SymbolKind::CallParameter) {
               parameter_direction = ParameterDirection::CallArgument;
             }
           }
           std::optional<uint64_t> address_alignment;
           const auto low_bit = [](uint64_t value) {
             return value == 0 ? uint64_t{0} : value & (~value + 1);
           };
           if (symbol != nullptr) {
             address_alignment = symbol->address_alignment;
           } else if (const auto *immediate = std::get_if<ResolvedImmediate>(
                          &selected.address.value.base)) {
             address_alignment = low_bit(immediate->bits);
           }
           if (address_alignment && selected.address.value.offset) {
             const uint64_t offset_alignment =
                 low_bit(selected.address.value.offset->value.bits);
             if (offset_alignment != 0 &&
                 (*address_alignment == 0 ||
                  offset_alignment < *address_alignment))
               address_alignment = offset_alignment;
           }
           // Register, immediate, and unresolved standalone address
           // bases remain unknown; spelling is not semantic evidence.
           return OperandView{
               .field_id = "address",
               .actual_shape = check_end::OperandShape::Address,
               .immediate_type = std::nullopt,
               .register_type = std::nullopt,
               .address_state_space = effective_state_space,
               .address_alignment = address_alignment,
               .enclosing_function_kind =
                   selected.address.value.enclosing_function_kind,
               .parameter_direction = parameter_direction,
               .parameter_qualifier =
                   selected.address.value.parameter_qualifier,
               .locations = selected.address.locs,
           };
         }(),
         OperandView{
             .field_id = "src",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.src.value.declared_type,
             .locations = selected.src.locs,
         }}};
    const auto &layouts =
        St::get_resolved_descriptor().variants[1].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "ExplicitScalar", selected.operand_layout.value, layouts.size(),
        context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          St::get_checker_descriptor().variants[1],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto operand_check = check_operands(
          layouts[selected.operand_layout.value].bindings, fields, operands,
          St::get_checker_descriptor().variants[1].operand_type_compatibilities,
          context);
      if (!operand_check) {
        diagnostics.insert(diagnostics.end(), operand_check.error().begin(),
                           operand_check.error().end());
      }
      const auto consistency_check = check_memory_consistency(
          St::get_checker_descriptor().variants[1].memory_consistency, fields,
          operands, context);
      if (!consistency_check) {
        diagnostics.insert(diagnostics.end(), consistency_check.error().begin(),
                           consistency_check.error().end());
      }
      const auto alignment_check = check_address_alignment(
          St::get_checker_descriptor().variants[1].address_alignment, fields,
          operands, context);
      if (!alignment_check) {
        diagnostics.insert(diagnostics.end(), alignment_check.error().begin(),
                           alignment_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(detail::VariantCheckFunction<decltype(check_explicit_scalar),
                                             St::ExplicitScalar>);

  const auto check_generic_vector =
      [&](const St::GenericVector &selected) -> CheckResult {
    const std::array<FieldView, 5> fields = {
        {FieldView{
             .field_id = "semantics",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = selected.semantics.value,
             .memory_scope = std::nullopt,
             .locations = selected.semantics.locs,
         },
         FieldView{
             .field_id = "scope",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = selected.scope.value,
             .locations = selected.scope.locs,
         },
         FieldView{
             .field_id = "cache",
             .bool_value = std::nullopt,
             .cache_operator = selected.cache.value,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.cache.locs,
         },
         FieldView{
             .field_id = "vector",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = selected.vector.value,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.vector.locs,
         },
         FieldView{
             .field_id = "type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = selected.type.value,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.type.locs,
         }}};
    const std::array<ModifierValueView, 5> modifier_values = {
        {ModifierValueView{
             .kind_id = "semantics",
             .value_kind = checker::ModifierValueKind::MemoryConsistency,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = selected.semantics.value,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.semantics.locs,
         },
         ModifierValueView{
             .kind_id = "scope",
             .value_kind = checker::ModifierValueKind::MemoryScope,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = selected.scope.value,
             .is_present = true,
             .locations = selected.scope.locs,
         },
         ModifierValueView{
             .kind_id = "cache",
             .value_kind = checker::ModifierValueKind::CacheOperator,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = selected.cache.value,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = selected.cache.value != CacheOperator::Unspecified,
             .locations = selected.cache.locs,
         },
         ModifierValueView{
             .kind_id = "vector",
             .value_kind = checker::ModifierValueKind::VectorArity,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = selected.vector.value,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.vector.locs,
         },
         ModifierValueView{
             .kind_id = "type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = selected.type.value,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.type.locs,
         }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(St::get_checker_descriptor(), "GenericVector", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        St::get_checker_descriptor().variants[2].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const std::array<OperandView, 2> operands = {
        {[&]() -> OperandView {
           const auto *symbol =
               std::get_if<ResolvedSymbolRef>(&selected.address.value.base);
           std::optional<MemoryStateSpace> effective_state_space;
           ParameterDirection parameter_direction = ParameterDirection::None;
           if (symbol != nullptr && symbol->address_state_space) {
             // Preserve the declaration-derived effective space. In
             // particular, device parameters may produce local rather
             // than declaration-space addresses in other instructions.
             switch (*symbol->address_state_space) {
             case syntax_ast::AstStateSpace::Global:
               effective_state_space = MemoryStateSpace::Global;
               break;
             case syntax_ast::AstStateSpace::Shared:
               effective_state_space = MemoryStateSpace::Shared;
               break;
             case syntax_ast::AstStateSpace::Local:
               effective_state_space = MemoryStateSpace::Local;
               break;
             case syntax_ast::AstStateSpace::Parameter:
               effective_state_space = MemoryStateSpace::Parameter;
               break;
             case syntax_ast::AstStateSpace::Constant:
               effective_state_space = MemoryStateSpace::Constant;
               break;
             case syntax_ast::AstStateSpace::Register:
               break;
             }
           }
           if (symbol != nullptr && symbol->declaration_kind) {
             if (*symbol->declaration_kind ==
                 binding::SymbolKind::InputParameter) {
               parameter_direction = ParameterDirection::Input;
             } else if (*symbol->declaration_kind ==
                        binding::SymbolKind::ReturnParameter) {
               parameter_direction = ParameterDirection::Return;
             } else if (*symbol->declaration_kind ==
                        binding::SymbolKind::CallParameter) {
               parameter_direction = ParameterDirection::CallArgument;
             }
           }
           std::optional<uint64_t> address_alignment;
           const auto low_bit = [](uint64_t value) {
             return value == 0 ? uint64_t{0} : value & (~value + 1);
           };
           if (symbol != nullptr) {
             address_alignment = symbol->address_alignment;
           } else if (const auto *immediate = std::get_if<ResolvedImmediate>(
                          &selected.address.value.base)) {
             address_alignment = low_bit(immediate->bits);
           }
           if (address_alignment && selected.address.value.offset) {
             const uint64_t offset_alignment =
                 low_bit(selected.address.value.offset->value.bits);
             if (offset_alignment != 0 &&
                 (*address_alignment == 0 ||
                  offset_alignment < *address_alignment))
               address_alignment = offset_alignment;
           }
           // Register, immediate, and unresolved standalone address
           // bases remain unknown; spelling is not semantic evidence.
           return OperandView{
               .field_id = "address",
               .actual_shape = check_end::OperandShape::Address,
               .immediate_type = std::nullopt,
               .register_type = std::nullopt,
               .address_state_space = effective_state_space,
               .address_alignment = address_alignment,
               .enclosing_function_kind =
                   selected.address.value.enclosing_function_kind,
               .parameter_direction = parameter_direction,
               .parameter_qualifier =
                   selected.address.value.parameter_qualifier,
               .locations = selected.address.locs,
           };
         }(),
         [&]() -> OperandView {
           OperandView view{
               .field_id = "src",
               .actual_shape = check_end::OperandShape::Vector,
               .vector_arity =
                   static_cast<uint8_t>(selected.src.value.elements.size()),
               .locations = selected.src.locs,
           };
           size_t index = 0;
           for (const auto &element : selected.src.value.elements) {
             if (index >= view.vector_element_types.size())
               break;
             if (element) {
               view.vector_element_types[index] =
                   element->declared_type.value_or(ScalarType::Invalid);
             } else {
               ++view.vector_sink_count;
             }
             ++index;
           }
           return view;
         }()}};
    const auto &layouts =
        St::get_resolved_descriptor().variants[2].operand_layouts;
    const auto layout_check =
        check_operand_layout_tag("GenericVector", selected.operand_layout.value,
                                 layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          St::get_checker_descriptor().variants[2],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto operand_check = check_operands(
          layouts[selected.operand_layout.value].bindings, fields, operands,
          St::get_checker_descriptor().variants[2].operand_type_compatibilities,
          context);
      if (!operand_check) {
        diagnostics.insert(diagnostics.end(), operand_check.error().begin(),
                           operand_check.error().end());
      }
      const auto consistency_check = check_memory_consistency(
          St::get_checker_descriptor().variants[2].memory_consistency, fields,
          operands, context);
      if (!consistency_check) {
        diagnostics.insert(diagnostics.end(), consistency_check.error().begin(),
                           consistency_check.error().end());
      }
      const auto memory_vector_check = check_memory_vector(
          St::get_checker_descriptor().variants[2].memory_vector, fields,
          operands, context);
      if (!memory_vector_check) {
        diagnostics.insert(diagnostics.end(),
                           memory_vector_check.error().begin(),
                           memory_vector_check.error().end());
      }
      const auto alignment_check = check_address_alignment(
          St::get_checker_descriptor().variants[2].address_alignment, fields,
          operands, context);
      if (!alignment_check) {
        diagnostics.insert(diagnostics.end(), alignment_check.error().begin(),
                           alignment_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(detail::VariantCheckFunction<decltype(check_generic_vector),
                                             St::GenericVector>);

  const auto check_explicit_vector =
      [&](const St::ExplicitVector &selected) -> CheckResult {
    const std::array<FieldView, 6> fields = {
        {FieldView{
             .field_id = "state_space",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = selected.state_space.value,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.state_space.locs,
         },
         FieldView{
             .field_id = "cache",
             .bool_value = std::nullopt,
             .cache_operator = selected.cache.value,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.cache.locs,
         },
         FieldView{
             .field_id = "semantics",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = selected.semantics.value,
             .memory_scope = std::nullopt,
             .locations = selected.semantics.locs,
         },
         FieldView{
             .field_id = "scope",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = selected.scope.value,
             .locations = selected.scope.locs,
         },
         FieldView{
             .field_id = "vector",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = selected.vector.value,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.vector.locs,
         },
         FieldView{
             .field_id = "type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = selected.type.value,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.type.locs,
         }}};
    const std::array<ModifierValueView, 6> modifier_values = {
        {ModifierValueView{
             .kind_id = "state_space",
             .value_kind = checker::ModifierValueKind::MemoryStateSpace,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = selected.state_space.value,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.state_space.locs,
         },
         ModifierValueView{
             .kind_id = "cache",
             .value_kind = checker::ModifierValueKind::CacheOperator,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = selected.cache.value,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = selected.cache.value != CacheOperator::Unspecified,
             .locations = selected.cache.locs,
         },
         ModifierValueView{
             .kind_id = "semantics",
             .value_kind = checker::ModifierValueKind::MemoryConsistency,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = selected.semantics.value,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.semantics.locs,
         },
         ModifierValueView{
             .kind_id = "scope",
             .value_kind = checker::ModifierValueKind::MemoryScope,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = selected.scope.value,
             .is_present = true,
             .locations = selected.scope.locs,
         },
         ModifierValueView{
             .kind_id = "vector",
             .value_kind = checker::ModifierValueKind::VectorArity,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = selected.vector.value,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.vector.locs,
         },
         ModifierValueView{
             .kind_id = "type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = selected.type.value,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.type.locs,
         }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(St::get_checker_descriptor(), "ExplicitVector", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        St::get_checker_descriptor().variants[3].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const std::array<OperandView, 2> operands = {
        {[&]() -> OperandView {
           const auto *symbol =
               std::get_if<ResolvedSymbolRef>(&selected.address.value.base);
           std::optional<MemoryStateSpace> effective_state_space;
           ParameterDirection parameter_direction = ParameterDirection::None;
           if (symbol != nullptr && symbol->address_state_space) {
             // Preserve the declaration-derived effective space. In
             // particular, device parameters may produce local rather
             // than declaration-space addresses in other instructions.
             switch (*symbol->address_state_space) {
             case syntax_ast::AstStateSpace::Global:
               effective_state_space = MemoryStateSpace::Global;
               break;
             case syntax_ast::AstStateSpace::Shared:
               effective_state_space = MemoryStateSpace::Shared;
               break;
             case syntax_ast::AstStateSpace::Local:
               effective_state_space = MemoryStateSpace::Local;
               break;
             case syntax_ast::AstStateSpace::Parameter:
               effective_state_space = MemoryStateSpace::Parameter;
               break;
             case syntax_ast::AstStateSpace::Constant:
               effective_state_space = MemoryStateSpace::Constant;
               break;
             case syntax_ast::AstStateSpace::Register:
               break;
             }
           }
           if (symbol != nullptr && symbol->declaration_kind) {
             if (*symbol->declaration_kind ==
                 binding::SymbolKind::InputParameter) {
               parameter_direction = ParameterDirection::Input;
             } else if (*symbol->declaration_kind ==
                        binding::SymbolKind::ReturnParameter) {
               parameter_direction = ParameterDirection::Return;
             } else if (*symbol->declaration_kind ==
                        binding::SymbolKind::CallParameter) {
               parameter_direction = ParameterDirection::CallArgument;
             }
           }
           std::optional<uint64_t> address_alignment;
           const auto low_bit = [](uint64_t value) {
             return value == 0 ? uint64_t{0} : value & (~value + 1);
           };
           if (symbol != nullptr) {
             address_alignment = symbol->address_alignment;
           } else if (const auto *immediate = std::get_if<ResolvedImmediate>(
                          &selected.address.value.base)) {
             address_alignment = low_bit(immediate->bits);
           }
           if (address_alignment && selected.address.value.offset) {
             const uint64_t offset_alignment =
                 low_bit(selected.address.value.offset->value.bits);
             if (offset_alignment != 0 &&
                 (*address_alignment == 0 ||
                  offset_alignment < *address_alignment))
               address_alignment = offset_alignment;
           }
           // Register, immediate, and unresolved standalone address
           // bases remain unknown; spelling is not semantic evidence.
           return OperandView{
               .field_id = "address",
               .actual_shape = check_end::OperandShape::Address,
               .immediate_type = std::nullopt,
               .register_type = std::nullopt,
               .address_state_space = effective_state_space,
               .address_alignment = address_alignment,
               .enclosing_function_kind =
                   selected.address.value.enclosing_function_kind,
               .parameter_direction = parameter_direction,
               .parameter_qualifier =
                   selected.address.value.parameter_qualifier,
               .locations = selected.address.locs,
           };
         }(),
         [&]() -> OperandView {
           OperandView view{
               .field_id = "src",
               .actual_shape = check_end::OperandShape::Vector,
               .vector_arity =
                   static_cast<uint8_t>(selected.src.value.elements.size()),
               .locations = selected.src.locs,
           };
           size_t index = 0;
           for (const auto &element : selected.src.value.elements) {
             if (index >= view.vector_element_types.size())
               break;
             if (element) {
               view.vector_element_types[index] =
                   element->declared_type.value_or(ScalarType::Invalid);
             } else {
               ++view.vector_sink_count;
             }
             ++index;
           }
           return view;
         }()}};
    const auto &layouts =
        St::get_resolved_descriptor().variants[3].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "ExplicitVector", selected.operand_layout.value, layouts.size(),
        context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          St::get_checker_descriptor().variants[3],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto operand_check = check_operands(
          layouts[selected.operand_layout.value].bindings, fields, operands,
          St::get_checker_descriptor().variants[3].operand_type_compatibilities,
          context);
      if (!operand_check) {
        diagnostics.insert(diagnostics.end(), operand_check.error().begin(),
                           operand_check.error().end());
      }
      const auto consistency_check = check_memory_consistency(
          St::get_checker_descriptor().variants[3].memory_consistency, fields,
          operands, context);
      if (!consistency_check) {
        diagnostics.insert(diagnostics.end(), consistency_check.error().begin(),
                           consistency_check.error().end());
      }
      const auto memory_vector_check = check_memory_vector(
          St::get_checker_descriptor().variants[3].memory_vector, fields,
          operands, context);
      if (!memory_vector_check) {
        diagnostics.insert(diagnostics.end(),
                           memory_vector_check.error().begin(),
                           memory_vector_check.error().end());
      }
      const auto alignment_check = check_address_alignment(
          St::get_checker_descriptor().variants[3].address_alignment, fields,
          operands, context);
      if (!alignment_check) {
        diagnostics.insert(diagnostics.end(), alignment_check.error().begin(),
                           alignment_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(detail::VariantCheckFunction<decltype(check_explicit_vector),
                                             St::ExplicitVector>);

  return std::visit(
      detail::Overloaded{check_generic_scalar, check_explicit_scalar,
                         check_generic_vector, check_explicit_vector},
      instruction.variant);
}

} // namespace checker

} // namespace ptx_frontend::resolved_ir
