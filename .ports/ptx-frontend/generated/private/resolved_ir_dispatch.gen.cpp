// Generated by python/scripts/gen_all.py. Do not edit.
// Generated at: omitted (set SOURCE_DATE_EPOCH for a reproducible timestamp)

#include "resolved_ir.gen.hpp"

#include <utility>

namespace ptx_frontend::resolved_ir {

namespace {

std::expected<ResolvedInstruction, ResolveDiagnostic>
resolve_instruction(const syntax_ast::AstInstruction &ast,
                    const ResolveContext *context) {
  if (ast.opcode.syntax.text == "add") {
    auto resolved = resolve<Add>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Add>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "sub") {
    auto resolved = resolve<Sub>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Sub>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "mul") {
    auto resolved = resolve<Mul>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Mul>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "fma") {
    auto resolved = resolve<Fma>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Fma>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "mad") {
    auto resolved = resolve<Mad>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Mad>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "div") {
    auto resolved = resolve<Div>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Div>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "rem") {
    auto resolved = resolve<Rem>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Rem>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "min") {
    auto resolved = resolve<Min>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Min>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "max") {
    auto resolved = resolve<Max>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Max>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "abs") {
    auto resolved = resolve<Abs>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Abs>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "neg") {
    auto resolved = resolve<Neg>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Neg>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "popc") {
    auto resolved = resolve<Popc>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Popc>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "clz") {
    auto resolved = resolve<Clz>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Clz>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "bfind") {
    auto resolved = resolve<Bfind>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Bfind>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "bfe") {
    auto resolved = resolve<Bfe>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Bfe>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "bfi") {
    auto resolved = resolve<Bfi>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Bfi>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "brev") {
    auto resolved = resolve<Brev>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Brev>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "set") {
    auto resolved = resolve<Set>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Set>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "setp") {
    auto resolved = resolve<Setp>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Setp>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "selp") {
    auto resolved = resolve<Selp>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Selp>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "slct") {
    auto resolved = resolve<Slct>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Slct>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "call") {
    auto resolved = resolve<Call>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Call>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "bra") {
    auto resolved = resolve<Bra>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Bra>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "brx") {
    auto resolved = resolve<Brx>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Brx>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "ret") {
    auto resolved = resolve<Ret>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Ret>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "exit") {
    auto resolved = resolve<Exit>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Exit>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "isspacep") {
    auto resolved = resolve<Isspacep>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Isspacep>,
                               std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "cvta") {
    auto resolved = resolve<Cvta>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Cvta>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "cvt") {
    auto resolved = resolve<Cvt>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Cvt>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "mov") {
    auto resolved = resolve<Mov>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Mov>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "ld") {
    auto resolved = resolve<Ld>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Ld>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "ldu") {
    auto resolved = resolve<Ldu>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Ldu>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "shfl") {
    auto resolved = resolve<Shfl>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Shfl>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "prefetch") {
    auto resolved = resolve<Prefetch>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Prefetch>,
                               std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "prefetchu") {
    auto resolved = resolve<Prefetchu>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Prefetchu>,
                               std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "createpolicy") {
    auto resolved = resolve<Createpolicy>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Createpolicy>,
                               std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "applypriority") {
    auto resolved = resolve<Applypriority>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Applypriority>,
                               std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "discard") {
    auto resolved = resolve<Discard>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Discard>,
                               std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "cp") {
    auto resolved = resolve<Cp>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Cp>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "st") {
    auto resolved = resolve<St>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<St>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "prmt") {
    auto resolved = resolve<Prmt>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Prmt>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "and") {
    auto resolved = resolve<And>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<And>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "or") {
    auto resolved = resolve<Or>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Or>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "xor") {
    auto resolved = resolve<Xor>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Xor>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "not") {
    auto resolved = resolve<Not>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Not>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "lop3") {
    auto resolved = resolve<Lop3>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Lop3>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "shf") {
    auto resolved = resolve<Shf>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Shf>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "shl") {
    auto resolved = resolve<Shl>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Shl>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "shr") {
    auto resolved = resolve<Shr>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Shr>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "trap") {
    auto resolved = resolve<Trap>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Trap>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "setmaxnreg") {
    auto resolved = resolve<Setmaxnreg>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Setmaxnreg>,
                               std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "bar") {
    auto resolved = resolve<Bar>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Bar>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "membar") {
    auto resolved = resolve<Membar>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Membar>,
                               std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "fence") {
    auto resolved = resolve<Fence>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Fence>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "atom") {
    auto resolved = resolve<Atom>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Atom>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "red") {
    auto resolved = resolve<Red>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Red>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "activemask") {
    auto resolved = resolve<Activemask>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Activemask>,
                               std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "vote") {
    auto resolved = resolve<Vote>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Vote>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "mma") {
    auto resolved = resolve<Mma>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Mma>, std::move(*resolved)};
  }

  if (ast.opcode.syntax.text == "ldmatrix") {
    auto resolved = resolve<Ldmatrix>(ast, context);
    if (!resolved)
      return std::unexpected(resolved.error());
    return ResolvedInstruction{std::in_place_type<Ldmatrix>,
                               std::move(*resolved)};
  }

  return std::unexpected(ResolveDiagnostic{
      .range = ast.opcode.syntax.range,
      .message = "Unknown PTX opcode '" + ast.opcode.syntax.text + "'.",
  });
}

} // namespace

std::expected<ResolvedInstruction, ResolveDiagnostic>
resolveInstruction(const syntax_ast::AstInstruction &ast) {
  return resolve_instruction(ast, nullptr);
}

std::expected<ResolvedInstruction, ResolveDiagnostic>
resolveInstruction(const syntax_ast::AstInstruction &ast,
                   const ResolveContext &context) {
  return resolve_instruction(ast, &context);
}

} // namespace ptx_frontend::resolved_ir
