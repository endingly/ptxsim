// Generated by python/scripts/gen_all.py. Do not edit.
// Generated at: omitted (set SOURCE_DATE_EPOCH for a reproducible timestamp)

#include "resolved_ir.gen.hpp"

namespace ptx_frontend::resolved_ir {

template <>
std::expected<Bar, ResolveDiagnostic>
resolve<Bar>(const syntax_ast::AstInstruction &ast,
             const ResolveContext *context) {
  const auto selected_variant = selectVariant<Bar>(ast);
  if (!selected_variant)
    return std::unexpected(selected_variant.error());

  auto fields = resolve_fields(
      ast, Bar::get_syntax_descriptor(), Bar::get_resolved_descriptor(),
      magic_enum::enum_name(*selected_variant), context);
  if (!fields)
    return std::unexpected(fields.error());

  if (fields->variant_name == "Sync") {
    if (fields->operand_layout.value == 0) {
      return Bar{.execution_predicate = std::move(fields->execution_predicate),
                 .variant = Bar::Sync{
                     .operand_layout = fields->operand_layout,

                     .operands =
                         Bar::Sync::Operands{
                             Bar::Sync::ImmediateBarrierOperands{
                                 .barrier = resolved_operand<ResolvedImmediate>(
                                     *fields, "barrier"),
                             },
                         },
                 }};
    }
    if (fields->operand_layout.value == 1) {
      return Bar{.execution_predicate = std::move(fields->execution_predicate),
                 .variant = Bar::Sync{
                     .operand_layout = fields->operand_layout,

                     .operands =
                         Bar::Sync::Operands{
                             Bar::Sync::BarrierOperands{
                                 .barrier = resolved_operand<RegOrImm>(
                                     *fields, "barrier"),
                             },
                         },
                 }};
    }
    if (fields->operand_layout.value == 2) {
      return Bar{.execution_predicate = std::move(fields->execution_predicate),
                 .variant = Bar::Sync{
                     .operand_layout = fields->operand_layout,

                     .operands =
                         Bar::Sync::Operands{
                             Bar::Sync::BarrierAndThreadCountOperands{
                                 .barrier = resolved_operand<RegOrImm>(
                                     *fields, "barrier"),
                                 .thread_count = resolved_operand<RegOrImm>(
                                     *fields, "thread_count"),
                             },
                         },
                 }};
    }
    throw ResolveException("Unknown Bar::Sync operand layout.");
  }
  if (fields->variant_name == "CtaSync") {
    if (fields->operand_layout.value == 0) {
      return Bar{.execution_predicate = std::move(fields->execution_predicate),
                 .variant = Bar::CtaSync{
                     .operand_layout = fields->operand_layout,

                     .operands =
                         Bar::CtaSync::Operands{
                             Bar::CtaSync::BarrierOperands{
                                 .barrier = resolved_operand<RegOrImm>(
                                     *fields, "barrier"),
                             },
                         },
                 }};
    }
    if (fields->operand_layout.value == 1) {
      return Bar{.execution_predicate = std::move(fields->execution_predicate),
                 .variant = Bar::CtaSync{
                     .operand_layout = fields->operand_layout,

                     .operands =
                         Bar::CtaSync::Operands{
                             Bar::CtaSync::BarrierAndThreadCountOperands{
                                 .barrier = resolved_operand<RegOrImm>(
                                     *fields, "barrier"),
                                 .thread_count = resolved_operand<RegOrImm>(
                                     *fields, "thread_count"),
                             },
                         },
                 }};
    }
    throw ResolveException("Unknown Bar::CtaSync operand layout.");
  }
  if (fields->variant_name == "Arrive") {
    return Bar{
        .execution_predicate = std::move(fields->execution_predicate),
        .variant = Bar::Arrive{
            .operand_layout = fields->operand_layout,
            .barrier = resolved_operand<RegOrImm>(*fields, "barrier"),
            .thread_count = resolved_operand<RegOrImm>(*fields, "thread_count"),
        }};
  }
  if (fields->variant_name == "CtaArrive") {
    return Bar{
        .execution_predicate = std::move(fields->execution_predicate),
        .variant = Bar::CtaArrive{
            .operand_layout = fields->operand_layout,
            .barrier = resolved_operand<RegOrImm>(*fields, "barrier"),
            .thread_count = resolved_operand<RegOrImm>(*fields, "thread_count"),
        }};
  }
  if (fields->variant_name == "RedPopcU32") {
    if (fields->operand_layout.value == 0) {
      return Bar{
          .execution_predicate = std::move(fields->execution_predicate),
          .variant = Bar::RedPopcU32{
              .operand_layout = fields->operand_layout,

              .operands =
                  Bar::RedPopcU32::Operands{
                      Bar::RedPopcU32::WithoutThreadCountOperands{
                          .dst = resolved_operand<ResolvedRegisterRef>(*fields,
                                                                       "dst"),
                          .barrier =
                              resolved_operand<RegOrImm>(*fields, "barrier"),
                          .predicate = resolved_operand<ResolvedPredicate>(
                              *fields, "predicate"),
                      },
                  },
          }};
    }
    if (fields->operand_layout.value == 1) {
      return Bar{
          .execution_predicate = std::move(fields->execution_predicate),
          .variant = Bar::RedPopcU32{
              .operand_layout = fields->operand_layout,

              .operands =
                  Bar::RedPopcU32::Operands{
                      Bar::RedPopcU32::WithThreadCountOperands{
                          .dst = resolved_operand<ResolvedRegisterRef>(*fields,
                                                                       "dst"),
                          .barrier =
                              resolved_operand<RegOrImm>(*fields, "barrier"),
                          .thread_count = resolved_operand<RegOrImm>(
                              *fields, "thread_count"),
                          .predicate = resolved_operand<ResolvedPredicate>(
                              *fields, "predicate"),
                      },
                  },
          }};
    }
    throw ResolveException("Unknown Bar::RedPopcU32 operand layout.");
  }
  if (fields->variant_name == "CtaRedPopcU32") {
    if (fields->operand_layout.value == 0) {
      return Bar{
          .execution_predicate = std::move(fields->execution_predicate),
          .variant = Bar::CtaRedPopcU32{
              .operand_layout = fields->operand_layout,

              .operands =
                  Bar::CtaRedPopcU32::Operands{
                      Bar::CtaRedPopcU32::WithoutThreadCountOperands{
                          .dst = resolved_operand<ResolvedRegisterRef>(*fields,
                                                                       "dst"),
                          .barrier =
                              resolved_operand<RegOrImm>(*fields, "barrier"),
                          .predicate = resolved_operand<ResolvedPredicate>(
                              *fields, "predicate"),
                      },
                  },
          }};
    }
    if (fields->operand_layout.value == 1) {
      return Bar{
          .execution_predicate = std::move(fields->execution_predicate),
          .variant = Bar::CtaRedPopcU32{
              .operand_layout = fields->operand_layout,

              .operands =
                  Bar::CtaRedPopcU32::Operands{
                      Bar::CtaRedPopcU32::WithThreadCountOperands{
                          .dst = resolved_operand<ResolvedRegisterRef>(*fields,
                                                                       "dst"),
                          .barrier =
                              resolved_operand<RegOrImm>(*fields, "barrier"),
                          .thread_count = resolved_operand<RegOrImm>(
                              *fields, "thread_count"),
                          .predicate = resolved_operand<ResolvedPredicate>(
                              *fields, "predicate"),
                      },
                  },
          }};
    }
    throw ResolveException("Unknown Bar::CtaRedPopcU32 operand layout.");
  }
  if (fields->variant_name == "RedAndPred") {
    if (fields->operand_layout.value == 0) {
      return Bar{
          .execution_predicate = std::move(fields->execution_predicate),
          .variant = Bar::RedAndPred{
              .operand_layout = fields->operand_layout,

              .operands =
                  Bar::RedAndPred::Operands{
                      Bar::RedAndPred::WithoutThreadCountOperands{
                          .dst = resolved_operand<ResolvedPredicate>(*fields,
                                                                     "dst"),
                          .barrier =
                              resolved_operand<RegOrImm>(*fields, "barrier"),
                          .predicate = resolved_operand<ResolvedPredicate>(
                              *fields, "predicate"),
                      },
                  },
          }};
    }
    if (fields->operand_layout.value == 1) {
      return Bar{
          .execution_predicate = std::move(fields->execution_predicate),
          .variant = Bar::RedAndPred{
              .operand_layout = fields->operand_layout,

              .operands =
                  Bar::RedAndPred::Operands{
                      Bar::RedAndPred::WithThreadCountOperands{
                          .dst = resolved_operand<ResolvedPredicate>(*fields,
                                                                     "dst"),
                          .barrier =
                              resolved_operand<RegOrImm>(*fields, "barrier"),
                          .thread_count = resolved_operand<RegOrImm>(
                              *fields, "thread_count"),
                          .predicate = resolved_operand<ResolvedPredicate>(
                              *fields, "predicate"),
                      },
                  },
          }};
    }
    throw ResolveException("Unknown Bar::RedAndPred operand layout.");
  }
  if (fields->variant_name == "CtaRedAndPred") {
    if (fields->operand_layout.value == 0) {
      return Bar{
          .execution_predicate = std::move(fields->execution_predicate),
          .variant = Bar::CtaRedAndPred{
              .operand_layout = fields->operand_layout,

              .operands =
                  Bar::CtaRedAndPred::Operands{
                      Bar::CtaRedAndPred::WithoutThreadCountOperands{
                          .dst = resolved_operand<ResolvedPredicate>(*fields,
                                                                     "dst"),
                          .barrier =
                              resolved_operand<RegOrImm>(*fields, "barrier"),
                          .predicate = resolved_operand<ResolvedPredicate>(
                              *fields, "predicate"),
                      },
                  },
          }};
    }
    if (fields->operand_layout.value == 1) {
      return Bar{
          .execution_predicate = std::move(fields->execution_predicate),
          .variant = Bar::CtaRedAndPred{
              .operand_layout = fields->operand_layout,

              .operands =
                  Bar::CtaRedAndPred::Operands{
                      Bar::CtaRedAndPred::WithThreadCountOperands{
                          .dst = resolved_operand<ResolvedPredicate>(*fields,
                                                                     "dst"),
                          .barrier =
                              resolved_operand<RegOrImm>(*fields, "barrier"),
                          .thread_count = resolved_operand<RegOrImm>(
                              *fields, "thread_count"),
                          .predicate = resolved_operand<ResolvedPredicate>(
                              *fields, "predicate"),
                      },
                  },
          }};
    }
    throw ResolveException("Unknown Bar::CtaRedAndPred operand layout.");
  }
  if (fields->variant_name == "RedOrPred") {
    if (fields->operand_layout.value == 0) {
      return Bar{
          .execution_predicate = std::move(fields->execution_predicate),
          .variant = Bar::RedOrPred{
              .operand_layout = fields->operand_layout,

              .operands =
                  Bar::RedOrPred::Operands{
                      Bar::RedOrPred::WithoutThreadCountOperands{
                          .dst = resolved_operand<ResolvedPredicate>(*fields,
                                                                     "dst"),
                          .barrier =
                              resolved_operand<RegOrImm>(*fields, "barrier"),
                          .predicate = resolved_operand<ResolvedPredicate>(
                              *fields, "predicate"),
                      },
                  },
          }};
    }
    if (fields->operand_layout.value == 1) {
      return Bar{
          .execution_predicate = std::move(fields->execution_predicate),
          .variant = Bar::RedOrPred{
              .operand_layout = fields->operand_layout,

              .operands =
                  Bar::RedOrPred::Operands{
                      Bar::RedOrPred::WithThreadCountOperands{
                          .dst = resolved_operand<ResolvedPredicate>(*fields,
                                                                     "dst"),
                          .barrier =
                              resolved_operand<RegOrImm>(*fields, "barrier"),
                          .thread_count = resolved_operand<RegOrImm>(
                              *fields, "thread_count"),
                          .predicate = resolved_operand<ResolvedPredicate>(
                              *fields, "predicate"),
                      },
                  },
          }};
    }
    throw ResolveException("Unknown Bar::RedOrPred operand layout.");
  }
  if (fields->variant_name == "CtaRedOrPred") {
    if (fields->operand_layout.value == 0) {
      return Bar{
          .execution_predicate = std::move(fields->execution_predicate),
          .variant = Bar::CtaRedOrPred{
              .operand_layout = fields->operand_layout,

              .operands =
                  Bar::CtaRedOrPred::Operands{
                      Bar::CtaRedOrPred::WithoutThreadCountOperands{
                          .dst = resolved_operand<ResolvedPredicate>(*fields,
                                                                     "dst"),
                          .barrier =
                              resolved_operand<RegOrImm>(*fields, "barrier"),
                          .predicate = resolved_operand<ResolvedPredicate>(
                              *fields, "predicate"),
                      },
                  },
          }};
    }
    if (fields->operand_layout.value == 1) {
      return Bar{
          .execution_predicate = std::move(fields->execution_predicate),
          .variant = Bar::CtaRedOrPred{
              .operand_layout = fields->operand_layout,

              .operands =
                  Bar::CtaRedOrPred::Operands{
                      Bar::CtaRedOrPred::WithThreadCountOperands{
                          .dst = resolved_operand<ResolvedPredicate>(*fields,
                                                                     "dst"),
                          .barrier =
                              resolved_operand<RegOrImm>(*fields, "barrier"),
                          .thread_count = resolved_operand<RegOrImm>(
                              *fields, "thread_count"),
                          .predicate = resolved_operand<ResolvedPredicate>(
                              *fields, "predicate"),
                      },
                  },
          }};
    }
    throw ResolveException("Unknown Bar::CtaRedOrPred operand layout.");
  }

  throw ResolveException("Unknown Bar variant in resolved field table.");
}

namespace checker {

template <>
CheckResult check<Bar>(const Bar &instruction, const Context &context) {
  const auto check_sync = [&](const Bar::Sync &selected) -> CheckResult {
    const std::array<FieldView, 1> fields = {{FieldView{
        .field_id = "sync",
        .bool_value = Bar::Sync::sync,
        .cache_operator = std::nullopt,
        .scalar_type = std::nullopt,
        .vector_arity = std::nullopt,
        .memory_state_space = std::nullopt,
        .memory_consistency = std::nullopt,
        .memory_scope = std::nullopt,
        .locations = std::span<const SourceRange>{},
    }}};
    const std::array<ModifierValueView, 1> modifier_values = {
        {ModifierValueView{
            .kind_id = "sync",
            .value_kind = checker::ModifierValueKind::Bool,
            .bool_value = Bar::Sync::sync,
            .scalar_type = ScalarType::Invalid,
            .rounding_mode = RoundingMode::Invalid,
            .cache_operator = CacheOperator::Unspecified,
            .vector_arity = VectorArity::Invalid,
            .memory_state_space = MemoryStateSpace::Invalid,
            .memory_consistency = MemoryConsistency::Omitted,
            .memory_scope = MemoryScope::None,
            .is_present = true,
            .locations = std::span<const SourceRange>{},
        }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Bar::get_checker_descriptor(), "Sync", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Bar::get_checker_descriptor().variants[0].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const auto check_bar_sync_immediate_barrier_operands =
        [&](const Bar::Sync::ImmediateBarrierOperands &payload) -> CheckResult {
      if (selected.operand_layout.value != 0) {
        return std::unexpected(CheckDiagnostics{CheckDiagnostic{
            .kind = CheckDiagnosticKind::OperandLayoutPayloadMismatch,
            .range = context.instruction_range,
            .message =
                "Resolved operand-layout tag and payload alternative disagree.",
        }});
      }
      const std::array<OperandView, 1> operands = {{OperandView{
          .field_id = "barrier",
          .actual_shape = check_end::OperandShape::Immediate,
          .immediate_type = payload.barrier.value.type,
          .register_type = std::nullopt,
          .locations = payload.barrier.locs,
      }}};
      return check_operands(Bar::get_resolved_descriptor()
                                .variants[0]
                                .operand_layouts[0]
                                .bindings,
                            fields, operands,
                            Bar::get_checker_descriptor()
                                .variants[0]
                                .operand_type_compatibilities,
                            context);
    };
    static_assert(detail::VariantCheckFunction<
                  decltype(check_bar_sync_immediate_barrier_operands),
                  Bar::Sync::ImmediateBarrierOperands>);

    const auto check_bar_sync_barrier_operands =
        [&](const Bar::Sync::BarrierOperands &payload) -> CheckResult {
      if (selected.operand_layout.value != 1) {
        return std::unexpected(CheckDiagnostics{CheckDiagnostic{
            .kind = CheckDiagnosticKind::OperandLayoutPayloadMismatch,
            .range = context.instruction_range,
            .message =
                "Resolved operand-layout tag and payload alternative disagree.",
        }});
      }
      const std::array<OperandView, 1> operands = {{[&]() -> OperandView {
        if (const auto *immediate =
                std::get_if<ResolvedImmediate>(&payload.barrier.value)) {
          return OperandView{
              .field_id = "barrier",
              .actual_shape = check_end::OperandShape::Immediate,
              .immediate_type = immediate->type,
              .register_type = std::nullopt,
              .locations = payload.barrier.locs,
          };
        }
        const auto &register_ref =
            std::get<ResolvedRegisterRef>(payload.barrier.value);
        return OperandView{
            .field_id = "barrier",
            .actual_shape = check_end::OperandShape::Register,
            .immediate_type = std::nullopt,
            .register_type = register_ref.declared_type,
            .locations = payload.barrier.locs,
        };
      }()}};
      return check_operands(Bar::get_resolved_descriptor()
                                .variants[0]
                                .operand_layouts[1]
                                .bindings,
                            fields, operands,
                            Bar::get_checker_descriptor()
                                .variants[0]
                                .operand_type_compatibilities,
                            context);
    };
    static_assert(
        detail::VariantCheckFunction<decltype(check_bar_sync_barrier_operands),
                                     Bar::Sync::BarrierOperands>);

    const auto check_bar_sync_barrier_and_thread_count_operands =
        [&](const Bar::Sync::BarrierAndThreadCountOperands &payload)
        -> CheckResult {
      if (selected.operand_layout.value != 2) {
        return std::unexpected(CheckDiagnostics{CheckDiagnostic{
            .kind = CheckDiagnosticKind::OperandLayoutPayloadMismatch,
            .range = context.instruction_range,
            .message =
                "Resolved operand-layout tag and payload alternative disagree.",
        }});
      }
      const std::array<OperandView, 2> operands = {
          {[&]() -> OperandView {
             if (const auto *immediate =
                     std::get_if<ResolvedImmediate>(&payload.barrier.value)) {
               return OperandView{
                   .field_id = "barrier",
                   .actual_shape = check_end::OperandShape::Immediate,
                   .immediate_type = immediate->type,
                   .register_type = std::nullopt,
                   .locations = payload.barrier.locs,
               };
             }
             const auto &register_ref =
                 std::get<ResolvedRegisterRef>(payload.barrier.value);
             return OperandView{
                 .field_id = "barrier",
                 .actual_shape = check_end::OperandShape::Register,
                 .immediate_type = std::nullopt,
                 .register_type = register_ref.declared_type,
                 .locations = payload.barrier.locs,
             };
           }(),
           [&]() -> OperandView {
             if (const auto *immediate = std::get_if<ResolvedImmediate>(
                     &payload.thread_count.value)) {
               return OperandView{
                   .field_id = "thread_count",
                   .actual_shape = check_end::OperandShape::Immediate,
                   .immediate_type = immediate->type,
                   .register_type = std::nullopt,
                   .locations = payload.thread_count.locs,
               };
             }
             const auto &register_ref =
                 std::get<ResolvedRegisterRef>(payload.thread_count.value);
             return OperandView{
                 .field_id = "thread_count",
                 .actual_shape = check_end::OperandShape::Register,
                 .immediate_type = std::nullopt,
                 .register_type = register_ref.declared_type,
                 .locations = payload.thread_count.locs,
             };
           }()}};
      return check_operands(Bar::get_resolved_descriptor()
                                .variants[0]
                                .operand_layouts[2]
                                .bindings,
                            fields, operands,
                            Bar::get_checker_descriptor()
                                .variants[0]
                                .operand_type_compatibilities,
                            context);
    };
    static_assert(detail::VariantCheckFunction<
                  decltype(check_bar_sync_barrier_and_thread_count_operands),
                  Bar::Sync::BarrierAndThreadCountOperands>);

    const auto &layouts =
        Bar::get_resolved_descriptor().variants[0].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "Sync", selected.operand_layout.value, layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Bar::get_checker_descriptor().variants[0],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto payload_check = std::visit(
          detail::Overloaded{check_bar_sync_immediate_barrier_operands,
                             check_bar_sync_barrier_operands,
                             check_bar_sync_barrier_and_thread_count_operands},
          selected.operands);
      if (!payload_check) {
        diagnostics.insert(diagnostics.end(), payload_check.error().begin(),
                           payload_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(detail::VariantCheckFunction<decltype(check_sync), Bar::Sync>);

  const auto check_cta_sync = [&](const Bar::CtaSync &selected) -> CheckResult {
    const std::array<FieldView, 2> fields = {
        {FieldView{
             .field_id = "cta",
             .bool_value = Bar::CtaSync::cta,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         },
         FieldView{
             .field_id = "sync",
             .bool_value = Bar::CtaSync::sync,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         }}};
    const std::array<ModifierValueView, 2> modifier_values = {
        {ModifierValueView{
             .kind_id = "cta",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = Bar::CtaSync::cta,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         },
         ModifierValueView{
             .kind_id = "sync",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = Bar::CtaSync::sync,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Bar::get_checker_descriptor(), "CtaSync", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Bar::get_checker_descriptor().variants[1].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const auto check_bar_cta_sync_barrier_operands =
        [&](const Bar::CtaSync::BarrierOperands &payload) -> CheckResult {
      if (selected.operand_layout.value != 0) {
        return std::unexpected(CheckDiagnostics{CheckDiagnostic{
            .kind = CheckDiagnosticKind::OperandLayoutPayloadMismatch,
            .range = context.instruction_range,
            .message =
                "Resolved operand-layout tag and payload alternative disagree.",
        }});
      }
      const std::array<OperandView, 1> operands = {{[&]() -> OperandView {
        if (const auto *immediate =
                std::get_if<ResolvedImmediate>(&payload.barrier.value)) {
          return OperandView{
              .field_id = "barrier",
              .actual_shape = check_end::OperandShape::Immediate,
              .immediate_type = immediate->type,
              .register_type = std::nullopt,
              .locations = payload.barrier.locs,
          };
        }
        const auto &register_ref =
            std::get<ResolvedRegisterRef>(payload.barrier.value);
        return OperandView{
            .field_id = "barrier",
            .actual_shape = check_end::OperandShape::Register,
            .immediate_type = std::nullopt,
            .register_type = register_ref.declared_type,
            .locations = payload.barrier.locs,
        };
      }()}};
      return check_operands(Bar::get_resolved_descriptor()
                                .variants[1]
                                .operand_layouts[0]
                                .bindings,
                            fields, operands,
                            Bar::get_checker_descriptor()
                                .variants[1]
                                .operand_type_compatibilities,
                            context);
    };
    static_assert(detail::VariantCheckFunction<
                  decltype(check_bar_cta_sync_barrier_operands),
                  Bar::CtaSync::BarrierOperands>);

    const auto check_bar_cta_sync_barrier_and_thread_count_operands =
        [&](const Bar::CtaSync::BarrierAndThreadCountOperands &payload)
        -> CheckResult {
      if (selected.operand_layout.value != 1) {
        return std::unexpected(CheckDiagnostics{CheckDiagnostic{
            .kind = CheckDiagnosticKind::OperandLayoutPayloadMismatch,
            .range = context.instruction_range,
            .message =
                "Resolved operand-layout tag and payload alternative disagree.",
        }});
      }
      const std::array<OperandView, 2> operands = {
          {[&]() -> OperandView {
             if (const auto *immediate =
                     std::get_if<ResolvedImmediate>(&payload.barrier.value)) {
               return OperandView{
                   .field_id = "barrier",
                   .actual_shape = check_end::OperandShape::Immediate,
                   .immediate_type = immediate->type,
                   .register_type = std::nullopt,
                   .locations = payload.barrier.locs,
               };
             }
             const auto &register_ref =
                 std::get<ResolvedRegisterRef>(payload.barrier.value);
             return OperandView{
                 .field_id = "barrier",
                 .actual_shape = check_end::OperandShape::Register,
                 .immediate_type = std::nullopt,
                 .register_type = register_ref.declared_type,
                 .locations = payload.barrier.locs,
             };
           }(),
           [&]() -> OperandView {
             if (const auto *immediate = std::get_if<ResolvedImmediate>(
                     &payload.thread_count.value)) {
               return OperandView{
                   .field_id = "thread_count",
                   .actual_shape = check_end::OperandShape::Immediate,
                   .immediate_type = immediate->type,
                   .register_type = std::nullopt,
                   .locations = payload.thread_count.locs,
               };
             }
             const auto &register_ref =
                 std::get<ResolvedRegisterRef>(payload.thread_count.value);
             return OperandView{
                 .field_id = "thread_count",
                 .actual_shape = check_end::OperandShape::Register,
                 .immediate_type = std::nullopt,
                 .register_type = register_ref.declared_type,
                 .locations = payload.thread_count.locs,
             };
           }()}};
      return check_operands(Bar::get_resolved_descriptor()
                                .variants[1]
                                .operand_layouts[1]
                                .bindings,
                            fields, operands,
                            Bar::get_checker_descriptor()
                                .variants[1]
                                .operand_type_compatibilities,
                            context);
    };
    static_assert(
        detail::VariantCheckFunction<
            decltype(check_bar_cta_sync_barrier_and_thread_count_operands),
            Bar::CtaSync::BarrierAndThreadCountOperands>);

    const auto &layouts =
        Bar::get_resolved_descriptor().variants[1].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "CtaSync", selected.operand_layout.value, layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Bar::get_checker_descriptor().variants[1],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto payload_check = std::visit(
          detail::Overloaded{
              check_bar_cta_sync_barrier_operands,
              check_bar_cta_sync_barrier_and_thread_count_operands},
          selected.operands);
      if (!payload_check) {
        diagnostics.insert(diagnostics.end(), payload_check.error().begin(),
                           payload_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(
      detail::VariantCheckFunction<decltype(check_cta_sync), Bar::CtaSync>);

  const auto check_arrive = [&](const Bar::Arrive &selected) -> CheckResult {
    const std::array<FieldView, 1> fields = {{FieldView{
        .field_id = "arrive",
        .bool_value = Bar::Arrive::arrive,
        .cache_operator = std::nullopt,
        .scalar_type = std::nullopt,
        .vector_arity = std::nullopt,
        .memory_state_space = std::nullopt,
        .memory_consistency = std::nullopt,
        .memory_scope = std::nullopt,
        .locations = std::span<const SourceRange>{},
    }}};
    const std::array<ModifierValueView, 1> modifier_values = {
        {ModifierValueView{
            .kind_id = "arrive",
            .value_kind = checker::ModifierValueKind::Bool,
            .bool_value = Bar::Arrive::arrive,
            .scalar_type = ScalarType::Invalid,
            .rounding_mode = RoundingMode::Invalid,
            .cache_operator = CacheOperator::Unspecified,
            .vector_arity = VectorArity::Invalid,
            .memory_state_space = MemoryStateSpace::Invalid,
            .memory_consistency = MemoryConsistency::Omitted,
            .memory_scope = MemoryScope::None,
            .is_present = true,
            .locations = std::span<const SourceRange>{},
        }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Bar::get_checker_descriptor(), "Arrive", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Bar::get_checker_descriptor().variants[2].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const std::array<OperandView, 2> operands = {
        {[&]() -> OperandView {
           if (const auto *immediate =
                   std::get_if<ResolvedImmediate>(&selected.barrier.value)) {
             return OperandView{
                 .field_id = "barrier",
                 .actual_shape = check_end::OperandShape::Immediate,
                 .immediate_type = immediate->type,
                 .register_type = std::nullopt,
                 .locations = selected.barrier.locs,
             };
           }
           const auto &register_ref =
               std::get<ResolvedRegisterRef>(selected.barrier.value);
           return OperandView{
               .field_id = "barrier",
               .actual_shape = check_end::OperandShape::Register,
               .immediate_type = std::nullopt,
               .register_type = register_ref.declared_type,
               .locations = selected.barrier.locs,
           };
         }(),
         [&]() -> OperandView {
           if (const auto *immediate = std::get_if<ResolvedImmediate>(
                   &selected.thread_count.value)) {
             return OperandView{
                 .field_id = "thread_count",
                 .actual_shape = check_end::OperandShape::Immediate,
                 .immediate_type = immediate->type,
                 .register_type = std::nullopt,
                 .locations = selected.thread_count.locs,
             };
           }
           const auto &register_ref =
               std::get<ResolvedRegisterRef>(selected.thread_count.value);
           return OperandView{
               .field_id = "thread_count",
               .actual_shape = check_end::OperandShape::Register,
               .immediate_type = std::nullopt,
               .register_type = register_ref.declared_type,
               .locations = selected.thread_count.locs,
           };
         }()}};
    const auto &layouts =
        Bar::get_resolved_descriptor().variants[2].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "Arrive", selected.operand_layout.value, layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Bar::get_checker_descriptor().variants[2],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto operand_check = check_operands(
          layouts[selected.operand_layout.value].bindings, fields, operands,
          Bar::get_checker_descriptor()
              .variants[2]
              .operand_type_compatibilities,
          context);
      if (!operand_check) {
        diagnostics.insert(diagnostics.end(), operand_check.error().begin(),
                           operand_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(
      detail::VariantCheckFunction<decltype(check_arrive), Bar::Arrive>);

  const auto check_cta_arrive =
      [&](const Bar::CtaArrive &selected) -> CheckResult {
    const std::array<FieldView, 2> fields = {
        {FieldView{
             .field_id = "cta",
             .bool_value = Bar::CtaArrive::cta,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         },
         FieldView{
             .field_id = "arrive",
             .bool_value = Bar::CtaArrive::arrive,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         }}};
    const std::array<ModifierValueView, 2> modifier_values = {
        {ModifierValueView{
             .kind_id = "cta",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = Bar::CtaArrive::cta,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         },
         ModifierValueView{
             .kind_id = "arrive",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = Bar::CtaArrive::arrive,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Bar::get_checker_descriptor(), "CtaArrive", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Bar::get_checker_descriptor().variants[3].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const std::array<OperandView, 2> operands = {
        {[&]() -> OperandView {
           if (const auto *immediate =
                   std::get_if<ResolvedImmediate>(&selected.barrier.value)) {
             return OperandView{
                 .field_id = "barrier",
                 .actual_shape = check_end::OperandShape::Immediate,
                 .immediate_type = immediate->type,
                 .register_type = std::nullopt,
                 .locations = selected.barrier.locs,
             };
           }
           const auto &register_ref =
               std::get<ResolvedRegisterRef>(selected.barrier.value);
           return OperandView{
               .field_id = "barrier",
               .actual_shape = check_end::OperandShape::Register,
               .immediate_type = std::nullopt,
               .register_type = register_ref.declared_type,
               .locations = selected.barrier.locs,
           };
         }(),
         [&]() -> OperandView {
           if (const auto *immediate = std::get_if<ResolvedImmediate>(
                   &selected.thread_count.value)) {
             return OperandView{
                 .field_id = "thread_count",
                 .actual_shape = check_end::OperandShape::Immediate,
                 .immediate_type = immediate->type,
                 .register_type = std::nullopt,
                 .locations = selected.thread_count.locs,
             };
           }
           const auto &register_ref =
               std::get<ResolvedRegisterRef>(selected.thread_count.value);
           return OperandView{
               .field_id = "thread_count",
               .actual_shape = check_end::OperandShape::Register,
               .immediate_type = std::nullopt,
               .register_type = register_ref.declared_type,
               .locations = selected.thread_count.locs,
           };
         }()}};
    const auto &layouts =
        Bar::get_resolved_descriptor().variants[3].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "CtaArrive", selected.operand_layout.value, layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Bar::get_checker_descriptor().variants[3],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto operand_check = check_operands(
          layouts[selected.operand_layout.value].bindings, fields, operands,
          Bar::get_checker_descriptor()
              .variants[3]
              .operand_type_compatibilities,
          context);
      if (!operand_check) {
        diagnostics.insert(diagnostics.end(), operand_check.error().begin(),
                           operand_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(
      detail::VariantCheckFunction<decltype(check_cta_arrive), Bar::CtaArrive>);

  const auto check_red_popc_u32 =
      [&](const Bar::RedPopcU32 &selected) -> CheckResult {
    const std::array<FieldView, 3> fields = {
        {FieldView{
             .field_id = "red",
             .bool_value = Bar::RedPopcU32::red,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         },
         FieldView{
             .field_id = "reduction",
             .bool_value = Bar::RedPopcU32::reduction,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         },
         FieldView{
             .field_id = "result_type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = Bar::RedPopcU32::result_type,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         }}};
    const std::array<ModifierValueView, 3> modifier_values = {
        {ModifierValueView{
             .kind_id = "red",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = Bar::RedPopcU32::red,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         },
         ModifierValueView{
             .kind_id = "reduction",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = Bar::RedPopcU32::reduction,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         },
         ModifierValueView{
             .kind_id = "result_type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = Bar::RedPopcU32::result_type,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Bar::get_checker_descriptor(), "RedPopcU32", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Bar::get_checker_descriptor().variants[4].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const auto check_bar_red_popc_u32_without_thread_count_operands =
        [&](const Bar::RedPopcU32::WithoutThreadCountOperands &payload)
        -> CheckResult {
      if (selected.operand_layout.value != 0) {
        return std::unexpected(CheckDiagnostics{CheckDiagnostic{
            .kind = CheckDiagnosticKind::OperandLayoutPayloadMismatch,
            .range = context.instruction_range,
            .message =
                "Resolved operand-layout tag and payload alternative disagree.",
        }});
      }
      const std::array<OperandView, 3> operands = {
          {OperandView{
               .field_id = "dst",
               .actual_shape = check_end::OperandShape::Register,
               .immediate_type = std::nullopt,
               .register_type = payload.dst.value.declared_type,
               .locations = payload.dst.locs,
           },
           [&]() -> OperandView {
             if (const auto *immediate =
                     std::get_if<ResolvedImmediate>(&payload.barrier.value)) {
               return OperandView{
                   .field_id = "barrier",
                   .actual_shape = check_end::OperandShape::Immediate,
                   .immediate_type = immediate->type,
                   .register_type = std::nullopt,
                   .locations = payload.barrier.locs,
               };
             }
             const auto &register_ref =
                 std::get<ResolvedRegisterRef>(payload.barrier.value);
             return OperandView{
                 .field_id = "barrier",
                 .actual_shape = check_end::OperandShape::Register,
                 .immediate_type = std::nullopt,
                 .register_type = register_ref.declared_type,
                 .locations = payload.barrier.locs,
             };
           }(),
           OperandView{
               .field_id = "predicate",
               .actual_shape = check_end::OperandShape::Predicate,
               .immediate_type = std::nullopt,
               .register_type =
                   payload.predicate.value.register_ref.declared_type,
               .locations = payload.predicate.locs,
           }}};
      return check_operands(Bar::get_resolved_descriptor()
                                .variants[4]
                                .operand_layouts[0]
                                .bindings,
                            fields, operands,
                            Bar::get_checker_descriptor()
                                .variants[4]
                                .operand_type_compatibilities,
                            context);
    };
    static_assert(
        detail::VariantCheckFunction<
            decltype(check_bar_red_popc_u32_without_thread_count_operands),
            Bar::RedPopcU32::WithoutThreadCountOperands>);

    const auto check_bar_red_popc_u32_with_thread_count_operands =
        [&](const Bar::RedPopcU32::WithThreadCountOperands &payload)
        -> CheckResult {
      if (selected.operand_layout.value != 1) {
        return std::unexpected(CheckDiagnostics{CheckDiagnostic{
            .kind = CheckDiagnosticKind::OperandLayoutPayloadMismatch,
            .range = context.instruction_range,
            .message =
                "Resolved operand-layout tag and payload alternative disagree.",
        }});
      }
      const std::array<OperandView, 4> operands = {
          {OperandView{
               .field_id = "dst",
               .actual_shape = check_end::OperandShape::Register,
               .immediate_type = std::nullopt,
               .register_type = payload.dst.value.declared_type,
               .locations = payload.dst.locs,
           },
           [&]() -> OperandView {
             if (const auto *immediate =
                     std::get_if<ResolvedImmediate>(&payload.barrier.value)) {
               return OperandView{
                   .field_id = "barrier",
                   .actual_shape = check_end::OperandShape::Immediate,
                   .immediate_type = immediate->type,
                   .register_type = std::nullopt,
                   .locations = payload.barrier.locs,
               };
             }
             const auto &register_ref =
                 std::get<ResolvedRegisterRef>(payload.barrier.value);
             return OperandView{
                 .field_id = "barrier",
                 .actual_shape = check_end::OperandShape::Register,
                 .immediate_type = std::nullopt,
                 .register_type = register_ref.declared_type,
                 .locations = payload.barrier.locs,
             };
           }(),
           [&]() -> OperandView {
             if (const auto *immediate = std::get_if<ResolvedImmediate>(
                     &payload.thread_count.value)) {
               return OperandView{
                   .field_id = "thread_count",
                   .actual_shape = check_end::OperandShape::Immediate,
                   .immediate_type = immediate->type,
                   .register_type = std::nullopt,
                   .locations = payload.thread_count.locs,
               };
             }
             const auto &register_ref =
                 std::get<ResolvedRegisterRef>(payload.thread_count.value);
             return OperandView{
                 .field_id = "thread_count",
                 .actual_shape = check_end::OperandShape::Register,
                 .immediate_type = std::nullopt,
                 .register_type = register_ref.declared_type,
                 .locations = payload.thread_count.locs,
             };
           }(),
           OperandView{
               .field_id = "predicate",
               .actual_shape = check_end::OperandShape::Predicate,
               .immediate_type = std::nullopt,
               .register_type =
                   payload.predicate.value.register_ref.declared_type,
               .locations = payload.predicate.locs,
           }}};
      return check_operands(Bar::get_resolved_descriptor()
                                .variants[4]
                                .operand_layouts[1]
                                .bindings,
                            fields, operands,
                            Bar::get_checker_descriptor()
                                .variants[4]
                                .operand_type_compatibilities,
                            context);
    };
    static_assert(detail::VariantCheckFunction<
                  decltype(check_bar_red_popc_u32_with_thread_count_operands),
                  Bar::RedPopcU32::WithThreadCountOperands>);

    const auto &layouts =
        Bar::get_resolved_descriptor().variants[4].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "RedPopcU32", selected.operand_layout.value, layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Bar::get_checker_descriptor().variants[4],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto payload_check = std::visit(
          detail::Overloaded{
              check_bar_red_popc_u32_without_thread_count_operands,
              check_bar_red_popc_u32_with_thread_count_operands},
          selected.operands);
      if (!payload_check) {
        diagnostics.insert(diagnostics.end(), payload_check.error().begin(),
                           payload_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(detail::VariantCheckFunction<decltype(check_red_popc_u32),
                                             Bar::RedPopcU32>);

  const auto check_cta_red_popc_u32 =
      [&](const Bar::CtaRedPopcU32 &selected) -> CheckResult {
    const std::array<FieldView, 4> fields = {
        {FieldView{
             .field_id = "cta",
             .bool_value = Bar::CtaRedPopcU32::cta,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         },
         FieldView{
             .field_id = "red",
             .bool_value = Bar::CtaRedPopcU32::red,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         },
         FieldView{
             .field_id = "reduction",
             .bool_value = Bar::CtaRedPopcU32::reduction,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         },
         FieldView{
             .field_id = "result_type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = Bar::CtaRedPopcU32::result_type,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         }}};
    const std::array<ModifierValueView, 4> modifier_values = {
        {ModifierValueView{
             .kind_id = "cta",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = Bar::CtaRedPopcU32::cta,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         },
         ModifierValueView{
             .kind_id = "red",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = Bar::CtaRedPopcU32::red,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         },
         ModifierValueView{
             .kind_id = "reduction",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = Bar::CtaRedPopcU32::reduction,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         },
         ModifierValueView{
             .kind_id = "result_type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = Bar::CtaRedPopcU32::result_type,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Bar::get_checker_descriptor(), "CtaRedPopcU32", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Bar::get_checker_descriptor().variants[5].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const auto check_bar_cta_red_popc_u32_without_thread_count_operands =
        [&](const Bar::CtaRedPopcU32::WithoutThreadCountOperands &payload)
        -> CheckResult {
      if (selected.operand_layout.value != 0) {
        return std::unexpected(CheckDiagnostics{CheckDiagnostic{
            .kind = CheckDiagnosticKind::OperandLayoutPayloadMismatch,
            .range = context.instruction_range,
            .message =
                "Resolved operand-layout tag and payload alternative disagree.",
        }});
      }
      const std::array<OperandView, 3> operands = {
          {OperandView{
               .field_id = "dst",
               .actual_shape = check_end::OperandShape::Register,
               .immediate_type = std::nullopt,
               .register_type = payload.dst.value.declared_type,
               .locations = payload.dst.locs,
           },
           [&]() -> OperandView {
             if (const auto *immediate =
                     std::get_if<ResolvedImmediate>(&payload.barrier.value)) {
               return OperandView{
                   .field_id = "barrier",
                   .actual_shape = check_end::OperandShape::Immediate,
                   .immediate_type = immediate->type,
                   .register_type = std::nullopt,
                   .locations = payload.barrier.locs,
               };
             }
             const auto &register_ref =
                 std::get<ResolvedRegisterRef>(payload.barrier.value);
             return OperandView{
                 .field_id = "barrier",
                 .actual_shape = check_end::OperandShape::Register,
                 .immediate_type = std::nullopt,
                 .register_type = register_ref.declared_type,
                 .locations = payload.barrier.locs,
             };
           }(),
           OperandView{
               .field_id = "predicate",
               .actual_shape = check_end::OperandShape::Predicate,
               .immediate_type = std::nullopt,
               .register_type =
                   payload.predicate.value.register_ref.declared_type,
               .locations = payload.predicate.locs,
           }}};
      return check_operands(Bar::get_resolved_descriptor()
                                .variants[5]
                                .operand_layouts[0]
                                .bindings,
                            fields, operands,
                            Bar::get_checker_descriptor()
                                .variants[5]
                                .operand_type_compatibilities,
                            context);
    };
    static_assert(
        detail::VariantCheckFunction<
            decltype(check_bar_cta_red_popc_u32_without_thread_count_operands),
            Bar::CtaRedPopcU32::WithoutThreadCountOperands>);

    const auto check_bar_cta_red_popc_u32_with_thread_count_operands =
        [&](const Bar::CtaRedPopcU32::WithThreadCountOperands &payload)
        -> CheckResult {
      if (selected.operand_layout.value != 1) {
        return std::unexpected(CheckDiagnostics{CheckDiagnostic{
            .kind = CheckDiagnosticKind::OperandLayoutPayloadMismatch,
            .range = context.instruction_range,
            .message =
                "Resolved operand-layout tag and payload alternative disagree.",
        }});
      }
      const std::array<OperandView, 4> operands = {
          {OperandView{
               .field_id = "dst",
               .actual_shape = check_end::OperandShape::Register,
               .immediate_type = std::nullopt,
               .register_type = payload.dst.value.declared_type,
               .locations = payload.dst.locs,
           },
           [&]() -> OperandView {
             if (const auto *immediate =
                     std::get_if<ResolvedImmediate>(&payload.barrier.value)) {
               return OperandView{
                   .field_id = "barrier",
                   .actual_shape = check_end::OperandShape::Immediate,
                   .immediate_type = immediate->type,
                   .register_type = std::nullopt,
                   .locations = payload.barrier.locs,
               };
             }
             const auto &register_ref =
                 std::get<ResolvedRegisterRef>(payload.barrier.value);
             return OperandView{
                 .field_id = "barrier",
                 .actual_shape = check_end::OperandShape::Register,
                 .immediate_type = std::nullopt,
                 .register_type = register_ref.declared_type,
                 .locations = payload.barrier.locs,
             };
           }(),
           [&]() -> OperandView {
             if (const auto *immediate = std::get_if<ResolvedImmediate>(
                     &payload.thread_count.value)) {
               return OperandView{
                   .field_id = "thread_count",
                   .actual_shape = check_end::OperandShape::Immediate,
                   .immediate_type = immediate->type,
                   .register_type = std::nullopt,
                   .locations = payload.thread_count.locs,
               };
             }
             const auto &register_ref =
                 std::get<ResolvedRegisterRef>(payload.thread_count.value);
             return OperandView{
                 .field_id = "thread_count",
                 .actual_shape = check_end::OperandShape::Register,
                 .immediate_type = std::nullopt,
                 .register_type = register_ref.declared_type,
                 .locations = payload.thread_count.locs,
             };
           }(),
           OperandView{
               .field_id = "predicate",
               .actual_shape = check_end::OperandShape::Predicate,
               .immediate_type = std::nullopt,
               .register_type =
                   payload.predicate.value.register_ref.declared_type,
               .locations = payload.predicate.locs,
           }}};
      return check_operands(Bar::get_resolved_descriptor()
                                .variants[5]
                                .operand_layouts[1]
                                .bindings,
                            fields, operands,
                            Bar::get_checker_descriptor()
                                .variants[5]
                                .operand_type_compatibilities,
                            context);
    };
    static_assert(
        detail::VariantCheckFunction<
            decltype(check_bar_cta_red_popc_u32_with_thread_count_operands),
            Bar::CtaRedPopcU32::WithThreadCountOperands>);

    const auto &layouts =
        Bar::get_resolved_descriptor().variants[5].operand_layouts;
    const auto layout_check =
        check_operand_layout_tag("CtaRedPopcU32", selected.operand_layout.value,
                                 layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Bar::get_checker_descriptor().variants[5],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto payload_check = std::visit(
          detail::Overloaded{
              check_bar_cta_red_popc_u32_without_thread_count_operands,
              check_bar_cta_red_popc_u32_with_thread_count_operands},
          selected.operands);
      if (!payload_check) {
        diagnostics.insert(diagnostics.end(), payload_check.error().begin(),
                           payload_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(detail::VariantCheckFunction<decltype(check_cta_red_popc_u32),
                                             Bar::CtaRedPopcU32>);

  const auto check_red_and_pred =
      [&](const Bar::RedAndPred &selected) -> CheckResult {
    const std::array<FieldView, 3> fields = {
        {FieldView{
             .field_id = "red",
             .bool_value = Bar::RedAndPred::red,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         },
         FieldView{
             .field_id = "reduction",
             .bool_value = Bar::RedAndPred::reduction,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         },
         FieldView{
             .field_id = "result_type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = Bar::RedAndPred::result_type,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         }}};
    const std::array<ModifierValueView, 3> modifier_values = {
        {ModifierValueView{
             .kind_id = "red",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = Bar::RedAndPred::red,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         },
         ModifierValueView{
             .kind_id = "reduction",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = Bar::RedAndPred::reduction,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         },
         ModifierValueView{
             .kind_id = "result_type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = Bar::RedAndPred::result_type,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Bar::get_checker_descriptor(), "RedAndPred", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Bar::get_checker_descriptor().variants[6].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const auto check_bar_red_and_pred_without_thread_count_operands =
        [&](const Bar::RedAndPred::WithoutThreadCountOperands &payload)
        -> CheckResult {
      if (selected.operand_layout.value != 0) {
        return std::unexpected(CheckDiagnostics{CheckDiagnostic{
            .kind = CheckDiagnosticKind::OperandLayoutPayloadMismatch,
            .range = context.instruction_range,
            .message =
                "Resolved operand-layout tag and payload alternative disagree.",
        }});
      }
      const std::array<OperandView, 3> operands = {
          {OperandView{
               .field_id = "dst",
               .actual_shape = check_end::OperandShape::Predicate,
               .immediate_type = std::nullopt,
               .register_type = payload.dst.value.register_ref.declared_type,
               .locations = payload.dst.locs,
           },
           [&]() -> OperandView {
             if (const auto *immediate =
                     std::get_if<ResolvedImmediate>(&payload.barrier.value)) {
               return OperandView{
                   .field_id = "barrier",
                   .actual_shape = check_end::OperandShape::Immediate,
                   .immediate_type = immediate->type,
                   .register_type = std::nullopt,
                   .locations = payload.barrier.locs,
               };
             }
             const auto &register_ref =
                 std::get<ResolvedRegisterRef>(payload.barrier.value);
             return OperandView{
                 .field_id = "barrier",
                 .actual_shape = check_end::OperandShape::Register,
                 .immediate_type = std::nullopt,
                 .register_type = register_ref.declared_type,
                 .locations = payload.barrier.locs,
             };
           }(),
           OperandView{
               .field_id = "predicate",
               .actual_shape = check_end::OperandShape::Predicate,
               .immediate_type = std::nullopt,
               .register_type =
                   payload.predicate.value.register_ref.declared_type,
               .locations = payload.predicate.locs,
           }}};
      return check_operands(Bar::get_resolved_descriptor()
                                .variants[6]
                                .operand_layouts[0]
                                .bindings,
                            fields, operands,
                            Bar::get_checker_descriptor()
                                .variants[6]
                                .operand_type_compatibilities,
                            context);
    };
    static_assert(
        detail::VariantCheckFunction<
            decltype(check_bar_red_and_pred_without_thread_count_operands),
            Bar::RedAndPred::WithoutThreadCountOperands>);

    const auto check_bar_red_and_pred_with_thread_count_operands =
        [&](const Bar::RedAndPred::WithThreadCountOperands &payload)
        -> CheckResult {
      if (selected.operand_layout.value != 1) {
        return std::unexpected(CheckDiagnostics{CheckDiagnostic{
            .kind = CheckDiagnosticKind::OperandLayoutPayloadMismatch,
            .range = context.instruction_range,
            .message =
                "Resolved operand-layout tag and payload alternative disagree.",
        }});
      }
      const std::array<OperandView, 4> operands = {
          {OperandView{
               .field_id = "dst",
               .actual_shape = check_end::OperandShape::Predicate,
               .immediate_type = std::nullopt,
               .register_type = payload.dst.value.register_ref.declared_type,
               .locations = payload.dst.locs,
           },
           [&]() -> OperandView {
             if (const auto *immediate =
                     std::get_if<ResolvedImmediate>(&payload.barrier.value)) {
               return OperandView{
                   .field_id = "barrier",
                   .actual_shape = check_end::OperandShape::Immediate,
                   .immediate_type = immediate->type,
                   .register_type = std::nullopt,
                   .locations = payload.barrier.locs,
               };
             }
             const auto &register_ref =
                 std::get<ResolvedRegisterRef>(payload.barrier.value);
             return OperandView{
                 .field_id = "barrier",
                 .actual_shape = check_end::OperandShape::Register,
                 .immediate_type = std::nullopt,
                 .register_type = register_ref.declared_type,
                 .locations = payload.barrier.locs,
             };
           }(),
           [&]() -> OperandView {
             if (const auto *immediate = std::get_if<ResolvedImmediate>(
                     &payload.thread_count.value)) {
               return OperandView{
                   .field_id = "thread_count",
                   .actual_shape = check_end::OperandShape::Immediate,
                   .immediate_type = immediate->type,
                   .register_type = std::nullopt,
                   .locations = payload.thread_count.locs,
               };
             }
             const auto &register_ref =
                 std::get<ResolvedRegisterRef>(payload.thread_count.value);
             return OperandView{
                 .field_id = "thread_count",
                 .actual_shape = check_end::OperandShape::Register,
                 .immediate_type = std::nullopt,
                 .register_type = register_ref.declared_type,
                 .locations = payload.thread_count.locs,
             };
           }(),
           OperandView{
               .field_id = "predicate",
               .actual_shape = check_end::OperandShape::Predicate,
               .immediate_type = std::nullopt,
               .register_type =
                   payload.predicate.value.register_ref.declared_type,
               .locations = payload.predicate.locs,
           }}};
      return check_operands(Bar::get_resolved_descriptor()
                                .variants[6]
                                .operand_layouts[1]
                                .bindings,
                            fields, operands,
                            Bar::get_checker_descriptor()
                                .variants[6]
                                .operand_type_compatibilities,
                            context);
    };
    static_assert(detail::VariantCheckFunction<
                  decltype(check_bar_red_and_pred_with_thread_count_operands),
                  Bar::RedAndPred::WithThreadCountOperands>);

    const auto &layouts =
        Bar::get_resolved_descriptor().variants[6].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "RedAndPred", selected.operand_layout.value, layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Bar::get_checker_descriptor().variants[6],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto payload_check = std::visit(
          detail::Overloaded{
              check_bar_red_and_pred_without_thread_count_operands,
              check_bar_red_and_pred_with_thread_count_operands},
          selected.operands);
      if (!payload_check) {
        diagnostics.insert(diagnostics.end(), payload_check.error().begin(),
                           payload_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(detail::VariantCheckFunction<decltype(check_red_and_pred),
                                             Bar::RedAndPred>);

  const auto check_cta_red_and_pred =
      [&](const Bar::CtaRedAndPred &selected) -> CheckResult {
    const std::array<FieldView, 4> fields = {
        {FieldView{
             .field_id = "cta",
             .bool_value = Bar::CtaRedAndPred::cta,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         },
         FieldView{
             .field_id = "red",
             .bool_value = Bar::CtaRedAndPred::red,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         },
         FieldView{
             .field_id = "reduction",
             .bool_value = Bar::CtaRedAndPred::reduction,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         },
         FieldView{
             .field_id = "result_type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = Bar::CtaRedAndPred::result_type,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         }}};
    const std::array<ModifierValueView, 4> modifier_values = {
        {ModifierValueView{
             .kind_id = "cta",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = Bar::CtaRedAndPred::cta,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         },
         ModifierValueView{
             .kind_id = "red",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = Bar::CtaRedAndPred::red,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         },
         ModifierValueView{
             .kind_id = "reduction",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = Bar::CtaRedAndPred::reduction,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         },
         ModifierValueView{
             .kind_id = "result_type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = Bar::CtaRedAndPred::result_type,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Bar::get_checker_descriptor(), "CtaRedAndPred", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Bar::get_checker_descriptor().variants[7].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const auto check_bar_cta_red_and_pred_without_thread_count_operands =
        [&](const Bar::CtaRedAndPred::WithoutThreadCountOperands &payload)
        -> CheckResult {
      if (selected.operand_layout.value != 0) {
        return std::unexpected(CheckDiagnostics{CheckDiagnostic{
            .kind = CheckDiagnosticKind::OperandLayoutPayloadMismatch,
            .range = context.instruction_range,
            .message =
                "Resolved operand-layout tag and payload alternative disagree.",
        }});
      }
      const std::array<OperandView, 3> operands = {
          {OperandView{
               .field_id = "dst",
               .actual_shape = check_end::OperandShape::Predicate,
               .immediate_type = std::nullopt,
               .register_type = payload.dst.value.register_ref.declared_type,
               .locations = payload.dst.locs,
           },
           [&]() -> OperandView {
             if (const auto *immediate =
                     std::get_if<ResolvedImmediate>(&payload.barrier.value)) {
               return OperandView{
                   .field_id = "barrier",
                   .actual_shape = check_end::OperandShape::Immediate,
                   .immediate_type = immediate->type,
                   .register_type = std::nullopt,
                   .locations = payload.barrier.locs,
               };
             }
             const auto &register_ref =
                 std::get<ResolvedRegisterRef>(payload.barrier.value);
             return OperandView{
                 .field_id = "barrier",
                 .actual_shape = check_end::OperandShape::Register,
                 .immediate_type = std::nullopt,
                 .register_type = register_ref.declared_type,
                 .locations = payload.barrier.locs,
             };
           }(),
           OperandView{
               .field_id = "predicate",
               .actual_shape = check_end::OperandShape::Predicate,
               .immediate_type = std::nullopt,
               .register_type =
                   payload.predicate.value.register_ref.declared_type,
               .locations = payload.predicate.locs,
           }}};
      return check_operands(Bar::get_resolved_descriptor()
                                .variants[7]
                                .operand_layouts[0]
                                .bindings,
                            fields, operands,
                            Bar::get_checker_descriptor()
                                .variants[7]
                                .operand_type_compatibilities,
                            context);
    };
    static_assert(
        detail::VariantCheckFunction<
            decltype(check_bar_cta_red_and_pred_without_thread_count_operands),
            Bar::CtaRedAndPred::WithoutThreadCountOperands>);

    const auto check_bar_cta_red_and_pred_with_thread_count_operands =
        [&](const Bar::CtaRedAndPred::WithThreadCountOperands &payload)
        -> CheckResult {
      if (selected.operand_layout.value != 1) {
        return std::unexpected(CheckDiagnostics{CheckDiagnostic{
            .kind = CheckDiagnosticKind::OperandLayoutPayloadMismatch,
            .range = context.instruction_range,
            .message =
                "Resolved operand-layout tag and payload alternative disagree.",
        }});
      }
      const std::array<OperandView, 4> operands = {
          {OperandView{
               .field_id = "dst",
               .actual_shape = check_end::OperandShape::Predicate,
               .immediate_type = std::nullopt,
               .register_type = payload.dst.value.register_ref.declared_type,
               .locations = payload.dst.locs,
           },
           [&]() -> OperandView {
             if (const auto *immediate =
                     std::get_if<ResolvedImmediate>(&payload.barrier.value)) {
               return OperandView{
                   .field_id = "barrier",
                   .actual_shape = check_end::OperandShape::Immediate,
                   .immediate_type = immediate->type,
                   .register_type = std::nullopt,
                   .locations = payload.barrier.locs,
               };
             }
             const auto &register_ref =
                 std::get<ResolvedRegisterRef>(payload.barrier.value);
             return OperandView{
                 .field_id = "barrier",
                 .actual_shape = check_end::OperandShape::Register,
                 .immediate_type = std::nullopt,
                 .register_type = register_ref.declared_type,
                 .locations = payload.barrier.locs,
             };
           }(),
           [&]() -> OperandView {
             if (const auto *immediate = std::get_if<ResolvedImmediate>(
                     &payload.thread_count.value)) {
               return OperandView{
                   .field_id = "thread_count",
                   .actual_shape = check_end::OperandShape::Immediate,
                   .immediate_type = immediate->type,
                   .register_type = std::nullopt,
                   .locations = payload.thread_count.locs,
               };
             }
             const auto &register_ref =
                 std::get<ResolvedRegisterRef>(payload.thread_count.value);
             return OperandView{
                 .field_id = "thread_count",
                 .actual_shape = check_end::OperandShape::Register,
                 .immediate_type = std::nullopt,
                 .register_type = register_ref.declared_type,
                 .locations = payload.thread_count.locs,
             };
           }(),
           OperandView{
               .field_id = "predicate",
               .actual_shape = check_end::OperandShape::Predicate,
               .immediate_type = std::nullopt,
               .register_type =
                   payload.predicate.value.register_ref.declared_type,
               .locations = payload.predicate.locs,
           }}};
      return check_operands(Bar::get_resolved_descriptor()
                                .variants[7]
                                .operand_layouts[1]
                                .bindings,
                            fields, operands,
                            Bar::get_checker_descriptor()
                                .variants[7]
                                .operand_type_compatibilities,
                            context);
    };
    static_assert(
        detail::VariantCheckFunction<
            decltype(check_bar_cta_red_and_pred_with_thread_count_operands),
            Bar::CtaRedAndPred::WithThreadCountOperands>);

    const auto &layouts =
        Bar::get_resolved_descriptor().variants[7].operand_layouts;
    const auto layout_check =
        check_operand_layout_tag("CtaRedAndPred", selected.operand_layout.value,
                                 layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Bar::get_checker_descriptor().variants[7],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto payload_check = std::visit(
          detail::Overloaded{
              check_bar_cta_red_and_pred_without_thread_count_operands,
              check_bar_cta_red_and_pred_with_thread_count_operands},
          selected.operands);
      if (!payload_check) {
        diagnostics.insert(diagnostics.end(), payload_check.error().begin(),
                           payload_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(detail::VariantCheckFunction<decltype(check_cta_red_and_pred),
                                             Bar::CtaRedAndPred>);

  const auto check_red_or_pred =
      [&](const Bar::RedOrPred &selected) -> CheckResult {
    const std::array<FieldView, 3> fields = {
        {FieldView{
             .field_id = "red",
             .bool_value = Bar::RedOrPred::red,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         },
         FieldView{
             .field_id = "reduction",
             .bool_value = Bar::RedOrPred::reduction,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         },
         FieldView{
             .field_id = "result_type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = Bar::RedOrPred::result_type,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         }}};
    const std::array<ModifierValueView, 3> modifier_values = {
        {ModifierValueView{
             .kind_id = "red",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = Bar::RedOrPred::red,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         },
         ModifierValueView{
             .kind_id = "reduction",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = Bar::RedOrPred::reduction,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         },
         ModifierValueView{
             .kind_id = "result_type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = Bar::RedOrPred::result_type,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Bar::get_checker_descriptor(), "RedOrPred", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Bar::get_checker_descriptor().variants[8].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const auto check_bar_red_or_pred_without_thread_count_operands =
        [&](const Bar::RedOrPred::WithoutThreadCountOperands &payload)
        -> CheckResult {
      if (selected.operand_layout.value != 0) {
        return std::unexpected(CheckDiagnostics{CheckDiagnostic{
            .kind = CheckDiagnosticKind::OperandLayoutPayloadMismatch,
            .range = context.instruction_range,
            .message =
                "Resolved operand-layout tag and payload alternative disagree.",
        }});
      }
      const std::array<OperandView, 3> operands = {
          {OperandView{
               .field_id = "dst",
               .actual_shape = check_end::OperandShape::Predicate,
               .immediate_type = std::nullopt,
               .register_type = payload.dst.value.register_ref.declared_type,
               .locations = payload.dst.locs,
           },
           [&]() -> OperandView {
             if (const auto *immediate =
                     std::get_if<ResolvedImmediate>(&payload.barrier.value)) {
               return OperandView{
                   .field_id = "barrier",
                   .actual_shape = check_end::OperandShape::Immediate,
                   .immediate_type = immediate->type,
                   .register_type = std::nullopt,
                   .locations = payload.barrier.locs,
               };
             }
             const auto &register_ref =
                 std::get<ResolvedRegisterRef>(payload.barrier.value);
             return OperandView{
                 .field_id = "barrier",
                 .actual_shape = check_end::OperandShape::Register,
                 .immediate_type = std::nullopt,
                 .register_type = register_ref.declared_type,
                 .locations = payload.barrier.locs,
             };
           }(),
           OperandView{
               .field_id = "predicate",
               .actual_shape = check_end::OperandShape::Predicate,
               .immediate_type = std::nullopt,
               .register_type =
                   payload.predicate.value.register_ref.declared_type,
               .locations = payload.predicate.locs,
           }}};
      return check_operands(Bar::get_resolved_descriptor()
                                .variants[8]
                                .operand_layouts[0]
                                .bindings,
                            fields, operands,
                            Bar::get_checker_descriptor()
                                .variants[8]
                                .operand_type_compatibilities,
                            context);
    };
    static_assert(detail::VariantCheckFunction<
                  decltype(check_bar_red_or_pred_without_thread_count_operands),
                  Bar::RedOrPred::WithoutThreadCountOperands>);

    const auto check_bar_red_or_pred_with_thread_count_operands =
        [&](const Bar::RedOrPred::WithThreadCountOperands &payload)
        -> CheckResult {
      if (selected.operand_layout.value != 1) {
        return std::unexpected(CheckDiagnostics{CheckDiagnostic{
            .kind = CheckDiagnosticKind::OperandLayoutPayloadMismatch,
            .range = context.instruction_range,
            .message =
                "Resolved operand-layout tag and payload alternative disagree.",
        }});
      }
      const std::array<OperandView, 4> operands = {
          {OperandView{
               .field_id = "dst",
               .actual_shape = check_end::OperandShape::Predicate,
               .immediate_type = std::nullopt,
               .register_type = payload.dst.value.register_ref.declared_type,
               .locations = payload.dst.locs,
           },
           [&]() -> OperandView {
             if (const auto *immediate =
                     std::get_if<ResolvedImmediate>(&payload.barrier.value)) {
               return OperandView{
                   .field_id = "barrier",
                   .actual_shape = check_end::OperandShape::Immediate,
                   .immediate_type = immediate->type,
                   .register_type = std::nullopt,
                   .locations = payload.barrier.locs,
               };
             }
             const auto &register_ref =
                 std::get<ResolvedRegisterRef>(payload.barrier.value);
             return OperandView{
                 .field_id = "barrier",
                 .actual_shape = check_end::OperandShape::Register,
                 .immediate_type = std::nullopt,
                 .register_type = register_ref.declared_type,
                 .locations = payload.barrier.locs,
             };
           }(),
           [&]() -> OperandView {
             if (const auto *immediate = std::get_if<ResolvedImmediate>(
                     &payload.thread_count.value)) {
               return OperandView{
                   .field_id = "thread_count",
                   .actual_shape = check_end::OperandShape::Immediate,
                   .immediate_type = immediate->type,
                   .register_type = std::nullopt,
                   .locations = payload.thread_count.locs,
               };
             }
             const auto &register_ref =
                 std::get<ResolvedRegisterRef>(payload.thread_count.value);
             return OperandView{
                 .field_id = "thread_count",
                 .actual_shape = check_end::OperandShape::Register,
                 .immediate_type = std::nullopt,
                 .register_type = register_ref.declared_type,
                 .locations = payload.thread_count.locs,
             };
           }(),
           OperandView{
               .field_id = "predicate",
               .actual_shape = check_end::OperandShape::Predicate,
               .immediate_type = std::nullopt,
               .register_type =
                   payload.predicate.value.register_ref.declared_type,
               .locations = payload.predicate.locs,
           }}};
      return check_operands(Bar::get_resolved_descriptor()
                                .variants[8]
                                .operand_layouts[1]
                                .bindings,
                            fields, operands,
                            Bar::get_checker_descriptor()
                                .variants[8]
                                .operand_type_compatibilities,
                            context);
    };
    static_assert(detail::VariantCheckFunction<
                  decltype(check_bar_red_or_pred_with_thread_count_operands),
                  Bar::RedOrPred::WithThreadCountOperands>);

    const auto &layouts =
        Bar::get_resolved_descriptor().variants[8].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "RedOrPred", selected.operand_layout.value, layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Bar::get_checker_descriptor().variants[8],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto payload_check = std::visit(
          detail::Overloaded{
              check_bar_red_or_pred_without_thread_count_operands,
              check_bar_red_or_pred_with_thread_count_operands},
          selected.operands);
      if (!payload_check) {
        diagnostics.insert(diagnostics.end(), payload_check.error().begin(),
                           payload_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(detail::VariantCheckFunction<decltype(check_red_or_pred),
                                             Bar::RedOrPred>);

  const auto check_cta_red_or_pred =
      [&](const Bar::CtaRedOrPred &selected) -> CheckResult {
    const std::array<FieldView, 4> fields = {
        {FieldView{
             .field_id = "cta",
             .bool_value = Bar::CtaRedOrPred::cta,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         },
         FieldView{
             .field_id = "red",
             .bool_value = Bar::CtaRedOrPred::red,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         },
         FieldView{
             .field_id = "reduction",
             .bool_value = Bar::CtaRedOrPred::reduction,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         },
         FieldView{
             .field_id = "result_type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = Bar::CtaRedOrPred::result_type,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         }}};
    const std::array<ModifierValueView, 4> modifier_values = {
        {ModifierValueView{
             .kind_id = "cta",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = Bar::CtaRedOrPred::cta,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         },
         ModifierValueView{
             .kind_id = "red",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = Bar::CtaRedOrPred::red,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         },
         ModifierValueView{
             .kind_id = "reduction",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = Bar::CtaRedOrPred::reduction,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         },
         ModifierValueView{
             .kind_id = "result_type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = Bar::CtaRedOrPred::result_type,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Bar::get_checker_descriptor(), "CtaRedOrPred", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Bar::get_checker_descriptor().variants[9].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const auto check_bar_cta_red_or_pred_without_thread_count_operands =
        [&](const Bar::CtaRedOrPred::WithoutThreadCountOperands &payload)
        -> CheckResult {
      if (selected.operand_layout.value != 0) {
        return std::unexpected(CheckDiagnostics{CheckDiagnostic{
            .kind = CheckDiagnosticKind::OperandLayoutPayloadMismatch,
            .range = context.instruction_range,
            .message =
                "Resolved operand-layout tag and payload alternative disagree.",
        }});
      }
      const std::array<OperandView, 3> operands = {
          {OperandView{
               .field_id = "dst",
               .actual_shape = check_end::OperandShape::Predicate,
               .immediate_type = std::nullopt,
               .register_type = payload.dst.value.register_ref.declared_type,
               .locations = payload.dst.locs,
           },
           [&]() -> OperandView {
             if (const auto *immediate =
                     std::get_if<ResolvedImmediate>(&payload.barrier.value)) {
               return OperandView{
                   .field_id = "barrier",
                   .actual_shape = check_end::OperandShape::Immediate,
                   .immediate_type = immediate->type,
                   .register_type = std::nullopt,
                   .locations = payload.barrier.locs,
               };
             }
             const auto &register_ref =
                 std::get<ResolvedRegisterRef>(payload.barrier.value);
             return OperandView{
                 .field_id = "barrier",
                 .actual_shape = check_end::OperandShape::Register,
                 .immediate_type = std::nullopt,
                 .register_type = register_ref.declared_type,
                 .locations = payload.barrier.locs,
             };
           }(),
           OperandView{
               .field_id = "predicate",
               .actual_shape = check_end::OperandShape::Predicate,
               .immediate_type = std::nullopt,
               .register_type =
                   payload.predicate.value.register_ref.declared_type,
               .locations = payload.predicate.locs,
           }}};
      return check_operands(Bar::get_resolved_descriptor()
                                .variants[9]
                                .operand_layouts[0]
                                .bindings,
                            fields, operands,
                            Bar::get_checker_descriptor()
                                .variants[9]
                                .operand_type_compatibilities,
                            context);
    };
    static_assert(
        detail::VariantCheckFunction<
            decltype(check_bar_cta_red_or_pred_without_thread_count_operands),
            Bar::CtaRedOrPred::WithoutThreadCountOperands>);

    const auto check_bar_cta_red_or_pred_with_thread_count_operands =
        [&](const Bar::CtaRedOrPred::WithThreadCountOperands &payload)
        -> CheckResult {
      if (selected.operand_layout.value != 1) {
        return std::unexpected(CheckDiagnostics{CheckDiagnostic{
            .kind = CheckDiagnosticKind::OperandLayoutPayloadMismatch,
            .range = context.instruction_range,
            .message =
                "Resolved operand-layout tag and payload alternative disagree.",
        }});
      }
      const std::array<OperandView, 4> operands = {
          {OperandView{
               .field_id = "dst",
               .actual_shape = check_end::OperandShape::Predicate,
               .immediate_type = std::nullopt,
               .register_type = payload.dst.value.register_ref.declared_type,
               .locations = payload.dst.locs,
           },
           [&]() -> OperandView {
             if (const auto *immediate =
                     std::get_if<ResolvedImmediate>(&payload.barrier.value)) {
               return OperandView{
                   .field_id = "barrier",
                   .actual_shape = check_end::OperandShape::Immediate,
                   .immediate_type = immediate->type,
                   .register_type = std::nullopt,
                   .locations = payload.barrier.locs,
               };
             }
             const auto &register_ref =
                 std::get<ResolvedRegisterRef>(payload.barrier.value);
             return OperandView{
                 .field_id = "barrier",
                 .actual_shape = check_end::OperandShape::Register,
                 .immediate_type = std::nullopt,
                 .register_type = register_ref.declared_type,
                 .locations = payload.barrier.locs,
             };
           }(),
           [&]() -> OperandView {
             if (const auto *immediate = std::get_if<ResolvedImmediate>(
                     &payload.thread_count.value)) {
               return OperandView{
                   .field_id = "thread_count",
                   .actual_shape = check_end::OperandShape::Immediate,
                   .immediate_type = immediate->type,
                   .register_type = std::nullopt,
                   .locations = payload.thread_count.locs,
               };
             }
             const auto &register_ref =
                 std::get<ResolvedRegisterRef>(payload.thread_count.value);
             return OperandView{
                 .field_id = "thread_count",
                 .actual_shape = check_end::OperandShape::Register,
                 .immediate_type = std::nullopt,
                 .register_type = register_ref.declared_type,
                 .locations = payload.thread_count.locs,
             };
           }(),
           OperandView{
               .field_id = "predicate",
               .actual_shape = check_end::OperandShape::Predicate,
               .immediate_type = std::nullopt,
               .register_type =
                   payload.predicate.value.register_ref.declared_type,
               .locations = payload.predicate.locs,
           }}};
      return check_operands(Bar::get_resolved_descriptor()
                                .variants[9]
                                .operand_layouts[1]
                                .bindings,
                            fields, operands,
                            Bar::get_checker_descriptor()
                                .variants[9]
                                .operand_type_compatibilities,
                            context);
    };
    static_assert(
        detail::VariantCheckFunction<
            decltype(check_bar_cta_red_or_pred_with_thread_count_operands),
            Bar::CtaRedOrPred::WithThreadCountOperands>);

    const auto &layouts =
        Bar::get_resolved_descriptor().variants[9].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "CtaRedOrPred", selected.operand_layout.value, layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Bar::get_checker_descriptor().variants[9],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto payload_check = std::visit(
          detail::Overloaded{
              check_bar_cta_red_or_pred_without_thread_count_operands,
              check_bar_cta_red_or_pred_with_thread_count_operands},
          selected.operands);
      if (!payload_check) {
        diagnostics.insert(diagnostics.end(), payload_check.error().begin(),
                           payload_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(detail::VariantCheckFunction<decltype(check_cta_red_or_pred),
                                             Bar::CtaRedOrPred>);

  return std::visit(
      detail::Overloaded{
          check_sync, check_cta_sync, check_arrive, check_cta_arrive,
          check_red_popc_u32, check_cta_red_popc_u32, check_red_and_pred,
          check_cta_red_and_pred, check_red_or_pred, check_cta_red_or_pred},
      instruction.variant);
}

} // namespace checker

} // namespace ptx_frontend::resolved_ir
