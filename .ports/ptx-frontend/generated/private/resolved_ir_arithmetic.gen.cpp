// Generated by python/scripts/gen_all.py. Do not edit.
// Generated at: omitted (set SOURCE_DATE_EPOCH for a reproducible timestamp)

#include "resolved_ir.gen.hpp"

namespace ptx_frontend::resolved_ir {

template <>
std::expected<Add, ResolveDiagnostic>
resolve<Add>(const syntax_ast::AstInstruction &ast,
             const ResolveContext *context) {
  const auto selected_variant = selectVariant<Add>(ast);
  if (!selected_variant)
    return std::unexpected(selected_variant.error());

  auto fields = resolve_fields(
      ast, Add::get_syntax_descriptor(), Add::get_resolved_descriptor(),
      magic_enum::enum_name(*selected_variant), context);
  if (!fields)
    return std::unexpected(fields.error());

  if (fields->variant_name == "FloatF32") {
    return Add{
        .execution_predicate = std::move(fields->execution_predicate),
        .variant = Add::FloatF32{
            .operand_layout = fields->operand_layout,
            .rounding = resolved_modifier<RoundingMode>(*fields, "rounding"),
            .ftz = resolved_modifier<bool>(*fields, "ftz"),
            .saturate = resolved_modifier<bool>(*fields, "saturate"),
            .dst = resolved_operand<ResolvedRegisterRef>(*fields, "dst"),
            .src1 = resolved_operand<RegOrImm>(*fields, "src1"),
            .src2 = resolved_operand<RegOrImm>(*fields, "src2"),
        }};
  }
  if (fields->variant_name == "FloatF32x2") {
    return Add{
        .execution_predicate = std::move(fields->execution_predicate),
        .variant = Add::FloatF32x2{
            .operand_layout = fields->operand_layout,
            .rounding = resolved_modifier<RoundingMode>(*fields, "rounding"),
            .ftz = resolved_modifier<bool>(*fields, "ftz"),
            .dst = resolved_operand<ResolvedRegisterRef>(*fields, "dst"),
            .src1 = resolved_operand<ResolvedRegisterRef>(*fields, "src1"),
            .src2 = resolved_operand<ResolvedRegisterRef>(*fields, "src2"),
        }};
  }
  if (fields->variant_name == "FloatF64") {
    return Add{
        .execution_predicate = std::move(fields->execution_predicate),
        .variant = Add::FloatF64{
            .operand_layout = fields->operand_layout,
            .rounding = resolved_modifier<RoundingMode>(*fields, "rounding"),
            .dst = resolved_operand<ResolvedRegisterRef>(*fields, "dst"),
            .src1 = resolved_operand<RegOrImm>(*fields, "src1"),
            .src2 = resolved_operand<RegOrImm>(*fields, "src2"),
        }};
  }
  if (fields->variant_name == "Half") {
    return Add{
        .execution_predicate = std::move(fields->execution_predicate),
        .variant = Add::Half{
            .operand_layout = fields->operand_layout,
            .rounding = resolved_modifier<RoundingMode>(*fields, "rounding"),
            .ftz = resolved_modifier<bool>(*fields, "ftz"),
            .saturate = resolved_modifier<bool>(*fields, "saturate"),
            .type = resolved_modifier<ScalarType>(*fields, "type"),
            .dst = resolved_operand<ResolvedRegisterRef>(*fields, "dst"),
            .src1 = resolved_operand<ResolvedRegisterRef>(*fields, "src1"),
            .src2 = resolved_operand<ResolvedRegisterRef>(*fields, "src2"),
        }};
  }
  if (fields->variant_name == "Bfloat") {
    return Add{
        .execution_predicate = std::move(fields->execution_predicate),
        .variant = Add::Bfloat{
            .operand_layout = fields->operand_layout,
            .rounding = resolved_modifier<RoundingMode>(*fields, "rounding"),
            .type = resolved_modifier<ScalarType>(*fields, "type"),
            .dst = resolved_operand<ResolvedRegisterRef>(*fields, "dst"),
            .src1 = resolved_operand<ResolvedRegisterRef>(*fields, "src1"),
            .src2 = resolved_operand<ResolvedRegisterRef>(*fields, "src2"),
        }};
  }
  if (fields->variant_name == "MixedF32") {
    return Add{
        .execution_predicate = std::move(fields->execution_predicate),
        .variant = Add::MixedF32{
            .operand_layout = fields->operand_layout,
            .rounding = resolved_modifier<RoundingMode>(*fields, "rounding"),
            .input_type = resolved_modifier<ScalarType>(*fields, "input_type"),
            .saturate = resolved_modifier<bool>(*fields, "saturate"),
            .dst = resolved_operand<ResolvedRegisterRef>(*fields, "dst"),
            .src = resolved_operand<ResolvedRegisterRef>(*fields, "src"),
            .addend = resolved_operand<ResolvedRegisterRef>(*fields, "addend"),
        }};
  }
  if (fields->variant_name == "IntegerNoSat") {
    return Add{.execution_predicate = std::move(fields->execution_predicate),
               .variant = Add::IntegerNoSat{
                   .operand_layout = fields->operand_layout,
                   .type = resolved_modifier<ScalarType>(*fields, "type"),
                   .dst = resolved_operand<ResolvedRegisterRef>(*fields, "dst"),
                   .src1 = resolved_operand<RegOrImm>(*fields, "src1"),
                   .src2 = resolved_operand<RegOrImm>(*fields, "src2"),
               }};
  }
  if (fields->variant_name == "Sat") {
    return Add{.execution_predicate = std::move(fields->execution_predicate),
               .variant = Add::Sat{
                   .operand_layout = fields->operand_layout,
                   .type = resolved_modifier<ScalarType>(*fields, "type"),
                   .dst = resolved_operand<ResolvedRegisterRef>(*fields, "dst"),
                   .src1 = resolved_operand<RegOrImm>(*fields, "src1"),
                   .src2 = resolved_operand<RegOrImm>(*fields, "src2"),
               }};
  }
  if (fields->variant_name == "PackedOptionalSat") {
    return Add{.execution_predicate = std::move(fields->execution_predicate),
               .variant = Add::PackedOptionalSat{
                   .operand_layout = fields->operand_layout,
                   .saturate = resolved_modifier<bool>(*fields, "saturate"),
                   .type = resolved_modifier<ScalarType>(*fields, "type"),
                   .dst = resolved_operand<ResolvedRegisterRef>(*fields, "dst"),
                   .src1 = resolved_operand<RegOrImm>(*fields, "src1"),
                   .src2 = resolved_operand<RegOrImm>(*fields, "src2"),
               }};
  }

  throw ResolveException("Unknown Add variant in resolved field table.");
}

template <>
std::expected<Sub, ResolveDiagnostic>
resolve<Sub>(const syntax_ast::AstInstruction &ast,
             const ResolveContext *context) {
  const auto selected_variant = selectVariant<Sub>(ast);
  if (!selected_variant)
    return std::unexpected(selected_variant.error());

  auto fields = resolve_fields(
      ast, Sub::get_syntax_descriptor(), Sub::get_resolved_descriptor(),
      magic_enum::enum_name(*selected_variant), context);
  if (!fields)
    return std::unexpected(fields.error());

  if (fields->variant_name == "FloatF32") {
    return Sub{
        .execution_predicate = std::move(fields->execution_predicate),
        .variant = Sub::FloatF32{
            .operand_layout = fields->operand_layout,
            .rounding = resolved_modifier<RoundingMode>(*fields, "rounding"),
            .ftz = resolved_modifier<bool>(*fields, "ftz"),
            .saturate = resolved_modifier<bool>(*fields, "saturate"),
            .dst = resolved_operand<ResolvedRegisterRef>(*fields, "dst"),
            .src1 = resolved_operand<RegOrImm>(*fields, "src1"),
            .src2 = resolved_operand<RegOrImm>(*fields, "src2"),
        }};
  }
  if (fields->variant_name == "FloatF32x2") {
    return Sub{
        .execution_predicate = std::move(fields->execution_predicate),
        .variant = Sub::FloatF32x2{
            .operand_layout = fields->operand_layout,
            .rounding = resolved_modifier<RoundingMode>(*fields, "rounding"),
            .ftz = resolved_modifier<bool>(*fields, "ftz"),
            .dst = resolved_operand<ResolvedRegisterRef>(*fields, "dst"),
            .src1 = resolved_operand<ResolvedRegisterRef>(*fields, "src1"),
            .src2 = resolved_operand<ResolvedRegisterRef>(*fields, "src2"),
        }};
  }
  if (fields->variant_name == "FloatF64") {
    return Sub{
        .execution_predicate = std::move(fields->execution_predicate),
        .variant = Sub::FloatF64{
            .operand_layout = fields->operand_layout,
            .rounding = resolved_modifier<RoundingMode>(*fields, "rounding"),
            .dst = resolved_operand<ResolvedRegisterRef>(*fields, "dst"),
            .src1 = resolved_operand<RegOrImm>(*fields, "src1"),
            .src2 = resolved_operand<RegOrImm>(*fields, "src2"),
        }};
  }
  if (fields->variant_name == "Half") {
    return Sub{
        .execution_predicate = std::move(fields->execution_predicate),
        .variant = Sub::Half{
            .operand_layout = fields->operand_layout,
            .rounding = resolved_modifier<RoundingMode>(*fields, "rounding"),
            .ftz = resolved_modifier<bool>(*fields, "ftz"),
            .saturate = resolved_modifier<bool>(*fields, "saturate"),
            .type = resolved_modifier<ScalarType>(*fields, "type"),
            .dst = resolved_operand<ResolvedRegisterRef>(*fields, "dst"),
            .src1 = resolved_operand<ResolvedRegisterRef>(*fields, "src1"),
            .src2 = resolved_operand<ResolvedRegisterRef>(*fields, "src2"),
        }};
  }
  if (fields->variant_name == "Bfloat") {
    return Sub{
        .execution_predicate = std::move(fields->execution_predicate),
        .variant = Sub::Bfloat{
            .operand_layout = fields->operand_layout,
            .rounding = resolved_modifier<RoundingMode>(*fields, "rounding"),
            .type = resolved_modifier<ScalarType>(*fields, "type"),
            .dst = resolved_operand<ResolvedRegisterRef>(*fields, "dst"),
            .src1 = resolved_operand<ResolvedRegisterRef>(*fields, "src1"),
            .src2 = resolved_operand<ResolvedRegisterRef>(*fields, "src2"),
        }};
  }
  if (fields->variant_name == "MixedF32") {
    return Sub{
        .execution_predicate = std::move(fields->execution_predicate),
        .variant = Sub::MixedF32{
            .operand_layout = fields->operand_layout,
            .rounding = resolved_modifier<RoundingMode>(*fields, "rounding"),
            .input_type = resolved_modifier<ScalarType>(*fields, "input_type"),
            .saturate = resolved_modifier<bool>(*fields, "saturate"),
            .dst = resolved_operand<ResolvedRegisterRef>(*fields, "dst"),
            .src = resolved_operand<ResolvedRegisterRef>(*fields, "src"),
            .subtrahend =
                resolved_operand<ResolvedRegisterRef>(*fields, "subtrahend"),
        }};
  }
  if (fields->variant_name == "IntegerNoSat") {
    return Sub{.execution_predicate = std::move(fields->execution_predicate),
               .variant = Sub::IntegerNoSat{
                   .operand_layout = fields->operand_layout,
                   .type = resolved_modifier<ScalarType>(*fields, "type"),
                   .dst = resolved_operand<ResolvedRegisterRef>(*fields, "dst"),
                   .src1 = resolved_operand<RegOrImm>(*fields, "src1"),
                   .src2 = resolved_operand<RegOrImm>(*fields, "src2"),
               }};
  }
  if (fields->variant_name == "OptionalSat") {
    return Sub{.execution_predicate = std::move(fields->execution_predicate),
               .variant = Sub::OptionalSat{
                   .operand_layout = fields->operand_layout,
                   .saturate = resolved_modifier<bool>(*fields, "saturate"),
                   .type = resolved_modifier<ScalarType>(*fields, "type"),
                   .dst = resolved_operand<ResolvedRegisterRef>(*fields, "dst"),
                   .src1 = resolved_operand<RegOrImm>(*fields, "src1"),
                   .src2 = resolved_operand<RegOrImm>(*fields, "src2"),
               }};
  }

  throw ResolveException("Unknown Sub variant in resolved field table.");
}

namespace checker {

template <>
CheckResult check<Add>(const Add &instruction, const Context &context) {
  const auto check_float_f32 =
      [&](const Add::FloatF32 &selected) -> CheckResult {
    const std::array<FieldView, 4> fields = {
        {FieldView{
             .field_id = "rounding",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.rounding.locs,
         },
         FieldView{
             .field_id = "ftz",
             .bool_value = selected.ftz.value,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.ftz.locs,
         },
         FieldView{
             .field_id = "saturate",
             .bool_value = selected.saturate.value,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.saturate.locs,
         },
         FieldView{
             .field_id = "type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = Add::FloatF32::type,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         }}};
    const std::array<ModifierValueView, 4> modifier_values = {
        {ModifierValueView{
             .kind_id = "rounding",
             .value_kind = checker::ModifierValueKind::RoundingMode,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = selected.rounding.value,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.rounding.locs,
         },
         ModifierValueView{
             .kind_id = "ftz",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = selected.ftz.value,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.ftz.locs,
         },
         ModifierValueView{
             .kind_id = "sat",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = selected.saturate.value,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.saturate.locs,
         },
         ModifierValueView{
             .kind_id = "type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = Add::FloatF32::type,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Add::get_checker_descriptor(), "FloatF32", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Add::get_checker_descriptor().variants[0].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const std::array<OperandView, 3> operands = {
        {OperandView{
             .field_id = "dst",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.dst.value.declared_type,
             .locations = selected.dst.locs,
         },
         [&]() -> OperandView {
           if (const auto *immediate =
                   std::get_if<ResolvedImmediate>(&selected.src1.value)) {
             return OperandView{
                 .field_id = "src1",
                 .actual_shape = check_end::OperandShape::Immediate,
                 .immediate_type = immediate->type,
                 .register_type = std::nullopt,
                 .locations = selected.src1.locs,
             };
           }
           const auto &register_ref =
               std::get<ResolvedRegisterRef>(selected.src1.value);
           return OperandView{
               .field_id = "src1",
               .actual_shape = check_end::OperandShape::Register,
               .immediate_type = std::nullopt,
               .register_type = register_ref.declared_type,
               .locations = selected.src1.locs,
           };
         }(),
         [&]() -> OperandView {
           if (const auto *immediate =
                   std::get_if<ResolvedImmediate>(&selected.src2.value)) {
             return OperandView{
                 .field_id = "src2",
                 .actual_shape = check_end::OperandShape::Immediate,
                 .immediate_type = immediate->type,
                 .register_type = std::nullopt,
                 .locations = selected.src2.locs,
             };
           }
           const auto &register_ref =
               std::get<ResolvedRegisterRef>(selected.src2.value);
           return OperandView{
               .field_id = "src2",
               .actual_shape = check_end::OperandShape::Register,
               .immediate_type = std::nullopt,
               .register_type = register_ref.declared_type,
               .locations = selected.src2.locs,
           };
         }()}};
    const auto &layouts =
        Add::get_resolved_descriptor().variants[0].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "FloatF32", selected.operand_layout.value, layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Add::get_checker_descriptor().variants[0],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto operand_check = check_operands(
          layouts[selected.operand_layout.value].bindings, fields, operands,
          Add::get_checker_descriptor()
              .variants[0]
              .operand_type_compatibilities,
          context);
      if (!operand_check) {
        diagnostics.insert(diagnostics.end(), operand_check.error().begin(),
                           operand_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(
      detail::VariantCheckFunction<decltype(check_float_f32), Add::FloatF32>);

  const auto check_float_f32x2 =
      [&](const Add::FloatF32x2 &selected) -> CheckResult {
    const std::array<FieldView, 3> fields = {
        {FieldView{
             .field_id = "rounding",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.rounding.locs,
         },
         FieldView{
             .field_id = "ftz",
             .bool_value = selected.ftz.value,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.ftz.locs,
         },
         FieldView{
             .field_id = "type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = Add::FloatF32x2::type,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         }}};
    const std::array<ModifierValueView, 3> modifier_values = {
        {ModifierValueView{
             .kind_id = "rounding",
             .value_kind = checker::ModifierValueKind::RoundingMode,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = selected.rounding.value,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.rounding.locs,
         },
         ModifierValueView{
             .kind_id = "ftz",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = selected.ftz.value,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.ftz.locs,
         },
         ModifierValueView{
             .kind_id = "type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = Add::FloatF32x2::type,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Add::get_checker_descriptor(), "FloatF32x2", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Add::get_checker_descriptor().variants[1].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const std::array<OperandView, 3> operands = {
        {OperandView{
             .field_id = "dst",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.dst.value.declared_type,
             .locations = selected.dst.locs,
         },
         OperandView{
             .field_id = "src1",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.src1.value.declared_type,
             .locations = selected.src1.locs,
         },
         OperandView{
             .field_id = "src2",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.src2.value.declared_type,
             .locations = selected.src2.locs,
         }}};
    const auto &layouts =
        Add::get_resolved_descriptor().variants[1].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "FloatF32x2", selected.operand_layout.value, layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Add::get_checker_descriptor().variants[1],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto operand_check = check_operands(
          layouts[selected.operand_layout.value].bindings, fields, operands,
          Add::get_checker_descriptor()
              .variants[1]
              .operand_type_compatibilities,
          context);
      if (!operand_check) {
        diagnostics.insert(diagnostics.end(), operand_check.error().begin(),
                           operand_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(detail::VariantCheckFunction<decltype(check_float_f32x2),
                                             Add::FloatF32x2>);

  const auto check_float_f64 =
      [&](const Add::FloatF64 &selected) -> CheckResult {
    const std::array<FieldView, 2> fields = {
        {FieldView{
             .field_id = "rounding",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.rounding.locs,
         },
         FieldView{
             .field_id = "type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = Add::FloatF64::type,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         }}};
    const std::array<ModifierValueView, 2> modifier_values = {
        {ModifierValueView{
             .kind_id = "rounding",
             .value_kind = checker::ModifierValueKind::RoundingMode,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = selected.rounding.value,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.rounding.locs,
         },
         ModifierValueView{
             .kind_id = "type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = Add::FloatF64::type,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Add::get_checker_descriptor(), "FloatF64", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Add::get_checker_descriptor().variants[2].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const std::array<OperandView, 3> operands = {
        {OperandView{
             .field_id = "dst",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.dst.value.declared_type,
             .locations = selected.dst.locs,
         },
         [&]() -> OperandView {
           if (const auto *immediate =
                   std::get_if<ResolvedImmediate>(&selected.src1.value)) {
             return OperandView{
                 .field_id = "src1",
                 .actual_shape = check_end::OperandShape::Immediate,
                 .immediate_type = immediate->type,
                 .register_type = std::nullopt,
                 .locations = selected.src1.locs,
             };
           }
           const auto &register_ref =
               std::get<ResolvedRegisterRef>(selected.src1.value);
           return OperandView{
               .field_id = "src1",
               .actual_shape = check_end::OperandShape::Register,
               .immediate_type = std::nullopt,
               .register_type = register_ref.declared_type,
               .locations = selected.src1.locs,
           };
         }(),
         [&]() -> OperandView {
           if (const auto *immediate =
                   std::get_if<ResolvedImmediate>(&selected.src2.value)) {
             return OperandView{
                 .field_id = "src2",
                 .actual_shape = check_end::OperandShape::Immediate,
                 .immediate_type = immediate->type,
                 .register_type = std::nullopt,
                 .locations = selected.src2.locs,
             };
           }
           const auto &register_ref =
               std::get<ResolvedRegisterRef>(selected.src2.value);
           return OperandView{
               .field_id = "src2",
               .actual_shape = check_end::OperandShape::Register,
               .immediate_type = std::nullopt,
               .register_type = register_ref.declared_type,
               .locations = selected.src2.locs,
           };
         }()}};
    const auto &layouts =
        Add::get_resolved_descriptor().variants[2].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "FloatF64", selected.operand_layout.value, layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Add::get_checker_descriptor().variants[2],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto operand_check = check_operands(
          layouts[selected.operand_layout.value].bindings, fields, operands,
          Add::get_checker_descriptor()
              .variants[2]
              .operand_type_compatibilities,
          context);
      if (!operand_check) {
        diagnostics.insert(diagnostics.end(), operand_check.error().begin(),
                           operand_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(
      detail::VariantCheckFunction<decltype(check_float_f64), Add::FloatF64>);

  const auto check_half = [&](const Add::Half &selected) -> CheckResult {
    const std::array<FieldView, 4> fields = {
        {FieldView{
             .field_id = "rounding",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.rounding.locs,
         },
         FieldView{
             .field_id = "ftz",
             .bool_value = selected.ftz.value,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.ftz.locs,
         },
         FieldView{
             .field_id = "saturate",
             .bool_value = selected.saturate.value,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.saturate.locs,
         },
         FieldView{
             .field_id = "type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = selected.type.value,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.type.locs,
         }}};
    const std::array<ModifierValueView, 4> modifier_values = {
        {ModifierValueView{
             .kind_id = "rounding",
             .value_kind = checker::ModifierValueKind::RoundingMode,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = selected.rounding.value,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.rounding.locs,
         },
         ModifierValueView{
             .kind_id = "ftz",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = selected.ftz.value,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.ftz.locs,
         },
         ModifierValueView{
             .kind_id = "sat",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = selected.saturate.value,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.saturate.locs,
         },
         ModifierValueView{
             .kind_id = "type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = selected.type.value,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.type.locs,
         }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Add::get_checker_descriptor(), "Half", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Add::get_checker_descriptor().variants[3].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const std::array<OperandView, 3> operands = {
        {OperandView{
             .field_id = "dst",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.dst.value.declared_type,
             .locations = selected.dst.locs,
         },
         OperandView{
             .field_id = "src1",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.src1.value.declared_type,
             .locations = selected.src1.locs,
         },
         OperandView{
             .field_id = "src2",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.src2.value.declared_type,
             .locations = selected.src2.locs,
         }}};
    const auto &layouts =
        Add::get_resolved_descriptor().variants[3].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "Half", selected.operand_layout.value, layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Add::get_checker_descriptor().variants[3],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto operand_check = check_operands(
          layouts[selected.operand_layout.value].bindings, fields, operands,
          Add::get_checker_descriptor()
              .variants[3]
              .operand_type_compatibilities,
          context);
      if (!operand_check) {
        diagnostics.insert(diagnostics.end(), operand_check.error().begin(),
                           operand_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(detail::VariantCheckFunction<decltype(check_half), Add::Half>);

  const auto check_bfloat = [&](const Add::Bfloat &selected) -> CheckResult {
    const std::array<FieldView, 2> fields = {
        {FieldView{
             .field_id = "rounding",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.rounding.locs,
         },
         FieldView{
             .field_id = "type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = selected.type.value,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.type.locs,
         }}};
    const std::array<ModifierValueView, 2> modifier_values = {
        {ModifierValueView{
             .kind_id = "rounding",
             .value_kind = checker::ModifierValueKind::RoundingMode,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = selected.rounding.value,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.rounding.locs,
         },
         ModifierValueView{
             .kind_id = "type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = selected.type.value,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.type.locs,
         }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Add::get_checker_descriptor(), "Bfloat", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Add::get_checker_descriptor().variants[4].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const std::array<OperandView, 3> operands = {
        {OperandView{
             .field_id = "dst",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.dst.value.declared_type,
             .locations = selected.dst.locs,
         },
         OperandView{
             .field_id = "src1",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.src1.value.declared_type,
             .locations = selected.src1.locs,
         },
         OperandView{
             .field_id = "src2",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.src2.value.declared_type,
             .locations = selected.src2.locs,
         }}};
    const auto &layouts =
        Add::get_resolved_descriptor().variants[4].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "Bfloat", selected.operand_layout.value, layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Add::get_checker_descriptor().variants[4],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto operand_check = check_operands(
          layouts[selected.operand_layout.value].bindings, fields, operands,
          Add::get_checker_descriptor()
              .variants[4]
              .operand_type_compatibilities,
          context);
      if (!operand_check) {
        diagnostics.insert(diagnostics.end(), operand_check.error().begin(),
                           operand_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(
      detail::VariantCheckFunction<decltype(check_bfloat), Add::Bfloat>);

  const auto check_mixed_f32 =
      [&](const Add::MixedF32 &selected) -> CheckResult {
    const std::array<FieldView, 4> fields = {
        {FieldView{
             .field_id = "rounding",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.rounding.locs,
         },
         FieldView{
             .field_id = "result_type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = Add::MixedF32::result_type,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         },
         FieldView{
             .field_id = "input_type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = selected.input_type.value,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.input_type.locs,
         },
         FieldView{
             .field_id = "saturate",
             .bool_value = selected.saturate.value,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.saturate.locs,
         }}};
    const std::array<ModifierValueView, 4> modifier_values = {
        {ModifierValueView{
             .kind_id = "rounding",
             .value_kind = checker::ModifierValueKind::RoundingMode,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = selected.rounding.value,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.rounding.locs,
         },
         ModifierValueView{
             .kind_id = "result_type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = Add::MixedF32::result_type,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         },
         ModifierValueView{
             .kind_id = "input_type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = selected.input_type.value,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.input_type.locs,
         },
         ModifierValueView{
             .kind_id = "sat",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = selected.saturate.value,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.saturate.locs,
         }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Add::get_checker_descriptor(), "MixedF32", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Add::get_checker_descriptor().variants[5].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const std::array<OperandView, 3> operands = {
        {OperandView{
             .field_id = "dst",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.dst.value.declared_type,
             .locations = selected.dst.locs,
         },
         OperandView{
             .field_id = "src",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.src.value.declared_type,
             .locations = selected.src.locs,
         },
         OperandView{
             .field_id = "addend",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.addend.value.declared_type,
             .locations = selected.addend.locs,
         }}};
    const auto &layouts =
        Add::get_resolved_descriptor().variants[5].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "MixedF32", selected.operand_layout.value, layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Add::get_checker_descriptor().variants[5],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto operand_check = check_operands(
          layouts[selected.operand_layout.value].bindings, fields, operands,
          Add::get_checker_descriptor()
              .variants[5]
              .operand_type_compatibilities,
          context);
      if (!operand_check) {
        diagnostics.insert(diagnostics.end(), operand_check.error().begin(),
                           operand_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(
      detail::VariantCheckFunction<decltype(check_mixed_f32), Add::MixedF32>);

  const auto check_integer_no_sat =
      [&](const Add::IntegerNoSat &selected) -> CheckResult {
    const std::array<FieldView, 1> fields = {{FieldView{
        .field_id = "type",
        .bool_value = std::nullopt,
        .cache_operator = std::nullopt,
        .scalar_type = selected.type.value,
        .vector_arity = std::nullopt,
        .memory_state_space = std::nullopt,
        .memory_consistency = std::nullopt,
        .memory_scope = std::nullopt,
        .locations = selected.type.locs,
    }}};
    const std::array<ModifierValueView, 1> modifier_values = {
        {ModifierValueView{
            .kind_id = "type",
            .value_kind = checker::ModifierValueKind::ScalarType,
            .bool_value = false,
            .scalar_type = selected.type.value,
            .rounding_mode = RoundingMode::Invalid,
            .cache_operator = CacheOperator::Unspecified,
            .vector_arity = VectorArity::Invalid,
            .memory_state_space = MemoryStateSpace::Invalid,
            .memory_consistency = MemoryConsistency::Omitted,
            .memory_scope = MemoryScope::None,
            .is_present = true,
            .locations = selected.type.locs,
        }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Add::get_checker_descriptor(), "IntegerNoSat", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Add::get_checker_descriptor().variants[6].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const std::array<OperandView, 3> operands = {
        {OperandView{
             .field_id = "dst",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.dst.value.declared_type,
             .locations = selected.dst.locs,
         },
         [&]() -> OperandView {
           if (const auto *immediate =
                   std::get_if<ResolvedImmediate>(&selected.src1.value)) {
             return OperandView{
                 .field_id = "src1",
                 .actual_shape = check_end::OperandShape::Immediate,
                 .immediate_type = immediate->type,
                 .register_type = std::nullopt,
                 .locations = selected.src1.locs,
             };
           }
           const auto &register_ref =
               std::get<ResolvedRegisterRef>(selected.src1.value);
           return OperandView{
               .field_id = "src1",
               .actual_shape = check_end::OperandShape::Register,
               .immediate_type = std::nullopt,
               .register_type = register_ref.declared_type,
               .locations = selected.src1.locs,
           };
         }(),
         [&]() -> OperandView {
           if (const auto *immediate =
                   std::get_if<ResolvedImmediate>(&selected.src2.value)) {
             return OperandView{
                 .field_id = "src2",
                 .actual_shape = check_end::OperandShape::Immediate,
                 .immediate_type = immediate->type,
                 .register_type = std::nullopt,
                 .locations = selected.src2.locs,
             };
           }
           const auto &register_ref =
               std::get<ResolvedRegisterRef>(selected.src2.value);
           return OperandView{
               .field_id = "src2",
               .actual_shape = check_end::OperandShape::Register,
               .immediate_type = std::nullopt,
               .register_type = register_ref.declared_type,
               .locations = selected.src2.locs,
           };
         }()}};
    const auto &layouts =
        Add::get_resolved_descriptor().variants[6].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "IntegerNoSat", selected.operand_layout.value, layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Add::get_checker_descriptor().variants[6],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto operand_check = check_operands(
          layouts[selected.operand_layout.value].bindings, fields, operands,
          Add::get_checker_descriptor()
              .variants[6]
              .operand_type_compatibilities,
          context);
      if (!operand_check) {
        diagnostics.insert(diagnostics.end(), operand_check.error().begin(),
                           operand_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(detail::VariantCheckFunction<decltype(check_integer_no_sat),
                                             Add::IntegerNoSat>);

  const auto check_sat = [&](const Add::Sat &selected) -> CheckResult {
    const std::array<FieldView, 2> fields = {
        {FieldView{
             .field_id = "saturate",
             .bool_value = Add::Sat::saturate,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         },
         FieldView{
             .field_id = "type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = selected.type.value,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.type.locs,
         }}};
    const std::array<ModifierValueView, 2> modifier_values = {
        {ModifierValueView{
             .kind_id = "sat",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = Add::Sat::saturate,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         },
         ModifierValueView{
             .kind_id = "type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = selected.type.value,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.type.locs,
         }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Add::get_checker_descriptor(), "Sat", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Add::get_checker_descriptor().variants[7].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const std::array<OperandView, 3> operands = {
        {OperandView{
             .field_id = "dst",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.dst.value.declared_type,
             .locations = selected.dst.locs,
         },
         [&]() -> OperandView {
           if (const auto *immediate =
                   std::get_if<ResolvedImmediate>(&selected.src1.value)) {
             return OperandView{
                 .field_id = "src1",
                 .actual_shape = check_end::OperandShape::Immediate,
                 .immediate_type = immediate->type,
                 .register_type = std::nullopt,
                 .locations = selected.src1.locs,
             };
           }
           const auto &register_ref =
               std::get<ResolvedRegisterRef>(selected.src1.value);
           return OperandView{
               .field_id = "src1",
               .actual_shape = check_end::OperandShape::Register,
               .immediate_type = std::nullopt,
               .register_type = register_ref.declared_type,
               .locations = selected.src1.locs,
           };
         }(),
         [&]() -> OperandView {
           if (const auto *immediate =
                   std::get_if<ResolvedImmediate>(&selected.src2.value)) {
             return OperandView{
                 .field_id = "src2",
                 .actual_shape = check_end::OperandShape::Immediate,
                 .immediate_type = immediate->type,
                 .register_type = std::nullopt,
                 .locations = selected.src2.locs,
             };
           }
           const auto &register_ref =
               std::get<ResolvedRegisterRef>(selected.src2.value);
           return OperandView{
               .field_id = "src2",
               .actual_shape = check_end::OperandShape::Register,
               .immediate_type = std::nullopt,
               .register_type = register_ref.declared_type,
               .locations = selected.src2.locs,
           };
         }()}};
    const auto &layouts =
        Add::get_resolved_descriptor().variants[7].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "Sat", selected.operand_layout.value, layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Add::get_checker_descriptor().variants[7],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto operand_check = check_operands(
          layouts[selected.operand_layout.value].bindings, fields, operands,
          Add::get_checker_descriptor()
              .variants[7]
              .operand_type_compatibilities,
          context);
      if (!operand_check) {
        diagnostics.insert(diagnostics.end(), operand_check.error().begin(),
                           operand_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(detail::VariantCheckFunction<decltype(check_sat), Add::Sat>);

  const auto check_packed_optional_sat =
      [&](const Add::PackedOptionalSat &selected) -> CheckResult {
    const std::array<FieldView, 2> fields = {
        {FieldView{
             .field_id = "saturate",
             .bool_value = selected.saturate.value,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.saturate.locs,
         },
         FieldView{
             .field_id = "type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = selected.type.value,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.type.locs,
         }}};
    const std::array<ModifierValueView, 2> modifier_values = {
        {ModifierValueView{
             .kind_id = "sat",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = selected.saturate.value,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.saturate.locs,
         },
         ModifierValueView{
             .kind_id = "type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = selected.type.value,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.type.locs,
         }}};
    CheckDiagnostics diagnostics;
    const auto common = check_common(Add::get_checker_descriptor(),
                                     "PackedOptionalSat", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Add::get_checker_descriptor().variants[8].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const std::array<OperandView, 3> operands = {
        {OperandView{
             .field_id = "dst",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.dst.value.declared_type,
             .locations = selected.dst.locs,
         },
         [&]() -> OperandView {
           if (const auto *immediate =
                   std::get_if<ResolvedImmediate>(&selected.src1.value)) {
             return OperandView{
                 .field_id = "src1",
                 .actual_shape = check_end::OperandShape::Immediate,
                 .immediate_type = immediate->type,
                 .register_type = std::nullopt,
                 .locations = selected.src1.locs,
             };
           }
           const auto &register_ref =
               std::get<ResolvedRegisterRef>(selected.src1.value);
           return OperandView{
               .field_id = "src1",
               .actual_shape = check_end::OperandShape::Register,
               .immediate_type = std::nullopt,
               .register_type = register_ref.declared_type,
               .locations = selected.src1.locs,
           };
         }(),
         [&]() -> OperandView {
           if (const auto *immediate =
                   std::get_if<ResolvedImmediate>(&selected.src2.value)) {
             return OperandView{
                 .field_id = "src2",
                 .actual_shape = check_end::OperandShape::Immediate,
                 .immediate_type = immediate->type,
                 .register_type = std::nullopt,
                 .locations = selected.src2.locs,
             };
           }
           const auto &register_ref =
               std::get<ResolvedRegisterRef>(selected.src2.value);
           return OperandView{
               .field_id = "src2",
               .actual_shape = check_end::OperandShape::Register,
               .immediate_type = std::nullopt,
               .register_type = register_ref.declared_type,
               .locations = selected.src2.locs,
           };
         }()}};
    const auto &layouts =
        Add::get_resolved_descriptor().variants[8].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "PackedOptionalSat", selected.operand_layout.value, layouts.size(),
        context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Add::get_checker_descriptor().variants[8],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto operand_check = check_operands(
          layouts[selected.operand_layout.value].bindings, fields, operands,
          Add::get_checker_descriptor()
              .variants[8]
              .operand_type_compatibilities,
          context);
      if (!operand_check) {
        diagnostics.insert(diagnostics.end(), operand_check.error().begin(),
                           operand_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(
      detail::VariantCheckFunction<decltype(check_packed_optional_sat),
                                   Add::PackedOptionalSat>);

  return std::visit(detail::Overloaded{check_float_f32, check_float_f32x2,
                                       check_float_f64, check_half,
                                       check_bfloat, check_mixed_f32,
                                       check_integer_no_sat, check_sat,
                                       check_packed_optional_sat},
                    instruction.variant);
}

template <>
CheckResult check<Sub>(const Sub &instruction, const Context &context) {
  const auto check_float_f32 =
      [&](const Sub::FloatF32 &selected) -> CheckResult {
    const std::array<FieldView, 4> fields = {
        {FieldView{
             .field_id = "rounding",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.rounding.locs,
         },
         FieldView{
             .field_id = "ftz",
             .bool_value = selected.ftz.value,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.ftz.locs,
         },
         FieldView{
             .field_id = "saturate",
             .bool_value = selected.saturate.value,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.saturate.locs,
         },
         FieldView{
             .field_id = "type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = Sub::FloatF32::type,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         }}};
    const std::array<ModifierValueView, 4> modifier_values = {
        {ModifierValueView{
             .kind_id = "rounding",
             .value_kind = checker::ModifierValueKind::RoundingMode,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = selected.rounding.value,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.rounding.locs,
         },
         ModifierValueView{
             .kind_id = "ftz",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = selected.ftz.value,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.ftz.locs,
         },
         ModifierValueView{
             .kind_id = "sat",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = selected.saturate.value,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.saturate.locs,
         },
         ModifierValueView{
             .kind_id = "type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = Sub::FloatF32::type,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Sub::get_checker_descriptor(), "FloatF32", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Sub::get_checker_descriptor().variants[0].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const std::array<OperandView, 3> operands = {
        {OperandView{
             .field_id = "dst",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.dst.value.declared_type,
             .locations = selected.dst.locs,
         },
         [&]() -> OperandView {
           if (const auto *immediate =
                   std::get_if<ResolvedImmediate>(&selected.src1.value)) {
             return OperandView{
                 .field_id = "src1",
                 .actual_shape = check_end::OperandShape::Immediate,
                 .immediate_type = immediate->type,
                 .register_type = std::nullopt,
                 .locations = selected.src1.locs,
             };
           }
           const auto &register_ref =
               std::get<ResolvedRegisterRef>(selected.src1.value);
           return OperandView{
               .field_id = "src1",
               .actual_shape = check_end::OperandShape::Register,
               .immediate_type = std::nullopt,
               .register_type = register_ref.declared_type,
               .locations = selected.src1.locs,
           };
         }(),
         [&]() -> OperandView {
           if (const auto *immediate =
                   std::get_if<ResolvedImmediate>(&selected.src2.value)) {
             return OperandView{
                 .field_id = "src2",
                 .actual_shape = check_end::OperandShape::Immediate,
                 .immediate_type = immediate->type,
                 .register_type = std::nullopt,
                 .locations = selected.src2.locs,
             };
           }
           const auto &register_ref =
               std::get<ResolvedRegisterRef>(selected.src2.value);
           return OperandView{
               .field_id = "src2",
               .actual_shape = check_end::OperandShape::Register,
               .immediate_type = std::nullopt,
               .register_type = register_ref.declared_type,
               .locations = selected.src2.locs,
           };
         }()}};
    const auto &layouts =
        Sub::get_resolved_descriptor().variants[0].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "FloatF32", selected.operand_layout.value, layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Sub::get_checker_descriptor().variants[0],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto operand_check = check_operands(
          layouts[selected.operand_layout.value].bindings, fields, operands,
          Sub::get_checker_descriptor()
              .variants[0]
              .operand_type_compatibilities,
          context);
      if (!operand_check) {
        diagnostics.insert(diagnostics.end(), operand_check.error().begin(),
                           operand_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(
      detail::VariantCheckFunction<decltype(check_float_f32), Sub::FloatF32>);

  const auto check_float_f32x2 =
      [&](const Sub::FloatF32x2 &selected) -> CheckResult {
    const std::array<FieldView, 3> fields = {
        {FieldView{
             .field_id = "rounding",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.rounding.locs,
         },
         FieldView{
             .field_id = "ftz",
             .bool_value = selected.ftz.value,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.ftz.locs,
         },
         FieldView{
             .field_id = "type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = Sub::FloatF32x2::type,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         }}};
    const std::array<ModifierValueView, 3> modifier_values = {
        {ModifierValueView{
             .kind_id = "rounding",
             .value_kind = checker::ModifierValueKind::RoundingMode,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = selected.rounding.value,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.rounding.locs,
         },
         ModifierValueView{
             .kind_id = "ftz",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = selected.ftz.value,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.ftz.locs,
         },
         ModifierValueView{
             .kind_id = "type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = Sub::FloatF32x2::type,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Sub::get_checker_descriptor(), "FloatF32x2", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Sub::get_checker_descriptor().variants[1].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const std::array<OperandView, 3> operands = {
        {OperandView{
             .field_id = "dst",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.dst.value.declared_type,
             .locations = selected.dst.locs,
         },
         OperandView{
             .field_id = "src1",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.src1.value.declared_type,
             .locations = selected.src1.locs,
         },
         OperandView{
             .field_id = "src2",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.src2.value.declared_type,
             .locations = selected.src2.locs,
         }}};
    const auto &layouts =
        Sub::get_resolved_descriptor().variants[1].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "FloatF32x2", selected.operand_layout.value, layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Sub::get_checker_descriptor().variants[1],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto operand_check = check_operands(
          layouts[selected.operand_layout.value].bindings, fields, operands,
          Sub::get_checker_descriptor()
              .variants[1]
              .operand_type_compatibilities,
          context);
      if (!operand_check) {
        diagnostics.insert(diagnostics.end(), operand_check.error().begin(),
                           operand_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(detail::VariantCheckFunction<decltype(check_float_f32x2),
                                             Sub::FloatF32x2>);

  const auto check_float_f64 =
      [&](const Sub::FloatF64 &selected) -> CheckResult {
    const std::array<FieldView, 2> fields = {
        {FieldView{
             .field_id = "rounding",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.rounding.locs,
         },
         FieldView{
             .field_id = "type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = Sub::FloatF64::type,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         }}};
    const std::array<ModifierValueView, 2> modifier_values = {
        {ModifierValueView{
             .kind_id = "rounding",
             .value_kind = checker::ModifierValueKind::RoundingMode,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = selected.rounding.value,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.rounding.locs,
         },
         ModifierValueView{
             .kind_id = "type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = Sub::FloatF64::type,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Sub::get_checker_descriptor(), "FloatF64", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Sub::get_checker_descriptor().variants[2].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const std::array<OperandView, 3> operands = {
        {OperandView{
             .field_id = "dst",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.dst.value.declared_type,
             .locations = selected.dst.locs,
         },
         [&]() -> OperandView {
           if (const auto *immediate =
                   std::get_if<ResolvedImmediate>(&selected.src1.value)) {
             return OperandView{
                 .field_id = "src1",
                 .actual_shape = check_end::OperandShape::Immediate,
                 .immediate_type = immediate->type,
                 .register_type = std::nullopt,
                 .locations = selected.src1.locs,
             };
           }
           const auto &register_ref =
               std::get<ResolvedRegisterRef>(selected.src1.value);
           return OperandView{
               .field_id = "src1",
               .actual_shape = check_end::OperandShape::Register,
               .immediate_type = std::nullopt,
               .register_type = register_ref.declared_type,
               .locations = selected.src1.locs,
           };
         }(),
         [&]() -> OperandView {
           if (const auto *immediate =
                   std::get_if<ResolvedImmediate>(&selected.src2.value)) {
             return OperandView{
                 .field_id = "src2",
                 .actual_shape = check_end::OperandShape::Immediate,
                 .immediate_type = immediate->type,
                 .register_type = std::nullopt,
                 .locations = selected.src2.locs,
             };
           }
           const auto &register_ref =
               std::get<ResolvedRegisterRef>(selected.src2.value);
           return OperandView{
               .field_id = "src2",
               .actual_shape = check_end::OperandShape::Register,
               .immediate_type = std::nullopt,
               .register_type = register_ref.declared_type,
               .locations = selected.src2.locs,
           };
         }()}};
    const auto &layouts =
        Sub::get_resolved_descriptor().variants[2].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "FloatF64", selected.operand_layout.value, layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Sub::get_checker_descriptor().variants[2],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto operand_check = check_operands(
          layouts[selected.operand_layout.value].bindings, fields, operands,
          Sub::get_checker_descriptor()
              .variants[2]
              .operand_type_compatibilities,
          context);
      if (!operand_check) {
        diagnostics.insert(diagnostics.end(), operand_check.error().begin(),
                           operand_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(
      detail::VariantCheckFunction<decltype(check_float_f64), Sub::FloatF64>);

  const auto check_half = [&](const Sub::Half &selected) -> CheckResult {
    const std::array<FieldView, 4> fields = {
        {FieldView{
             .field_id = "rounding",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.rounding.locs,
         },
         FieldView{
             .field_id = "ftz",
             .bool_value = selected.ftz.value,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.ftz.locs,
         },
         FieldView{
             .field_id = "saturate",
             .bool_value = selected.saturate.value,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.saturate.locs,
         },
         FieldView{
             .field_id = "type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = selected.type.value,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.type.locs,
         }}};
    const std::array<ModifierValueView, 4> modifier_values = {
        {ModifierValueView{
             .kind_id = "rounding",
             .value_kind = checker::ModifierValueKind::RoundingMode,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = selected.rounding.value,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.rounding.locs,
         },
         ModifierValueView{
             .kind_id = "ftz",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = selected.ftz.value,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.ftz.locs,
         },
         ModifierValueView{
             .kind_id = "sat",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = selected.saturate.value,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.saturate.locs,
         },
         ModifierValueView{
             .kind_id = "type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = selected.type.value,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.type.locs,
         }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Sub::get_checker_descriptor(), "Half", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Sub::get_checker_descriptor().variants[3].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const std::array<OperandView, 3> operands = {
        {OperandView{
             .field_id = "dst",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.dst.value.declared_type,
             .locations = selected.dst.locs,
         },
         OperandView{
             .field_id = "src1",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.src1.value.declared_type,
             .locations = selected.src1.locs,
         },
         OperandView{
             .field_id = "src2",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.src2.value.declared_type,
             .locations = selected.src2.locs,
         }}};
    const auto &layouts =
        Sub::get_resolved_descriptor().variants[3].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "Half", selected.operand_layout.value, layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Sub::get_checker_descriptor().variants[3],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto operand_check = check_operands(
          layouts[selected.operand_layout.value].bindings, fields, operands,
          Sub::get_checker_descriptor()
              .variants[3]
              .operand_type_compatibilities,
          context);
      if (!operand_check) {
        diagnostics.insert(diagnostics.end(), operand_check.error().begin(),
                           operand_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(detail::VariantCheckFunction<decltype(check_half), Sub::Half>);

  const auto check_bfloat = [&](const Sub::Bfloat &selected) -> CheckResult {
    const std::array<FieldView, 2> fields = {
        {FieldView{
             .field_id = "rounding",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.rounding.locs,
         },
         FieldView{
             .field_id = "type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = selected.type.value,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.type.locs,
         }}};
    const std::array<ModifierValueView, 2> modifier_values = {
        {ModifierValueView{
             .kind_id = "rounding",
             .value_kind = checker::ModifierValueKind::RoundingMode,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = selected.rounding.value,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.rounding.locs,
         },
         ModifierValueView{
             .kind_id = "type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = selected.type.value,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.type.locs,
         }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Sub::get_checker_descriptor(), "Bfloat", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Sub::get_checker_descriptor().variants[4].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const std::array<OperandView, 3> operands = {
        {OperandView{
             .field_id = "dst",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.dst.value.declared_type,
             .locations = selected.dst.locs,
         },
         OperandView{
             .field_id = "src1",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.src1.value.declared_type,
             .locations = selected.src1.locs,
         },
         OperandView{
             .field_id = "src2",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.src2.value.declared_type,
             .locations = selected.src2.locs,
         }}};
    const auto &layouts =
        Sub::get_resolved_descriptor().variants[4].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "Bfloat", selected.operand_layout.value, layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Sub::get_checker_descriptor().variants[4],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto operand_check = check_operands(
          layouts[selected.operand_layout.value].bindings, fields, operands,
          Sub::get_checker_descriptor()
              .variants[4]
              .operand_type_compatibilities,
          context);
      if (!operand_check) {
        diagnostics.insert(diagnostics.end(), operand_check.error().begin(),
                           operand_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(
      detail::VariantCheckFunction<decltype(check_bfloat), Sub::Bfloat>);

  const auto check_mixed_f32 =
      [&](const Sub::MixedF32 &selected) -> CheckResult {
    const std::array<FieldView, 4> fields = {
        {FieldView{
             .field_id = "rounding",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.rounding.locs,
         },
         FieldView{
             .field_id = "result_type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = Sub::MixedF32::result_type,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = std::span<const SourceRange>{},
         },
         FieldView{
             .field_id = "input_type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = selected.input_type.value,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.input_type.locs,
         },
         FieldView{
             .field_id = "saturate",
             .bool_value = selected.saturate.value,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.saturate.locs,
         }}};
    const std::array<ModifierValueView, 4> modifier_values = {
        {ModifierValueView{
             .kind_id = "rounding",
             .value_kind = checker::ModifierValueKind::RoundingMode,
             .bool_value = false,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = selected.rounding.value,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.rounding.locs,
         },
         ModifierValueView{
             .kind_id = "result_type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = Sub::MixedF32::result_type,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = std::span<const SourceRange>{},
         },
         ModifierValueView{
             .kind_id = "input_type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = selected.input_type.value,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.input_type.locs,
         },
         ModifierValueView{
             .kind_id = "sat",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = selected.saturate.value,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.saturate.locs,
         }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Sub::get_checker_descriptor(), "MixedF32", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Sub::get_checker_descriptor().variants[5].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const std::array<OperandView, 3> operands = {
        {OperandView{
             .field_id = "dst",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.dst.value.declared_type,
             .locations = selected.dst.locs,
         },
         OperandView{
             .field_id = "src",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.src.value.declared_type,
             .locations = selected.src.locs,
         },
         OperandView{
             .field_id = "subtrahend",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.subtrahend.value.declared_type,
             .locations = selected.subtrahend.locs,
         }}};
    const auto &layouts =
        Sub::get_resolved_descriptor().variants[5].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "MixedF32", selected.operand_layout.value, layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Sub::get_checker_descriptor().variants[5],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto operand_check = check_operands(
          layouts[selected.operand_layout.value].bindings, fields, operands,
          Sub::get_checker_descriptor()
              .variants[5]
              .operand_type_compatibilities,
          context);
      if (!operand_check) {
        diagnostics.insert(diagnostics.end(), operand_check.error().begin(),
                           operand_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(
      detail::VariantCheckFunction<decltype(check_mixed_f32), Sub::MixedF32>);

  const auto check_integer_no_sat =
      [&](const Sub::IntegerNoSat &selected) -> CheckResult {
    const std::array<FieldView, 1> fields = {{FieldView{
        .field_id = "type",
        .bool_value = std::nullopt,
        .cache_operator = std::nullopt,
        .scalar_type = selected.type.value,
        .vector_arity = std::nullopt,
        .memory_state_space = std::nullopt,
        .memory_consistency = std::nullopt,
        .memory_scope = std::nullopt,
        .locations = selected.type.locs,
    }}};
    const std::array<ModifierValueView, 1> modifier_values = {
        {ModifierValueView{
            .kind_id = "type",
            .value_kind = checker::ModifierValueKind::ScalarType,
            .bool_value = false,
            .scalar_type = selected.type.value,
            .rounding_mode = RoundingMode::Invalid,
            .cache_operator = CacheOperator::Unspecified,
            .vector_arity = VectorArity::Invalid,
            .memory_state_space = MemoryStateSpace::Invalid,
            .memory_consistency = MemoryConsistency::Omitted,
            .memory_scope = MemoryScope::None,
            .is_present = true,
            .locations = selected.type.locs,
        }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Sub::get_checker_descriptor(), "IntegerNoSat", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Sub::get_checker_descriptor().variants[6].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const std::array<OperandView, 3> operands = {
        {OperandView{
             .field_id = "dst",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.dst.value.declared_type,
             .locations = selected.dst.locs,
         },
         [&]() -> OperandView {
           if (const auto *immediate =
                   std::get_if<ResolvedImmediate>(&selected.src1.value)) {
             return OperandView{
                 .field_id = "src1",
                 .actual_shape = check_end::OperandShape::Immediate,
                 .immediate_type = immediate->type,
                 .register_type = std::nullopt,
                 .locations = selected.src1.locs,
             };
           }
           const auto &register_ref =
               std::get<ResolvedRegisterRef>(selected.src1.value);
           return OperandView{
               .field_id = "src1",
               .actual_shape = check_end::OperandShape::Register,
               .immediate_type = std::nullopt,
               .register_type = register_ref.declared_type,
               .locations = selected.src1.locs,
           };
         }(),
         [&]() -> OperandView {
           if (const auto *immediate =
                   std::get_if<ResolvedImmediate>(&selected.src2.value)) {
             return OperandView{
                 .field_id = "src2",
                 .actual_shape = check_end::OperandShape::Immediate,
                 .immediate_type = immediate->type,
                 .register_type = std::nullopt,
                 .locations = selected.src2.locs,
             };
           }
           const auto &register_ref =
               std::get<ResolvedRegisterRef>(selected.src2.value);
           return OperandView{
               .field_id = "src2",
               .actual_shape = check_end::OperandShape::Register,
               .immediate_type = std::nullopt,
               .register_type = register_ref.declared_type,
               .locations = selected.src2.locs,
           };
         }()}};
    const auto &layouts =
        Sub::get_resolved_descriptor().variants[6].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "IntegerNoSat", selected.operand_layout.value, layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Sub::get_checker_descriptor().variants[6],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto operand_check = check_operands(
          layouts[selected.operand_layout.value].bindings, fields, operands,
          Sub::get_checker_descriptor()
              .variants[6]
              .operand_type_compatibilities,
          context);
      if (!operand_check) {
        diagnostics.insert(diagnostics.end(), operand_check.error().begin(),
                           operand_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(detail::VariantCheckFunction<decltype(check_integer_no_sat),
                                             Sub::IntegerNoSat>);

  const auto check_optional_sat =
      [&](const Sub::OptionalSat &selected) -> CheckResult {
    const std::array<FieldView, 2> fields = {
        {FieldView{
             .field_id = "saturate",
             .bool_value = selected.saturate.value,
             .cache_operator = std::nullopt,
             .scalar_type = std::nullopt,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.saturate.locs,
         },
         FieldView{
             .field_id = "type",
             .bool_value = std::nullopt,
             .cache_operator = std::nullopt,
             .scalar_type = selected.type.value,
             .vector_arity = std::nullopt,
             .memory_state_space = std::nullopt,
             .memory_consistency = std::nullopt,
             .memory_scope = std::nullopt,
             .locations = selected.type.locs,
         }}};
    const std::array<ModifierValueView, 2> modifier_values = {
        {ModifierValueView{
             .kind_id = "sat",
             .value_kind = checker::ModifierValueKind::Bool,
             .bool_value = selected.saturate.value,
             .scalar_type = ScalarType::Invalid,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.saturate.locs,
         },
         ModifierValueView{
             .kind_id = "type",
             .value_kind = checker::ModifierValueKind::ScalarType,
             .bool_value = false,
             .scalar_type = selected.type.value,
             .rounding_mode = RoundingMode::Invalid,
             .cache_operator = CacheOperator::Unspecified,
             .vector_arity = VectorArity::Invalid,
             .memory_state_space = MemoryStateSpace::Invalid,
             .memory_consistency = MemoryConsistency::Omitted,
             .memory_scope = MemoryScope::None,
             .is_present = true,
             .locations = selected.type.locs,
         }}};
    CheckDiagnostics diagnostics;
    const auto common =
        check_common(Sub::get_checker_descriptor(), "OptionalSat", context);
    if (!common) {
      diagnostics.insert(diagnostics.end(), common.error().begin(),
                         common.error().end());
    }
    const auto modifier_availability = check_modifier_value_availability(
        Sub::get_checker_descriptor().variants[7].modifier_value_availabilities,
        modifier_values, context);
    if (!modifier_availability) {
      diagnostics.insert(diagnostics.end(),
                         modifier_availability.error().begin(),
                         modifier_availability.error().end());
    }
    const std::array<OperandView, 3> operands = {
        {OperandView{
             .field_id = "dst",
             .actual_shape = check_end::OperandShape::Register,
             .immediate_type = std::nullopt,
             .register_type = selected.dst.value.declared_type,
             .locations = selected.dst.locs,
         },
         [&]() -> OperandView {
           if (const auto *immediate =
                   std::get_if<ResolvedImmediate>(&selected.src1.value)) {
             return OperandView{
                 .field_id = "src1",
                 .actual_shape = check_end::OperandShape::Immediate,
                 .immediate_type = immediate->type,
                 .register_type = std::nullopt,
                 .locations = selected.src1.locs,
             };
           }
           const auto &register_ref =
               std::get<ResolvedRegisterRef>(selected.src1.value);
           return OperandView{
               .field_id = "src1",
               .actual_shape = check_end::OperandShape::Register,
               .immediate_type = std::nullopt,
               .register_type = register_ref.declared_type,
               .locations = selected.src1.locs,
           };
         }(),
         [&]() -> OperandView {
           if (const auto *immediate =
                   std::get_if<ResolvedImmediate>(&selected.src2.value)) {
             return OperandView{
                 .field_id = "src2",
                 .actual_shape = check_end::OperandShape::Immediate,
                 .immediate_type = immediate->type,
                 .register_type = std::nullopt,
                 .locations = selected.src2.locs,
             };
           }
           const auto &register_ref =
               std::get<ResolvedRegisterRef>(selected.src2.value);
           return OperandView{
               .field_id = "src2",
               .actual_shape = check_end::OperandShape::Register,
               .immediate_type = std::nullopt,
               .register_type = register_ref.declared_type,
               .locations = selected.src2.locs,
           };
         }()}};
    const auto &layouts =
        Sub::get_resolved_descriptor().variants[7].operand_layouts;
    const auto layout_check = check_operand_layout_tag(
        "OptionalSat", selected.operand_layout.value, layouts.size(), context);
    if (!layout_check) {
      diagnostics.insert(diagnostics.end(), layout_check.error().begin(),
                         layout_check.error().end());
    } else {
      const auto availability_check = check_operand_layout_availability(
          Sub::get_checker_descriptor().variants[7],
          selected.operand_layout.value, context);
      if (!availability_check) {
        diagnostics.insert(diagnostics.end(),
                           availability_check.error().begin(),
                           availability_check.error().end());
      }
      const auto operand_check = check_operands(
          layouts[selected.operand_layout.value].bindings, fields, operands,
          Sub::get_checker_descriptor()
              .variants[7]
              .operand_type_compatibilities,
          context);
      if (!operand_check) {
        diagnostics.insert(diagnostics.end(), operand_check.error().begin(),
                           operand_check.error().end());
      }
    }
    if (diagnostics.empty())
      return CheckResult{};
    return std::unexpected(std::move(diagnostics));
  };
  static_assert(detail::VariantCheckFunction<decltype(check_optional_sat),
                                             Sub::OptionalSat>);

  return std::visit(
      detail::Overloaded{check_float_f32, check_float_f32x2, check_float_f64,
                         check_half, check_bfloat, check_mixed_f32,
                         check_integer_no_sat, check_optional_sat},
      instruction.variant);
}

} // namespace checker

} // namespace ptx_frontend::resolved_ir
