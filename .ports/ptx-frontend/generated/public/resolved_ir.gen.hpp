// Generated by python/scripts/gen_all.py. Do not edit.
// Generated at: omitted (set SOURCE_DATE_EPOCH for a reproducible timestamp)
#pragma once

#include <optional>
#include <string>
#include <variant>
#include <vector>

#include <ptx_frontend/resolved_ir/ptx_resolved_ir.hpp>
#include <ptx_frontend/resolved_ir/ptx_resolved_ir_checker.hpp>

namespace ptx_frontend::resolved_ir {

struct Call {
  enum class VariantType {
    Direct,
  };

  // YAML: call_direct
  struct Direct {
    ResolvedOperandLayoutTag operand_layout;
    WithLocs<bool> uni;

    // YAML operand layout: target
    struct TargetOperands {
      WithLocs<ResolvedFunctionRef> target;
    };

    // YAML operand layout: target_input
    struct TargetInputOperands {
      WithLocs<ResolvedFunctionRef> target;
      WithLocs<ResolvedCallArguments> arguments;
    };

    // YAML operand layout: return_target_input
    struct ReturnTargetInputOperands {
      WithLocs<ResolvedCallParameterRef> return_value;
      WithLocs<ResolvedFunctionRef> target;
      WithLocs<ResolvedCallArguments> arguments;
    };

    // YAML operand layout: target_metadata
    struct TargetMetadataOperands {
      WithLocs<ResolvedIndirectCallee> target;
      WithLocs<ResolvedIndirectCallee> metadata;
    };

    // YAML operand layout: target_input_metadata
    struct TargetInputMetadataOperands {
      WithLocs<ResolvedIndirectCallee> target;
      WithLocs<ResolvedCallArguments> arguments;
      WithLocs<ResolvedIndirectCallee> metadata;
    };

    // YAML operand layout: return_target_input_metadata
    struct ReturnTargetInputMetadataOperands {
      WithLocs<ResolvedCallParameterRef> return_value;
      WithLocs<ResolvedIndirectCallee> target;
      WithLocs<ResolvedCallArguments> arguments;
      WithLocs<ResolvedIndirectCallee> metadata;
    };

    using Operands =
        std::variant<TargetOperands, TargetInputOperands,
                     ReturnTargetInputOperands, TargetMetadataOperands,
                     TargetInputMetadataOperands,
                     ReturnTargetInputMetadataOperands>;
    Operands operands;
  };

  using Variant = std::variant<Direct>;
  std::optional<WithLocs<ResolvedPredicate>> execution_predicate;
  Variant variant;

  static const check_end::SyntaxInstructionDescriptor &
  get_syntax_descriptor() noexcept;
  static const check_end::ResolvedInstructionDescriptor &
  get_resolved_descriptor() noexcept;
  static const checker::InstructionDescriptor &
  get_checker_descriptor() noexcept;
};

struct Bra {
  enum class VariantType {
    Direct,
  };

  // YAML: bra_direct
  struct Direct {
    ResolvedOperandLayoutTag operand_layout;
    WithLocs<bool> uni;
    WithLocs<ResolvedBranchTarget> target;
  };

  using Variant = std::variant<Direct>;
  std::optional<WithLocs<ResolvedPredicate>> execution_predicate;
  Variant variant;

  static const check_end::SyntaxInstructionDescriptor &
  get_syntax_descriptor() noexcept;
  static const check_end::ResolvedInstructionDescriptor &
  get_resolved_descriptor() noexcept;
  static const checker::InstructionDescriptor &
  get_checker_descriptor() noexcept;
};

struct Brx {
  enum class VariantType {
    Idx,
  };

  // YAML: brx_idx
  struct Idx {
    ResolvedOperandLayoutTag operand_layout;
    inline static constexpr bool idx = true;
    WithLocs<bool> uni;
    WithLocs<ResolvedRegisterRef> index;
    WithLocs<ResolvedBranchTargetSet> tlist;
  };

  using Variant = std::variant<Idx>;
  std::optional<WithLocs<ResolvedPredicate>> execution_predicate;
  Variant variant;

  static const check_end::SyntaxInstructionDescriptor &
  get_syntax_descriptor() noexcept;
  static const check_end::ResolvedInstructionDescriptor &
  get_resolved_descriptor() noexcept;
  static const checker::InstructionDescriptor &
  get_checker_descriptor() noexcept;
};

struct Mov {
  enum class VariantType {
    Scalar,
    Pred,
  };

  // YAML: mov_scalar
  struct Scalar {
    ResolvedOperandLayoutTag operand_layout;
    WithLocs<ScalarType> type;

    // YAML operand layout: scalar
    struct ScalarOperands {
      WithLocs<ResolvedRegisterRef> dst;
      WithLocs<ResolvedMovSource> src;
    };

    // YAML operand layout: pack
    struct PackOperands {
      WithLocs<ResolvedRegisterRef> dst;
      WithLocs<ResolvedRegisterVector> src;
    };

    // YAML operand layout: unpack
    struct UnpackOperands {
      WithLocs<ResolvedRegisterVector> dst;
      WithLocs<ResolvedRegisterRef> src;
    };

    using Operands = std::variant<ScalarOperands, PackOperands, UnpackOperands>;
    Operands operands;
  };

  // YAML: mov_pred
  struct Pred {
    ResolvedOperandLayoutTag operand_layout;
    inline static constexpr ScalarType type = ScalarType::Pred;
    WithLocs<ResolvedPredicate> dst;
    WithLocs<ResolvedPredicate> src;
  };

  using Variant = std::variant<Scalar, Pred>;
  std::optional<WithLocs<ResolvedPredicate>> execution_predicate;
  Variant variant;

  static const check_end::SyntaxInstructionDescriptor &
  get_syntax_descriptor() noexcept;
  static const check_end::ResolvedInstructionDescriptor &
  get_resolved_descriptor() noexcept;
  static const checker::InstructionDescriptor &
  get_checker_descriptor() noexcept;
};

struct Ld {
  enum class VariantType {
    GenericScalar,
    ExplicitScalar,
    GenericVector,
    ExplicitVector,
  };

  // YAML: ld_generic_scalar
  struct GenericScalar {
    ResolvedOperandLayoutTag operand_layout;
    WithLocs<MemoryConsistency> semantics;
    WithLocs<MemoryScope> scope;
    WithLocs<bool> mmio;
    WithLocs<CacheOperator> cache;
    WithLocs<ScalarType> type;
    WithLocs<ResolvedRegisterRef> dst;
    WithLocs<ResolvedAddress> address;
  };

  // YAML: ld_explicit_scalar
  struct ExplicitScalar {
    ResolvedOperandLayoutTag operand_layout;
    WithLocs<MemoryStateSpace> state_space;
    WithLocs<CacheOperator> cache;
    WithLocs<MemoryConsistency> semantics;
    WithLocs<MemoryScope> scope;
    WithLocs<bool> mmio;
    WithLocs<ScalarType> type;
    WithLocs<ResolvedRegisterRef> dst;
    WithLocs<ResolvedAddress> address;
  };

  // YAML: ld_generic_vector
  struct GenericVector {
    ResolvedOperandLayoutTag operand_layout;
    WithLocs<MemoryConsistency> semantics;
    WithLocs<MemoryScope> scope;
    WithLocs<CacheOperator> cache;
    WithLocs<VectorArity> vector;
    WithLocs<ScalarType> type;
    WithLocs<ResolvedRegisterVector> dst;
    WithLocs<ResolvedAddress> address;
  };

  // YAML: ld_explicit_vector
  struct ExplicitVector {
    ResolvedOperandLayoutTag operand_layout;
    WithLocs<MemoryStateSpace> state_space;
    WithLocs<CacheOperator> cache;
    WithLocs<MemoryConsistency> semantics;
    WithLocs<MemoryScope> scope;
    WithLocs<VectorArity> vector;
    WithLocs<ScalarType> type;
    WithLocs<ResolvedRegisterVector> dst;
    WithLocs<ResolvedAddress> address;
  };

  using Variant = std::variant<GenericScalar, ExplicitScalar, GenericVector,
                               ExplicitVector>;
  std::optional<WithLocs<ResolvedPredicate>> execution_predicate;
  Variant variant;

  static const check_end::SyntaxInstructionDescriptor &
  get_syntax_descriptor() noexcept;
  static const check_end::ResolvedInstructionDescriptor &
  get_resolved_descriptor() noexcept;
  static const checker::InstructionDescriptor &
  get_checker_descriptor() noexcept;
};

struct St {
  enum class VariantType {
    GenericScalar,
    ExplicitScalar,
    GenericVector,
    ExplicitVector,
  };

  // YAML: st_generic_scalar
  struct GenericScalar {
    ResolvedOperandLayoutTag operand_layout;
    WithLocs<MemoryConsistency> semantics;
    WithLocs<MemoryScope> scope;
    WithLocs<bool> mmio;
    WithLocs<CacheOperator> cache;
    WithLocs<ScalarType> type;
    WithLocs<ResolvedAddress> address;
    WithLocs<ResolvedRegisterRef> src;
  };

  // YAML: st_explicit_scalar
  struct ExplicitScalar {
    ResolvedOperandLayoutTag operand_layout;
    WithLocs<MemoryStateSpace> state_space;
    WithLocs<CacheOperator> cache;
    WithLocs<MemoryConsistency> semantics;
    WithLocs<MemoryScope> scope;
    WithLocs<bool> mmio;
    WithLocs<ScalarType> type;
    WithLocs<ResolvedAddress> address;
    WithLocs<ResolvedRegisterRef> src;
  };

  // YAML: st_generic_vector
  struct GenericVector {
    ResolvedOperandLayoutTag operand_layout;
    WithLocs<MemoryConsistency> semantics;
    WithLocs<MemoryScope> scope;
    WithLocs<CacheOperator> cache;
    WithLocs<VectorArity> vector;
    WithLocs<ScalarType> type;
    WithLocs<ResolvedAddress> address;
    WithLocs<ResolvedRegisterVector> src;
  };

  // YAML: st_explicit_vector
  struct ExplicitVector {
    ResolvedOperandLayoutTag operand_layout;
    WithLocs<MemoryStateSpace> state_space;
    WithLocs<CacheOperator> cache;
    WithLocs<MemoryConsistency> semantics;
    WithLocs<MemoryScope> scope;
    WithLocs<VectorArity> vector;
    WithLocs<ScalarType> type;
    WithLocs<ResolvedAddress> address;
    WithLocs<ResolvedRegisterVector> src;
  };

  using Variant = std::variant<GenericScalar, ExplicitScalar, GenericVector,
                               ExplicitVector>;
  std::optional<WithLocs<ResolvedPredicate>> execution_predicate;
  Variant variant;

  static const check_end::SyntaxInstructionDescriptor &
  get_syntax_descriptor() noexcept;
  static const check_end::ResolvedInstructionDescriptor &
  get_resolved_descriptor() noexcept;
  static const checker::InstructionDescriptor &
  get_checker_descriptor() noexcept;
};

struct Add {
  enum class VariantType {
    FloatF32,
    FloatF32x2,
    FloatF64,
    Half,
    Bfloat,
    MixedF32,
    IntegerNoSat,
    Sat,
    PackedOptionalSat,
  };

  // YAML: add_float_f32
  struct FloatF32 {
    ResolvedOperandLayoutTag operand_layout;
    WithLocs<RoundingMode> rounding;
    WithLocs<bool> ftz;
    WithLocs<bool> saturate;
    inline static constexpr ScalarType type = ScalarType::F32;
    WithLocs<ResolvedRegisterRef> dst;
    WithLocs<RegOrImm> src1;
    WithLocs<RegOrImm> src2;
  };

  // YAML: add_float_f32x2
  struct FloatF32x2 {
    ResolvedOperandLayoutTag operand_layout;
    WithLocs<RoundingMode> rounding;
    WithLocs<bool> ftz;
    inline static constexpr ScalarType type = ScalarType::F32x2;
    WithLocs<ResolvedRegisterRef> dst;
    WithLocs<ResolvedRegisterRef> src1;
    WithLocs<ResolvedRegisterRef> src2;
  };

  // YAML: add_float_f64
  struct FloatF64 {
    ResolvedOperandLayoutTag operand_layout;
    WithLocs<RoundingMode> rounding;
    inline static constexpr ScalarType type = ScalarType::F64;
    WithLocs<ResolvedRegisterRef> dst;
    WithLocs<RegOrImm> src1;
    WithLocs<RegOrImm> src2;
  };

  // YAML: add_half
  struct Half {
    ResolvedOperandLayoutTag operand_layout;
    WithLocs<RoundingMode> rounding;
    WithLocs<bool> ftz;
    WithLocs<bool> saturate;
    WithLocs<ScalarType> type;
    WithLocs<ResolvedRegisterRef> dst;
    WithLocs<ResolvedRegisterRef> src1;
    WithLocs<ResolvedRegisterRef> src2;
  };

  // YAML: add_bfloat
  struct Bfloat {
    ResolvedOperandLayoutTag operand_layout;
    WithLocs<RoundingMode> rounding;
    WithLocs<ScalarType> type;
    WithLocs<ResolvedRegisterRef> dst;
    WithLocs<ResolvedRegisterRef> src1;
    WithLocs<ResolvedRegisterRef> src2;
  };

  // YAML: add_mixed_f32
  struct MixedF32 {
    ResolvedOperandLayoutTag operand_layout;
    WithLocs<RoundingMode> rounding;
    inline static constexpr ScalarType result_type = ScalarType::F32;
    WithLocs<ScalarType> input_type;
    WithLocs<bool> saturate;
    WithLocs<ResolvedRegisterRef> dst;
    WithLocs<ResolvedRegisterRef> src;
    WithLocs<ResolvedRegisterRef> addend;
  };

  // YAML: add_integer_no_sat
  struct IntegerNoSat {
    ResolvedOperandLayoutTag operand_layout;
    WithLocs<ScalarType> type;
    WithLocs<ResolvedRegisterRef> dst;
    WithLocs<RegOrImm> src1;
    WithLocs<RegOrImm> src2;
  };

  // YAML: add_sat
  struct Sat {
    ResolvedOperandLayoutTag operand_layout;
    inline static constexpr bool saturate = true;
    WithLocs<ScalarType> type;
    WithLocs<ResolvedRegisterRef> dst;
    WithLocs<RegOrImm> src1;
    WithLocs<RegOrImm> src2;
  };

  // YAML: add_packed_optional_sat
  struct PackedOptionalSat {
    ResolvedOperandLayoutTag operand_layout;
    WithLocs<bool> saturate;
    WithLocs<ScalarType> type;
    WithLocs<ResolvedRegisterRef> dst;
    WithLocs<RegOrImm> src1;
    WithLocs<RegOrImm> src2;
  };

  using Variant = std::variant<FloatF32, FloatF32x2, FloatF64, Half, Bfloat,
                               MixedF32, IntegerNoSat, Sat, PackedOptionalSat>;
  std::optional<WithLocs<ResolvedPredicate>> execution_predicate;
  Variant variant;

  static const check_end::SyntaxInstructionDescriptor &
  get_syntax_descriptor() noexcept;
  static const check_end::ResolvedInstructionDescriptor &
  get_resolved_descriptor() noexcept;
  static const checker::InstructionDescriptor &
  get_checker_descriptor() noexcept;
};

struct Sub {
  enum class VariantType {
    FloatF32,
    FloatF32x2,
    FloatF64,
    Half,
    Bfloat,
    MixedF32,
    IntegerNoSat,
    OptionalSat,
  };

  // YAML: sub_float_f32
  struct FloatF32 {
    ResolvedOperandLayoutTag operand_layout;
    WithLocs<RoundingMode> rounding;
    WithLocs<bool> ftz;
    WithLocs<bool> saturate;
    inline static constexpr ScalarType type = ScalarType::F32;
    WithLocs<ResolvedRegisterRef> dst;
    WithLocs<RegOrImm> src1;
    WithLocs<RegOrImm> src2;
  };

  // YAML: sub_float_f32x2
  struct FloatF32x2 {
    ResolvedOperandLayoutTag operand_layout;
    WithLocs<RoundingMode> rounding;
    WithLocs<bool> ftz;
    inline static constexpr ScalarType type = ScalarType::F32x2;
    WithLocs<ResolvedRegisterRef> dst;
    WithLocs<ResolvedRegisterRef> src1;
    WithLocs<ResolvedRegisterRef> src2;
  };

  // YAML: sub_float_f64
  struct FloatF64 {
    ResolvedOperandLayoutTag operand_layout;
    WithLocs<RoundingMode> rounding;
    inline static constexpr ScalarType type = ScalarType::F64;
    WithLocs<ResolvedRegisterRef> dst;
    WithLocs<RegOrImm> src1;
    WithLocs<RegOrImm> src2;
  };

  // YAML: sub_half
  struct Half {
    ResolvedOperandLayoutTag operand_layout;
    WithLocs<RoundingMode> rounding;
    WithLocs<bool> ftz;
    WithLocs<bool> saturate;
    WithLocs<ScalarType> type;
    WithLocs<ResolvedRegisterRef> dst;
    WithLocs<ResolvedRegisterRef> src1;
    WithLocs<ResolvedRegisterRef> src2;
  };

  // YAML: sub_bfloat
  struct Bfloat {
    ResolvedOperandLayoutTag operand_layout;
    WithLocs<RoundingMode> rounding;
    WithLocs<ScalarType> type;
    WithLocs<ResolvedRegisterRef> dst;
    WithLocs<ResolvedRegisterRef> src1;
    WithLocs<ResolvedRegisterRef> src2;
  };

  // YAML: sub_mixed_f32
  struct MixedF32 {
    ResolvedOperandLayoutTag operand_layout;
    WithLocs<RoundingMode> rounding;
    inline static constexpr ScalarType result_type = ScalarType::F32;
    WithLocs<ScalarType> input_type;
    WithLocs<bool> saturate;
    WithLocs<ResolvedRegisterRef> dst;
    WithLocs<ResolvedRegisterRef> src;
    WithLocs<ResolvedRegisterRef> subtrahend;
  };

  // YAML: sub_integer_no_sat
  struct IntegerNoSat {
    ResolvedOperandLayoutTag operand_layout;
    WithLocs<ScalarType> type;
    WithLocs<ResolvedRegisterRef> dst;
    WithLocs<RegOrImm> src1;
    WithLocs<RegOrImm> src2;
  };

  // YAML: sub_optional_sat
  struct OptionalSat {
    ResolvedOperandLayoutTag operand_layout;
    WithLocs<bool> saturate;
    WithLocs<ScalarType> type;
    WithLocs<ResolvedRegisterRef> dst;
    WithLocs<RegOrImm> src1;
    WithLocs<RegOrImm> src2;
  };

  using Variant = std::variant<FloatF32, FloatF32x2, FloatF64, Half, Bfloat,
                               MixedF32, IntegerNoSat, OptionalSat>;
  std::optional<WithLocs<ResolvedPredicate>> execution_predicate;
  Variant variant;

  static const check_end::SyntaxInstructionDescriptor &
  get_syntax_descriptor() noexcept;
  static const check_end::ResolvedInstructionDescriptor &
  get_resolved_descriptor() noexcept;
  static const checker::InstructionDescriptor &
  get_checker_descriptor() noexcept;
};

struct Bar {
  enum class VariantType {
    Sync,
    CtaSync,
    Arrive,
    CtaArrive,
    RedPopcU32,
    CtaRedPopcU32,
    RedAndPred,
    CtaRedAndPred,
    RedOrPred,
    CtaRedOrPred,
  };

  // YAML: bar_sync
  struct Sync {
    ResolvedOperandLayoutTag operand_layout;
    inline static constexpr bool sync = true;

    // YAML operand layout: immediate_barrier
    struct ImmediateBarrierOperands {
      WithLocs<ResolvedImmediate> barrier;
    };

    // YAML operand layout: barrier
    struct BarrierOperands {
      WithLocs<RegOrImm> barrier;
    };

    // YAML operand layout: barrier_and_thread_count
    struct BarrierAndThreadCountOperands {
      WithLocs<RegOrImm> barrier;
      WithLocs<RegOrImm> thread_count;
    };

    using Operands = std::variant<ImmediateBarrierOperands, BarrierOperands,
                                  BarrierAndThreadCountOperands>;
    Operands operands;
  };

  // YAML: bar_cta_sync
  struct CtaSync {
    ResolvedOperandLayoutTag operand_layout;
    inline static constexpr bool cta = true;
    inline static constexpr bool sync = true;

    // YAML operand layout: barrier
    struct BarrierOperands {
      WithLocs<RegOrImm> barrier;
    };

    // YAML operand layout: barrier_and_thread_count
    struct BarrierAndThreadCountOperands {
      WithLocs<RegOrImm> barrier;
      WithLocs<RegOrImm> thread_count;
    };

    using Operands =
        std::variant<BarrierOperands, BarrierAndThreadCountOperands>;
    Operands operands;
  };

  // YAML: bar_arrive
  struct Arrive {
    ResolvedOperandLayoutTag operand_layout;
    inline static constexpr bool arrive = true;
    WithLocs<RegOrImm> barrier;
    WithLocs<RegOrImm> thread_count;
  };

  // YAML: bar_cta_arrive
  struct CtaArrive {
    ResolvedOperandLayoutTag operand_layout;
    inline static constexpr bool cta = true;
    inline static constexpr bool arrive = true;
    WithLocs<RegOrImm> barrier;
    WithLocs<RegOrImm> thread_count;
  };

  // YAML: bar_red_popc_u32
  struct RedPopcU32 {
    ResolvedOperandLayoutTag operand_layout;
    inline static constexpr bool red = true;
    inline static constexpr bool reduction = true;
    inline static constexpr ScalarType result_type = ScalarType::U32;

    // YAML operand layout: without_thread_count
    struct WithoutThreadCountOperands {
      WithLocs<ResolvedRegisterRef> dst;
      WithLocs<RegOrImm> barrier;
      WithLocs<ResolvedPredicate> predicate;
    };

    // YAML operand layout: with_thread_count
    struct WithThreadCountOperands {
      WithLocs<ResolvedRegisterRef> dst;
      WithLocs<RegOrImm> barrier;
      WithLocs<RegOrImm> thread_count;
      WithLocs<ResolvedPredicate> predicate;
    };

    using Operands =
        std::variant<WithoutThreadCountOperands, WithThreadCountOperands>;
    Operands operands;
  };

  // YAML: bar_cta_red_popc_u32
  struct CtaRedPopcU32 {
    ResolvedOperandLayoutTag operand_layout;
    inline static constexpr bool cta = true;
    inline static constexpr bool red = true;
    inline static constexpr bool reduction = true;
    inline static constexpr ScalarType result_type = ScalarType::U32;

    // YAML operand layout: without_thread_count
    struct WithoutThreadCountOperands {
      WithLocs<ResolvedRegisterRef> dst;
      WithLocs<RegOrImm> barrier;
      WithLocs<ResolvedPredicate> predicate;
    };

    // YAML operand layout: with_thread_count
    struct WithThreadCountOperands {
      WithLocs<ResolvedRegisterRef> dst;
      WithLocs<RegOrImm> barrier;
      WithLocs<RegOrImm> thread_count;
      WithLocs<ResolvedPredicate> predicate;
    };

    using Operands =
        std::variant<WithoutThreadCountOperands, WithThreadCountOperands>;
    Operands operands;
  };

  // YAML: bar_red_and_pred
  struct RedAndPred {
    ResolvedOperandLayoutTag operand_layout;
    inline static constexpr bool red = true;
    inline static constexpr bool reduction = true;
    inline static constexpr ScalarType result_type = ScalarType::Pred;

    // YAML operand layout: without_thread_count
    struct WithoutThreadCountOperands {
      WithLocs<ResolvedPredicate> dst;
      WithLocs<RegOrImm> barrier;
      WithLocs<ResolvedPredicate> predicate;
    };

    // YAML operand layout: with_thread_count
    struct WithThreadCountOperands {
      WithLocs<ResolvedPredicate> dst;
      WithLocs<RegOrImm> barrier;
      WithLocs<RegOrImm> thread_count;
      WithLocs<ResolvedPredicate> predicate;
    };

    using Operands =
        std::variant<WithoutThreadCountOperands, WithThreadCountOperands>;
    Operands operands;
  };

  // YAML: bar_cta_red_and_pred
  struct CtaRedAndPred {
    ResolvedOperandLayoutTag operand_layout;
    inline static constexpr bool cta = true;
    inline static constexpr bool red = true;
    inline static constexpr bool reduction = true;
    inline static constexpr ScalarType result_type = ScalarType::Pred;

    // YAML operand layout: without_thread_count
    struct WithoutThreadCountOperands {
      WithLocs<ResolvedPredicate> dst;
      WithLocs<RegOrImm> barrier;
      WithLocs<ResolvedPredicate> predicate;
    };

    // YAML operand layout: with_thread_count
    struct WithThreadCountOperands {
      WithLocs<ResolvedPredicate> dst;
      WithLocs<RegOrImm> barrier;
      WithLocs<RegOrImm> thread_count;
      WithLocs<ResolvedPredicate> predicate;
    };

    using Operands =
        std::variant<WithoutThreadCountOperands, WithThreadCountOperands>;
    Operands operands;
  };

  // YAML: bar_red_or_pred
  struct RedOrPred {
    ResolvedOperandLayoutTag operand_layout;
    inline static constexpr bool red = true;
    inline static constexpr bool reduction = true;
    inline static constexpr ScalarType result_type = ScalarType::Pred;

    // YAML operand layout: without_thread_count
    struct WithoutThreadCountOperands {
      WithLocs<ResolvedPredicate> dst;
      WithLocs<RegOrImm> barrier;
      WithLocs<ResolvedPredicate> predicate;
    };

    // YAML operand layout: with_thread_count
    struct WithThreadCountOperands {
      WithLocs<ResolvedPredicate> dst;
      WithLocs<RegOrImm> barrier;
      WithLocs<RegOrImm> thread_count;
      WithLocs<ResolvedPredicate> predicate;
    };

    using Operands =
        std::variant<WithoutThreadCountOperands, WithThreadCountOperands>;
    Operands operands;
  };

  // YAML: bar_cta_red_or_pred
  struct CtaRedOrPred {
    ResolvedOperandLayoutTag operand_layout;
    inline static constexpr bool cta = true;
    inline static constexpr bool red = true;
    inline static constexpr bool reduction = true;
    inline static constexpr ScalarType result_type = ScalarType::Pred;

    // YAML operand layout: without_thread_count
    struct WithoutThreadCountOperands {
      WithLocs<ResolvedPredicate> dst;
      WithLocs<RegOrImm> barrier;
      WithLocs<ResolvedPredicate> predicate;
    };

    // YAML operand layout: with_thread_count
    struct WithThreadCountOperands {
      WithLocs<ResolvedPredicate> dst;
      WithLocs<RegOrImm> barrier;
      WithLocs<RegOrImm> thread_count;
      WithLocs<ResolvedPredicate> predicate;
    };

    using Operands =
        std::variant<WithoutThreadCountOperands, WithThreadCountOperands>;
    Operands operands;
  };

  using Variant =
      std::variant<Sync, CtaSync, Arrive, CtaArrive, RedPopcU32, CtaRedPopcU32,
                   RedAndPred, CtaRedAndPred, RedOrPred, CtaRedOrPred>;
  std::optional<WithLocs<ResolvedPredicate>> execution_predicate;
  Variant variant;

  static const check_end::SyntaxInstructionDescriptor &
  get_syntax_descriptor() noexcept;
  static const check_end::ResolvedInstructionDescriptor &
  get_resolved_descriptor() noexcept;
  static const checker::InstructionDescriptor &
  get_checker_descriptor() noexcept;
};

using ResolvedInstruction =
    std::variant<Call, Bra, Brx, Mov, Ld, St, Add, Sub, Bar>;

struct ResolvedFunction {
  binding::SymbolId symbol_id;
  std::string name;
  bool is_entry{};
  bool is_prototype{};
  std::vector<ResolvedInstruction> body;
  SourceRange range;
};

struct ResolvedModule {
  binding::SymbolTable symbols;
  std::vector<ResolvedFunction> functions;
  SourceRange range;
};

std::expected<ResolvedInstruction, ResolveDiagnostic>
resolveInstruction(const syntax_ast::AstInstruction &ast);

std::expected<ResolvedInstruction, ResolveDiagnostic>
resolveInstruction(const syntax_ast::AstInstruction &ast,
                   const ResolveContext &context);

using ModuleResolveDiagnostics = std::vector<ResolveDiagnostic>;

std::expected<ResolvedModule, ModuleResolveDiagnostics>
resolveModule(const syntax_ast::AstModule &ast);

template <>
std::expected<Call, ResolveDiagnostic>
resolve<Call>(const syntax_ast::AstInstruction &ast,
              const ResolveContext *context);

template <>
std::expected<Bra, ResolveDiagnostic>
resolve<Bra>(const syntax_ast::AstInstruction &ast,
             const ResolveContext *context);

template <>
std::expected<Brx, ResolveDiagnostic>
resolve<Brx>(const syntax_ast::AstInstruction &ast,
             const ResolveContext *context);

template <>
std::expected<Mov, ResolveDiagnostic>
resolve<Mov>(const syntax_ast::AstInstruction &ast,
             const ResolveContext *context);

template <>
std::expected<Ld, ResolveDiagnostic>
resolve<Ld>(const syntax_ast::AstInstruction &ast,
            const ResolveContext *context);

template <>
std::expected<St, ResolveDiagnostic>
resolve<St>(const syntax_ast::AstInstruction &ast,
            const ResolveContext *context);

template <>
std::expected<Add, ResolveDiagnostic>
resolve<Add>(const syntax_ast::AstInstruction &ast,
             const ResolveContext *context);

template <>
std::expected<Sub, ResolveDiagnostic>
resolve<Sub>(const syntax_ast::AstInstruction &ast,
             const ResolveContext *context);

template <>
std::expected<Bar, ResolveDiagnostic>
resolve<Bar>(const syntax_ast::AstInstruction &ast,
             const ResolveContext *context);

namespace checker {

template <>
CheckResult check<Call>(const Call &instruction, const Context &context);

template <>
CheckResult check<Bra>(const Bra &instruction, const Context &context);

template <>
CheckResult check<Brx>(const Brx &instruction, const Context &context);

template <>
CheckResult check<Mov>(const Mov &instruction, const Context &context);

template <>
CheckResult check<Ld>(const Ld &instruction, const Context &context);

template <>
CheckResult check<St>(const St &instruction, const Context &context);

template <>
CheckResult check<Add>(const Add &instruction, const Context &context);

template <>
CheckResult check<Sub>(const Sub &instruction, const Context &context);

template <>
CheckResult check<Bar>(const Bar &instruction, const Context &context);

} // namespace checker

} // namespace ptx_frontend::resolved_ir
