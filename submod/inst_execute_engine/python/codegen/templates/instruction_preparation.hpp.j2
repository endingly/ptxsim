// Generated by ptxsim_codegen.inst_execute_engine. Do not edit.
#pragma once

#include <expected>
#include <optional>

#include <ptxsim/arith/context.hpp>
#include <ptxsim/exec_ir/exec_ir.hpp>

#include "prepared_effect.hpp"
#include "resource_resolution.hpp"

namespace ptxsim::inst_execute_engine::detail::generated {

/** @brief Validate every projected {{ mov_operation.cpp_name }} form before lane resources are read. */
auto validate_{{ mov_operation.opcode }}(const exec_ir::Instruction& instruction) noexcept -> bool;

/** @brief Prepare every projected {{ mov_operation.cpp_name }} form without architectural mutation. */
auto prepare_{{ mov_operation.opcode }}(LaneResourceResolver& resolver, const arith::context& arithmetic,
                 const exec_ir::Instruction& instruction,
                 std::optional<common::ProgramCounter> successor)
    -> std::expected<PreparedEffect, LaneFaultCause>;

{% for operation in operations %}/** @brief Validate the projected {{ operation.cpp_name }} form before any lane resources are read. */
auto validate_{{ operation.opcode }}(const exec_ir::Instruction& instruction) noexcept -> bool;

/**
 * @brief Prepare every projected {{ operation.cpp_name }} form without architectural mutation.
 * @pre @ref validate_{{ operation.opcode }} has accepted @p instruction and @p successor is set.
 */
auto prepare_{{ operation.opcode }}(LaneResourceResolver& resolver, const arith::context& arithmetic,
                 const exec_ir::Instruction& instruction,
                 std::optional<common::ProgramCounter> successor)
    -> std::expected<PreparedEffect, LaneFaultCause>;

{% endfor %}
{% for operation in memory_operations %}/** @brief Validate the projected {{ operation.cpp_name }} form before any lane resources are read. */
auto validate_{{ operation.opcode }}(const exec_ir::Instruction& instruction) noexcept -> bool;

/** @brief Prepare every supported ordinary {{ operation.cpp_name }} form without mutation. */
auto prepare_{{ operation.opcode }}(LaneResourceResolver& resolver, const arith::context& arithmetic,
                 const exec_ir::Instruction& instruction,
                 std::optional<common::ProgramCounter> successor)
    -> std::expected<PreparedEffect, LaneFaultCause>;

{% endfor %}
/** @brief Validate the projected {{ setp_operation.cpp_name }} form before any lane resources are read. */
auto validate_{{ setp_operation.opcode }}(const exec_ir::Instruction& instruction) noexcept -> bool;

/**
 * @brief Prepare every projected {{ setp_operation.cpp_name }} form without architectural mutation.
 * @pre @ref validate_{{ setp_operation.opcode }} has accepted @p instruction and @p successor is set.
 */
auto prepare_{{ setp_operation.opcode }}(LaneResourceResolver& resolver, const arith::context& arithmetic,
                 const exec_ir::Instruction& instruction,
                 std::optional<common::ProgramCounter> successor)
    -> std::expected<PreparedEffect, LaneFaultCause>;

/** @brief Validate every projected collective {{ bar_operation.cpp_name }} form before lane resources are read. */
auto validate_{{ bar_operation.opcode }}(const exec_ir::Instruction& instruction) noexcept -> bool;

/** @brief Prepare every projected {{ bar_operation.cpp_name }} collective form without mutation. */
auto prepare_{{ bar_operation.opcode }}(LaneResourceResolver& resolver, const arith::context& arithmetic,
                 const exec_ir::Instruction& instruction,
                 std::optional<common::ProgramCounter> successor)
    -> std::expected<PreparedEffect, LaneFaultCause>;

/** @brief Validate the projected direct {{ bra_operation.cpp_name }} form before control selection. */
auto validate_{{ bra_operation.opcode }}(const exec_ir::Instruction& instruction) noexcept -> bool;

/** @brief Prepare the projected direct {{ bra_operation.cpp_name }} form without resolving lane resources. */
auto prepare_{{ bra_operation.opcode }}(LaneResourceResolver& resolver, const arith::context& arithmetic,
                 const exec_ir::Instruction& instruction,
                 std::optional<common::ProgramCounter> successor)
    -> std::expected<PreparedEffect, LaneFaultCause>;

/** @brief Validate the projected bare {{ exit_operation.cpp_name }} form before control selection. */
auto validate_{{ exit_operation.opcode }}(const exec_ir::Instruction& instruction) noexcept -> bool;

/** @brief Prepare the projected bare {{ exit_operation.cpp_name }} form without resolving lane resources. */
auto prepare_{{ exit_operation.opcode }}(LaneResourceResolver& resolver, const arith::context& arithmetic,
                 const exec_ir::Instruction& instruction,
                 std::optional<common::ProgramCounter> successor)
    -> std::expected<PreparedEffect, LaneFaultCause>;

}  // namespace ptxsim::inst_execute_engine::detail::generated
